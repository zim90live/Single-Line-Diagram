# 项目目标

最后更新：2026-09-09

## 愿景

创建一个面向 AIDC 场景的一次接线图绘制/监控系统，为电气一次系统的图形化建模、展示和运行监控提供统一工作界面。

## 当前已确认目标

1. 建设编辑、监控双模式的 AIDC 一次接线图系统。
2. 系统覆盖冷却、电力两套线路。
3. 每套线路支持多层级图纸，已明确的层级示例包括园区图、楼宇图、POD 图和设备图。
4. 支持一次接线图相关对象的运行监控，监控模式需要包含状态动画、趋势/指标图表和数据模拟能力。
5. 让项目目标、关键决策、任务状态和历史记录持续与实现同步，减少返工。
6. 让监控运行时可合入 AIDC React 项目；两个项目的基础 UI 组件、公共 Token、Recipe、样式和可访问行为最终只由 AIDC 的 `@aidc/ui` 单一真源提供，一次接线图仅保留线路 Domain Token 与业务 Pattern。

## 目标用户与交付形态

- 主要用户：AIDC 运维人员。
- 交付形态：Web 系统；第一阶段按桌面端编辑场景规划。

## 当前阶段

2026-09-10 电力子线支持实例粗细及批量配置，编辑与监控同步，Schema v43，详见 KD-200。

2026-09-10 模拟数据严格使用编辑配置的指标列表及显示开关；不足两项时不再通过额外指标强求红黄异常覆盖（KD-199 补充）。

2026-09-10 演示模拟数据：每张含支持模拟设备的图纸分配一红一黄两项数值异常，仍保留每页一个在线故障设备；数据是否呈现仅由编辑模式开关手动控制，兼容运行包导出（KD-199）。

2026-09-09 增加“文字”通用图元，用于图纸纯文字说明，可配置内容、文字颜色、字号与字重，默认 24px Bold #888888。

2026-09-09 分接单元供电修复：Tap-off Unit-group 的 A/B 输入分别传递到对应输出，不再作为供电终点截断；FM 继续 A 优先/B 备用，原 Cabinet 终点规则保持。

2026-09-09 MP 改为通用传感器：素材分类为通用，冷却/电力均可使用同一稳定键和单测量锚点；端口按所在图纸适配，电力新实例默认仅配置温度，冷却保留温度/压力/流量，已有指标保持。漏液标识仍仅作用于对应冷却网络。

2026-09-09 制冷动画速度与求解流量解耦：各模式直接使用其冷却 `baseSpeed` 配置，流量仅保留流向及流动资格判断。

2026-09-09 圆点替代箭头：电路默认圆点、冷却默认光波，两系统独立记忆会话动画模式，切换线路 Tab 不互相覆盖；圆点直径和圆心间距在 `FLOW_DOT_ANIMATION_STYLES` 按系统独立配置。

2026-09-09 虚线动画间隔保留同色，以较低不透明度显示；电路、水路分别配置 `gapOpacity`，默认 0.25，不增加几何或逐帧 React 更新。

2026-09-09 动画新增“虚线”：与光波、箭头并列，电路和水路分别配置移动速度、线段长度、空隙长度，参数位于 `src/monitoring/flowPresentation.ts` 的 `FLOW_DASH_ANIMATION_STYLES`。暂停冻结，沿用运行态流向及水流速度倍率，会话选项不写存档。

2026-09-09 供电方式告警修正：演示指标“主路 / 主路供电”始终正常，只有“旁路 / 旁路供电”显示异常；兼容旧候选等级配置。

2026-09-09 漏液范围修订：仅“TMU到FM 01”和“TMU到FM 02”各选一个冷却管网，优先带 MP 的网络；关联传感器标签增加“疑似漏液”，与管路红色闪烁对应。其他图纸不模拟漏液，详见 KD-191。

2026-09-09 疑似漏液首版：三次冷/热改为深紫 #7C5CFF / 亮粉 #F06BD8，释放红色用于漏液。监控每张冷却图稳定选一个非空管网，将现有两侧描边改为红色两秒明暗闪动；管腔、流动、拓扑保持，不标具体漏点，不加详情。

2026-09-09 演示故障分配：每张含可模拟设备的图纸固定一个在线故障，优先选有 Fault SVG 的设备，隐藏指标不排除故障候选；固定种子下位置稳定。取代此前每页 1–3 个异常及随机离线分配，故障不切断电流/水流。

2026-09-09 故障素材：Battery-group、Tap-off Unit-group（兼容旧 A/B）、Cabinet、UPS-group、UPS、FM、TMU、CDU、CT 在监控三级异常时切换专用 Fault SVG，恢复正常后还原。故障只表达外观，不新增断电或断水条件。

2026-09-09 FM 实例支持上下管路接口对调，保持图元方向及接口语义，画布和接线同步；Schema v42，详见 KD-185。

2026-09-09 液冷编辑选中反馈：主要/辅助选中轨道调整为 10px/6px 中性亮边，选中管腔提亮并保留回路色，避免轨道被加宽管腔覆盖；详见 KD-184。

2026-09-09 分接单元素材统一：Tap-off Unit A/B 均显示为 48×48 Tap-off Unit-group，素材栏仅提供一个入口。历史 `cabinet` / `cabinet-b` 模板键保留以维持各自锚点、接线和下探语义；旧默认实例名同步更新，自定义设备标识保持。

2026-09-09 动画样式：监控播放按钮旁新增“光波 / 箭头”，默认保留光波；实心三角方向箭头使用 5×4 图纸单位、20 单位间距，随图纸缩放并沿实际流向移动，直接使用线路/管路颜色、不混白（KD-183），取消固定位置明暗推进（KD-181），暂停冻结、停流隐藏。主画布、嵌入设备图与 React 运行时入口支持，样式属于会话偏好，不写项目，详见 KD-177。

2026-09-09 标签与导航增量：母线支持运行数据；图元标签提供可保存的项目级字号缩放，指标名称左对齐，文本与数值分别按紧凑列宽右对齐；监控默认播放，跳转图纸按含线路和标签的完整场景在面板外取景。详见 KD-172 / KD-173，Schema v41。

2026-09-08 电力线宽增量：子线与母线统一加粗至此前的 1.5 倍（2.25px / 6px），选中态及光波同比例调整，编辑/监控/设备面板小接线图一致；详见 KD-171。

2026-09-08 制冷表达更新：采用清晰两侧描边与暗色管腔，主要 `8px / 6px`、辅助 `4px / 2.4px`；有效水流使用管路本色的亮头与单向线性渐隐长拖尾（96 图纸单位长度、2 单位连续亮度过渡（KD-182），缩放不重排波形；动画宽度为管腔的 80%，尾部/头部不透明度 50%/100%，KD-168）；同网同流速子线及跨线遮挡前后连续累计动画距离，避免按片段重启（KD-166）；跨子线的转弯共用几何接头，活动分叉无透明间隙（KD-167），不使用内阴影、颗粒或辉光。制冷暂停保留冻结拖尾，停流撤去拖尾；液冷光波基准速度为 168 图纸单位/秒（KD-182）；电力统一采用同色渐变光波，48 单位波长、1 单位过渡、168 单位/秒，活动静态底线压暗以显示光波，无电流线路保留原色 50% 以确保可读（KD-170）。旧双层材质与制冷虚线条款以 KD-164 为准，拓扑、回路颜色和流量计算不变。

2026-09-08 泵素材更新：CHWP、CWP 运行素材替换为最新 80×128 SVG，监控关闭态共用同尺寸 `CHWP CWP Off.svg`；实例尺寸与接口保持。

2026-09-08 TMU 布局增量：支持单实例上下接口对调，不改变图元方向、左右接口位置、回路语义或原接线；正式路由与监控共用派生接口位置，Schema v40 保存实例配置，详见 KD-162。

2026-09-08 项目级配色：电力网络/母线可按 A/B/未分配分类，冷却沿用六种回路/通用；已分配线路和锚点统一跟随项目配色，空选面板可编辑八种类型色，未分配线路仍可单独改色并保留此前颜色。Schema v39 及 RuntimeBundle 携带配置，详见 KD-161。

2026-09-08 素材更新：UPS-group、CDU 替换为最新附件，均以 SVG 原始 48×48 尺寸登记；CDU 历史版式按原实例缩放比例迁移为正方形，保持中心附近网格位置与锚点引用。

2026-09-08 管网回路编辑：选中冷却子线后右侧显示并允许整网修改回路类型，连接特定回路接口时锁定，多选批量遵循同样约束；不修改图元接口，详见 KD-157。

2026-09-08 开关标签：监控画布隐藏 Switch 正常开启/关闭状态指标行，由图元表达开关状态；设备标识、其他指标及异常文字继续显示。

2026-09-08 母线端点出线增量：两个几何端点允许子线垂直出线或沿母线向外延伸；中间接入点保持垂直出线。接线预览与正式路由共用该规则，接入位置和拓扑引用不变。

2026-09-08 三次回路增量：TMU–FM 独立末端回路新增紫色冷端 `#A970FF` 与红色热端 `#FF4D6D`，与二次换热隔离；编辑端口/接线及监控闭环模拟支持三次回路。Schema v38 兼容旧项目，不自动改型既有二次网络，详见 KD-154。

2026-09-07 素材替换增量：TMU、FM、UPS-group、Cabinet 均替换为用户最新提供的 SVG。TMU 按附件根画布采用 48×48 逻辑尺寸；既有 48×64、72×96、64×96 默认实例在载入时迁移到 48×48，稳定键、冷却分类、锚点和接线保持。其余三者继续保持各自既有逻辑尺寸。图元键、监控语义和 Schema 均不变。

2026-09-07 跨线修订：电力和冷却线路以直线跨过下层线，保留上层局部遮罩，不再绘制拱桥圆弧；编辑、监控静态及动画一致，普通管路圆角和跨线层级保持，详见 KD-152。

2026-09-07 冷却材质修订：外层管体取线路自身配置色（缺省回路色），主要/辅助分别混入 70% / 85% 黑色后不透明绘制，避免重叠叠色；保留既有粗细、内芯及动画。KD-153 已取代 KD-151 的透明度方案。

第一阶段基础编辑与图元锚点编辑器已于 2026-08-14 通过用户视觉与操作验收。线路连接已改为同锚点多条逻辑边与网络内可自然重合的派生路径，固定子线分叉入口和可见分叉点已移除；电力母线、tap 自动滑移、锚点方向感知路由、不同母线普通子线互联和母线缩短时的 tap 受约束压缩均已完成实现和非视觉验证。图元与母线现可混合框选、移动、旋转，并统一支持复制、粘贴和 Duplicate；选区内两端对象均被复制的逻辑接线关系会同步生成新 tap、节点、边和网络，连向选区外对象的支路不复制。框选或追加多选显式包含线路节点时，Copy/Paste 与 Duplicate 还会带上该节点直接接触的子线段；未选图元或母线一侧转换为同位置自由端点，不递归复制外部对象或下一段支路。母线和逻辑子线现可在属性面板使用应用内 HEX 颜色控件改色；子线支持跨网络追加多选，并可与一条或多条母线组成线路混合选择，统一批量预览、提交或恢复颜色。网络主干选择会批量作用于网络内全部子线，多色选择明确显示“多种颜色”；母线 tap 与候选连接点继承所属母线颜色。空选属性面板按图元、母线、子线汇总当前画布实际颜色与使用数量，可一次替换同类同色对象；Switch 关/开颜色作为独立槽位汇总和替换。每个图元设备标识默认显示，单选图元后可在右侧属性面板独立隐藏或恢复；母线与独立逻辑子线均可填写可选文字标签，空值始终隐藏，非空标签也可通过当前对象的独立开关隐藏或恢复。子线标签绑定源或目标端点，并根据端点附近派生线段的方向放在横线的上/下侧或竖线的左/右侧。独立逻辑子线还可配置双向、起点→终点或终点→起点通行；单向子线在配置终点显示固定屏幕尺寸箭头，电力拓扑不允许逆箭头传播。三类标签均保持水平世界空间文字，并支持各自既定的方位交互。图元拖动、母线拉伸、方向键连续移动与起线跟随的高频路径已改为逐帧合并和轻量几何预览；正式路由由可取消的 latest-wins Web Worker 生成派生快照，并按直接连接关系和空间影响走廊只重算脏网络。单次任务复用障碍网格，交叉与标签避让使用空间索引，拱桥显示路径按路由身份缓存；大图静态 SVG 使用视口 overscan 裁剪，接线锚点按需挂载，同色图元共享滤镜。正式普通子线不再因同网络已有主干获得复用加分；等长少折线路把拐点居中作为唯一造型偏好。非连接交叉已使用带局部底衬的真圆弧拱桥。当前等待用户继续检查设备标识显隐与标签、母线与子线端点标签、子线单向箭头、混合选择、拖动/拉伸/方向键移动/终点悬停流畅度、自然重合路径、中点拐点、类型颜色、避障/拱桥以及母线相关效果。监控模式首轮已实现顶部模式 Tab、只读画布、Switch 独立自动保存，以 Grid、Generator、Battery 为 Source、算力 POD、动力 POD、FM 为 Target 的有效最短业务路径动画，以及 Off Switch 两侧相邻线路停流和 WebGL 深色移动虚线；闭合但没有 Target 的整条电力链路及有效主干旁的死端支路均不动画。每个图元实例的运行指标现同时支持数值读数和文本状态，编辑模式稳定预览，监控模式每 30 秒刷新且文本模拟正常状态占绝大多数；右侧属性面板可用一个实例级总开关统一隐藏或恢复全部指标名称与单位，指标值、文本状态和告警颜色继续显示。工作区已收敛为顶部栏、左侧导航/素材栏、中间画布和右侧属性栏四个一级区域；左侧栏可通过右边缘纯图形入口收起并把宽度让给画布，收起状态仅当前应用会话生效。原次级上下文工具栏、画布外标题栏和状态栏已并入无背景贴边画布 HUD。画布缩放和平移现为不写项目、不持久化且不触发“未保存”的纯会话视图状态。顶部/左侧标尺与状态栏鼠标坐标已经移除，画布使用完整中间区域并继续保留自适应点阵和当前网格间隔。中键、空格+左键与触控板双指平移共用命令式共享视口：逐帧只更新 SVG 合成层和 WebGL Uniform，达到 120px 屏幕位移或手势结束时再同步 React 裁剪状态；POD A 在 25% 缩放下的基准帧时间 P95 已由约 50ms 降至 16.8ms。制冷阶段 B 已实现水泵/阀门资产角色、阀门/CV 门控和沿双层管体最终显示几何的 WebGL 移动虚线；2026-08-26 已进一步增加监控态泵阀选择与右侧运行面板、水泵 `0–100%` 输出功率、固定 `100 m³/h` 基准流量和等阻力视觉流量求解。真实遥测接入、图表、物理水力求解和监控动画规模边界仍待后续。

2026-09-03 工作区增量：AIDC `97ecddf` 的沉浸式画布优先布局取代此前平铺四区解释；后续澄清以参考项目“监控模式”作为编辑与监控业务的统一外壳。主画布铺满应用，64px 顶栏、280px 左侧层级/素材面板、返回/命令/缩放 HUD 及 360px 右侧检查器均以 4px 贴边覆盖。左右面板统一使用 `rgba(28,28,28,.9)`、12px 圆角、30px 模糊、无阴影和无常驻描边；树统一使用 28px 行、监控 SVG、无边框搜索、低对比 hover/选中。编辑业务继续保留素材、新增、重命名、拖放和删除，现有路由、业务、数据、文案、会话视口和可访问行为保持。

2026-08-28 增量：编辑与监控画布增加 Figma 风格的临时抓手。指针位于画布或画布已聚焦时，按住空格进入抓手状态，再用鼠标左键拖动即可平移；松开空格恢复原工具。该手势复用中键平移的命令式会话视口和逐帧合并热路径，不触发对象拖动、框选、接线、dirty、历史或持久化；表单与可编辑文本保留正常空格输入。

2026-08-28 增量：macOS 触控板可直接双指平移并捏合缩放。画布将 `Ctrl+wheel` 作为指针中心连续捏合，将连续像素级或带横向分量的无修饰滚动作为双指平移，并继续把离散鼠标滚轮作为缩放。双指平移复用命令式共享视口，捏合与其他缩放继续按帧更新 React 视口；全部仍为纯会话观察状态。

2026-08-29 增量：图元运行指标的次要、重要、紧急异常数据分别使用既有黄、橙、红告警色作为紧凑背景，数值或文本状态使用黑色文字；指标名称与单位继续使用原样式且不带底色，正常数据继续使用透明背景和原中性色。关闭指标名称与单位不改变异常数据底色本身，只让组合标签按仅值布局收紧。该变化只调整派生 SVG 标签视觉，不改变指标配置、告警计算、模拟、Schema 或持久化。

2026-08-31 增量：编辑模式的图纸层级树支持从当前上下文新增图纸、行内修改名称，并通过拖拽把非根图纸移动为同专业目标的子图或同级前后位置；移动子树自动归一化园区、楼宇、POD、设备层级。根图、跨冷却/电力线路、循环关系、第二根和超过四层的操作会被拒绝；键盘可用 `Alt+方向键` 完成排序、提升与缩进。冷却/电力线路分组和含子图的节点均可独立收起/展开，普通 `←/→` 提供键盘操作，切换图纸时自动展开当前路径。管理结果进入项目 dirty、本地保存和 JSON；折叠状态只属于当前会话，不触发保存，监控模式继续只读，Schema 保持 v24。

2026-09-01 图纸删除增量：编辑模式 hover 非根图纸行或以键盘聚焦该行时，最右侧显示删除图标；点击后立即删除该图纸及全部下级图纸，并同步清理子树内图元、母线和连接网络。若当前图纸位于被删子树，自动返回目标原上级图纸；删除其他分支保持当前画布。线路根图继续固定且不可删除，监控模式不显示入口。删除会更新项目 dirty、本地保存和 JSON，不升级 Schema。

2026-09-01 视窗导航增量：通过树、面包屑、返回、新建/删除回退或项目打开/导入进入图纸时，画布按旋转后图元、完整母线和自由线路节点的稳定业务边界执行一次居中适配并保留 48px 屏幕留白；小图不超过 100%，空图恢复标准视窗，超大图可低于常规 25% 以确保看全。适配不等待或跟随异步路由结果，避免二次跳动；仍是纯会话视图，不进入项目保存、dirty 或历史。

2026-09-03 监控迁移增量：选定图纸可生成为独立版本的 RuntimeBundle v1，自动携带入口下级子树、必要祖先、画布业务对象、已使用资产、冻结下探关系和 Switch/2WV/CV 状态快照，并排除编辑历史、dirty、当前相机、随机读数和渲染临时对象。另一个 React 宿主可通过受控或非受控 `RuntimeBundleViewer` 恢复图纸、播放动画、接管下探和注入数据 Provider；跨项目入口现使用同日完成的专用只读场景，素材仍随运行时代码发布，资产解析/内嵌属于后续拆分。

2026-09-03 只读场景增量：跨项目 `DiagramRuntimeViewer`、`RuntimeBundleViewer` 与当前产品 `App` 的监控模式统一改用监控专用 `DiagramMonitorCanvas`。跨项目入口不再加载 8 千行级编辑画布及其历史、剪贴板、拖放、接线、变换和属性命令；当前产品因同时提供编辑能力仍会打包两条画布，但只有编辑模式实例化 `DiagramCanvas`。新场景保留正式路由、图元/母线/线路/标签、WebGL 点阵与流动动画、下探选择和桌面/触控板视口手势，公开控制只保留三个缩放命令。

2026-09-03 共享场景增量：编辑画布与监控画布的重复 SVG 已收敛到无状态 `DiagramScenePrimitives`，统一普通图片/通用框图元主体、图元/母线/子线标签与指标行、母线基线与母线节点、Symbol 颜色滤镜、冷却管内阴影、监控静态线路、桥面双层遮罩、冷却管壳和线路方向箭头。两条画布进一步共用纯数据 `connectionScene` 派生线路渲染分组、最终显示路径、冷却管壳、静态监控线路、桥下动画排除、冷却节点和连接锚点；静态流动表现拆到不依赖 React/Three 的 `flowPresentation`，避免纯场景模型意外加载 WebGL。编辑锚点、节点手柄、标签拖动、监控下探和运行对象选择仍由各自画布注入或处理，共享层不读取 Store、不写项目、不持有历史或 dirty。

2026-09-03 场景与路由目录增量：二维几何/视口、点阵密度、手势、线路外观/排序/颜色、通用图元、连接几何、三类标签布局、素材目录及 WebGL 点阵实现已迁到 `src/scene/`；正式路由调度、Worker 和 Hook 已迁到 `src/runtime/`。监控与共享场景直接引用新入口，旧 `editor/` 路径保留兼容重导出，选择平移仍留在编辑专属几何包装层。模块守卫保证 `runtime/scene/monitoring` 产品代码不反向依赖编辑目录；资产交付、包发布和目标项目集成作为独立后续阶段。

2026-09-03 标签运行数据列宽增量：图元、节点间子线与母线继续沿用各自既定的标题字号、方位和端点布局；其中图元与子线的多条运行指标改为分别测量名称列和数据列。数据列只取当前所有数值读数或文本状态中的最大宽度，所有行共享该列宽并统一右对齐，异常数据背景也使用同一宽度；设备标识、子线标签、指标名称或单位再长都不参与数据列宽计算。母线当前只有标题行，继续保持 14px 四态端点布局。本次不改变 Schema、指标配置、模拟、告警、显隐或持久化。

2026-09-03 拟真演示运行时增量：下一阶段以统一、确定、可移植的 Demo Runtime Engine 取代指标、泵阀状态和动画各自独立随机。每张包含可模拟设备的非空图纸只保留极少数稳定异常，同一 RuntimeBundle 在刷新、换机器和迁移到另一 React 宿主后复现相同异常对象、故障类型和慢速数据趋势。正常、异常、离线与开关/运行状态分层表达；正常关闭或停机不算异常，离线停止相关流动且数据为 `--`。Grid、Generator、Transformer、UPS、Battery、Switch、Tap-off Unit、Cabinet、CDU、TMU、泵、阀、CT、PHE、MP、WMT、CPD 等使用版本化设备档案与指标语义生成合理正常/异常范围，历史手工模拟范围保留用于兼容但不再主导标准演示。该模拟只追求演示可信，不宣称工程潮流、水力、热力或告警整定精度；详细方案见 `DEMO_RUNTIME_SIMULATION_DESIGN.md`。

2026-09-03 拟真演示运行时实施：上述核心能力已完成。监控默认使用覆盖全部现有图元的设备档案和确定性 `MonitorMetricRuntimeSnapshot`，以 3 秒慢速曲线刷新并在暂停时冻结；每页按设备数稳定选择 1–3 个异常，缺指标的异常设备只在运行态补最多两项指标。On/Off、泵启停、阀位、功率/流量归零、离线压暗与安全支路停流已关联，用户项目原模拟配置不被改写。RuntimeBundle v2 固化种子、档案/算法版本、语义和异常分配，v1 继续兼容，另一 React 宿主可零配置复现。工程级负载聚合、潮流、水力和热力计算仍明确不在本阶段范围。

2026-09-03 UPS L 演示校准增量：UPS L/R 父子供电映射同时识别 `ups` 与 `ups-group`，使子图能正确继承父图带电状态并驱动已配置电流动画。UPS L 锂电组 SOC 演示区间固定为 `82–98%`，异常分配优先落在其他指标，保证每组剩余电量持续高于 80%。备电时间由同一快照 SOC 直接派生，正常满电额定 `2.0 h`，异常只作可用容量折减，因而两项数据保持强正相关。三级异常数据底色提升为高饱和黄/橙/红，标题仍无底色、异常数据文字仍为黑色。该增量不改项目 Schema 或 RuntimeBundle 格式。

2026-09-03 电力图元增量：电力素材库新增用户提供的 48×48 Supply、Load、AC-AC Converter、Rectifier、Inverter 五个独立图元。Supply 与 Load 具有“运行 / 待机”双状态，新增实例默认运行，两种状态可在右侧分别配置颜色，并复用既有状态快照、监控即时切换、项目保存及 RuntimeBundle 导出链路；待机属于正常运行方式，演示有功功率与负载率归零，不产生异常。其余三个保留附件原始固定配色且无双态入口。五类设备均纳入确定性演示档案和 RuntimeBundle 素材清单，但不自动赋予电力 Source/Target、拓扑截断、父子绑定或预置锚点语义。项目 Schema 保持 v33，演示档案版本保持 `aidc-demo-profile-2026-09-r4`。

2026-09-03 监控设备面板增量：监控模式新增与 Figma `AIDC-Color / 3028:3395` 对齐的 MLVR、Generator、UPS 右侧详情和图元/子线标签数据趋势浮层。Generator 和单体 UPS 固定显示详情；Switch 不按型号硬编码，而是每个实例在编辑模式选择“控制开关”或“显示设备面板”，历史实例继续默认控制。右侧面板只在当前恰好选中一个图元时挂载，进入监控、切换图纸或点击画布空白处后的空选状态完全隐藏。设备面板为 500px，其他检查器保持 360px；数据浮层为 360px，在读数上方展开并可由空白处或 `Esc` 关闭。UPS 线路画布复用本项目真实电力素材与正交接线，趋势与告警复用 AIDC 表达及当前确定性 Demo Runtime，不把高频值写入项目。Schema 升至 v34。

2026-09-03 水泵存档增量：项目 Schema 升至 v35，CHWP、CWP 及任何显式配置为 `pump` 的图元实例保存当前启停状态和 `0–100%` 输出功率。编辑与监控右侧面板共用同一水泵控制区和实例快照；任一模式修改都会更新项目时间并标记 dirty，但不进入编辑画布撤销历史。手动保存、项目 JSON、图元复制和 RuntimeBundle v2 均保留该快照。v1–v34 旧存档缺少字段时仍按“运行 / 100%”解析，宿主显式运行态覆盖优先。派生流量、指标读数、趋势、动画相位和 Provider 继续不持久化。

2026-08-31 指标布局微调：多条图元运行指标继续共用统一的名称列和右对齐数值列，最宽一行的名称/单位到数值列间距由 8px 收紧为 0px；较短行保持同列对齐。该变化仅收紧组合标签宽度，不改变字号、行高、告警、避让、配置或持久化。

2026-08-31 冷却视觉层级微调：辅助线路继续保持 `4px / 1px` 管体/内芯，但外管改为 `#1D1F20`、内阴影降至 `20%`，与主要线路的 `#262626 / 50%` 拉开材质对比；监控静态连接改为按编辑态稳定线路组顺序逐组重放遮罩、管体和内芯，保证主要—辅助及主要—主要、辅助—辅助同级拱桥在播放与静止状态都保持完整双层管体。该调整不改变拓扑、流量、路由、Schema 或持久化。

2026-09-01 通用图元增量：编辑模式单选通用图元后，右侧属性面板可独立修改或恢复背景颜色。默认继续使用近黑色 `#121316`，连续调整复用现有应用内 HEX 控件和轻量 SVG 预览，确认后作为一次实例属性修改进入历史、复制、本地保存和 JSON；虚线框颜色、虚线显隐与背景色互不耦合。背景色复用 `properties.genericBackgroundColor`，不升级 Schema。

2026-08-26 增量：2WV 与 CV 已和 Switch 一样归并为默认 Off 的 On/Off 状态图元。编辑属性面板、监控画布、SVG 素材切换、关/开颜色槽、项目快照与兼容自动保存共享同一状态；它们作为制冷阀门参与拓扑时也以该状态门控水流，不再另用一套会话阀门状态。Schema v23 起三类状态进入本地项目存档和 JSON 导入导出，但状态切换仍不产生 dirty、更新时间或撤销记录。

2026-08-26 增量：CV 明确为止回阀。Schema v24 增加 `check-valve` 角色，CV 开启时只允许稳定入口流向出口，关闭时完全阻断；2WV 与普通阀门开启后仍为双向。旧文档标准 CV 自动以顶部中心为入口、底部中心为出口，保留锚点 ID 和原接线。

2026-08-27 增量：CHWP、CWP、CPD、CT、PHE 的正式素材由同名 SVG 切换为透明 PNG。前四者保持既有画布逻辑尺寸；PHE 从 80×160 竖版改为 200×80 横版。旧 PHE 项目载入时一次性迁移实例尺寸和旋转，保留原显示包围框左上位置；标准锚点 ID、名称、类型和接线引用保持，坐标与方向对齐新素材上下接口。素材源像素尺寸可高于画布逻辑尺寸，缩放与网格约束继续以目录登记的逻辑尺寸为准，Schema 保持 v24。

补充演化：上述“固定子线分叉入口和可见分叉点已移除”仍指旧式常驻分叉工具与自动派生标记。Schema v21 已把原途径点和分流节点统一为网络内自由“节点”；节点仅在编辑 hover/选中时提示，可从节点双击起线。v1–v20 旧缓存载入时迁移旧点模型，并继续清除自环、悬空边及其孤立节点。

2026-08-31 兼容修复：混合选区旋转后的自由节点和兼容途径点统一重新吸附 8px 网格。对于旧缺陷已经写入的 Schema v25 历史，载入器仅修复编辑器自身生成的 `route-waypoint-*`、`connection-node-*` 越网格坐标；其他非法节点仍由 Schema 拒绝，避免用历史兼容放宽项目数据边界。

2026-09-01 节点与子线语义增量：节点、图元和母线可组成同一框选或追加多选选区，从任一已选对象拖动或使用方向键时保持相对位置一起移动。持久化节点成为子线的真实分割边界，选择、删除、颜色、通行方向和冷却主要/辅助级别以相邻节点边为单位；自动路由的普通拐角仍不持久化。Schema v26 会把旧 `routeNodeIds` 展开为相邻节点边并保留属性、单份标签及自动布线恢复所需的逻辑组标识。

2026-09-01 持续绘线增量：编辑画布 HUD 新增显式“绘制线路”工具。开启后可从任意 8px 网格点、锚点、母线、节点或子线单击起笔，连续单击空白点生成真实节点间线段；落到已有拓扑或闭环后结束当前线但保持工具开启，`Esc` 才退出。Schema v27 允许连接度为 1 的自由端点和用户显式闭合的同网络回路；开路线不在无有效业务路径时制造监控流动。

2026-09-01 子线标签运行数据增量：恰好单选一条节点间子线后，可配置与图元相同的数值/文本运行指标、模拟范围、文本候选和三级告警，并通过“显示子线标签”“显示运行数据”“显示指标名称与单位”分别控制名称、指标行和指标标题。编辑模式保持稳定预览，监控模式与图元共用全图单一 30 秒模拟节拍。Schema v28 将配置保存到 `ConnectionEdge`；节点拆分与收缩只保留一份显示数据，复制子线时重建指标 ID，旧项目默认不显示运行数据。

2026-09-01 TMU 图元增量：冷却素材库新增稳定键 `tmu` 的 TMU 图元，并在 2026-09-07 替换为用户提供的 SVG；当前按 SVG 原始画布采用 48×48 逻辑尺寸。首版不赋予水泵、阀门或止回阀角色，也不预置锚点；用户可继续通过锚点编辑器配置实际接口。新素材通过安装资产同步自动进入既有项目；旧目录下仍为 48×64、72×96 或此前 64×96 默认尺寸的实例与边缘锚点定向迁移，主动缩放过的实例尺寸保留，不升级 Schema。

2026-09-01 CDU 图元增量：冷却素材库稳定键 `cdu` 换用用户提供的横版 SVG，画布逻辑尺寸由 96×96 调整为 192×96。旧项目载入时，CDU 实例按原横向与纵向缩放比例迁移到新素材尺寸，旋转实例保持原显示外接框位置；既有锚点横坐标按新宽度映射，ID、类型、方向和接线引用保持。迁移幂等，不新增字段，当前 Schema 保持 v29。

2026-09-01 水泵停机态增量：用户提供的 1000×403 透明 PNG 登记为统一水泵停机素材。监控模式下，任意配置为 `pump` 冷却设备角色的实例在“水泵运行状态”关闭时显示该素材，重新运行后恢复各自原图；编辑模式始终使用原素材。状态替换复用实例原有画布框、旋转、缩放、锚点、标签和接线，不改变水力拓扑之外的图纸数据。该增量实施时泵启停属于会话态；2026-09-03 已由 Schema v35 / KD-142 升级为实例存档快照。

2026-09-01 Cabinet 与分接单元增量：电力素材库新增用户提供的 48×48 `Cabinet.svg`，显示名为 `Cabinet`，使用独立稳定键 `cabinet-device`。原 `Cabinet A / B` 的用户可见名称分别更新为电气语境中的 `Tap-off Unit A / B`，内部继续使用历史键 `cabinet` / `cabinet-b` 和原素材文件，保留既有资产锚点及接线。旧默认实例名 `Cabinet`、`Cabinet A`、`Cabinet B` 定向更新，自定义名称不变；不升级 Schema。

2026-09-01 Tap-off Unit 子图元增量：电力素材库新增用户提供的 32×32 `Tap-off Unit.svg`，稳定键 `tap-off-unit`，语义上作为 Tap-off Unit A/B 的子图元。它可独立拖入、变换、复制、配置锚点和保存，并可从右侧属性面板修改单一实例颜色；首版不预置锚点，也不自动嵌入、绑定或跟随 A/B。现有项目通过安装资产同步自动获得，不升级 Schema。

2026-09-02 Battery-group 图元增量：电力素材库新增用户提供的 48×48 `Battery-group.svg`，显示名 `Battery-group`、稳定键 `battery-group`。它是与现有 `Battery` 并列的独立普通电力图元，可拖入、变换、复制、配置标识/指标/锚点并保存；首版无预置锚点、实例改色、On/Off 状态或电力 Source 角色，不继承 `Battery` 的运行语义。现有项目通过安装资产同步自动获得，不升级 Schema。

2026-09-02 UPS-group 图元增量：电力素材库新增用户提供的 48×48 `UPS-group.svg`，显示名 `UPS-group`、稳定键 `ups-group`。它是与现有 `UPS` 并列的独立普通电力图元，可拖入、变换、复制、配置标识/指标/锚点并保存；首版无预置锚点、实例改色、On/Off 状态或子图下探关系，不继承现有 UPS 的实例与监控语义。现有项目通过安装资产同步自动获得，不升级 Schema。

2026-09-01 跨线层级增量：节点间子线可在右侧属性面板按整线设置“下层 / 自动 / 上层”，纯子线多选支持批量设置与混合状态。最终未连接交叉顺序为“上层 > 自动 > 下层”，同层继续保留冷却主要高于辅助和稳定创建顺序；子线跨母线仍为不可反转的固定规则。Schema v29 将显式上下层保存到 `ConnectionEdge`，节点拆分继承、二连合并保留人工优先级；编辑、监控静态与动画共用同一层级，不改变路由、拓扑和流量。

2026-09-02 节点复制增量：`Command/Ctrl+C`、`Command/Ctrl+V` 与 `Command/Ctrl+D` 支持只选节点或图元/母线/节点混合选区。每个显式选中节点复制所有直接相邻节点间边；未选宿主端点按原世界坐标脱离为自由端点，下一跳边和外部对象不进入副本。副本保留子线颜色、标签、显隐、方向、主辅级别、跨线层级和指标配置，但节点、边、逻辑组、网络及指标使用独立 ID；节点剪切仍不在本次范围内，不升级 Schema。

2026-09-02 图元标签锚点避让增量：普通图元的设备标识与运行指标组合标签只避让当前实际已连接锚点的 24px 出线走廊。资产中存在但没有参与任何连接边的锚点不再占用标签候选区域；新增或删除接线后，标签布局根据当前拓扑即时重新派生。手动标签方位、其他图元与标签之间的避让、通用图元框内标识、Schema 和持久化均不变。

2026-09-02 锚点跨边拖动增量：图元模板锚点继续只能位于原始矩形四边的非角点 8px 网格位置，但一次拖动可从当前边直接切换到其他任意边。拖动预览在四边全部合法点中选择离指针最近的位置；距离相同时优先保持当前边，减少角点附近抖动。跨边后出线方向按新边自动派生并与坐标一起即时应用、进入锚点编辑历史；数据模型、Schema、添加规则和既有锚点保持不变。

2026-09-02 监控子图增量（2026-09-04 由双路继承规则补充）：POD A 监控画面可直接单击 Tap-off Unit A 下探“分接单元 L”，单击左侧 UPS 设备组下探“UPS L”，并继续使用现有面包屑、返回和自动适配。子图是否有电由父图对应设备当前是否处于有效供电路径决定；带电时优先从用户标记的子线物理端点注入外部供电，只有整张子图没有显式入口时才兼容使用母线首个接入点或明确单向的自由输入端；Cabinet 或明确单向的自由输出端作为 Target，全部传播严格服从子线方向。Battery-group 在普通图纸中仍不自动作为 Source，仅当 UPS-group 父级 A/B 均失电时在对应 UPS 子图临时接管。

2026-09-02 冷却图元下探增量：监控模式下，冷却 POD 内带编号设备标识的 CDU 可直接进入同一 POD 下编号一致的“TMU到FM”设备图；例如 POD A 的 `CDU-01` 单击后进入 `TMU到FM 01`。编号比较忽略前导零，映射只作用于 `cdu` 图元和直接子图，显式 `drillDownDiagramId` 仍优先；编辑模式点击、选择和移动保持不变，不新增项目字段或升级 Schema。

2026-09-03 画布配色增量：编辑与监控主画布背景统一由近黑色调整为纯黑 `#000000`。WebGL 正常渲染和 CSS 回退路径使用同一背景值；8px 最小世界网格、自适应可见间隔、点的大小/颜色/透明度、世界原点、缩放和平移逻辑均保持不变，点状网格继续显示。本次仅改变画布表面，不修改侧栏、HUD、通用图元背景、项目数据或 Schema。

2026-09-02 UPS L 母线兼容修正：旧图中第三至第五台 UPS 的输入线端点虽然与母线坐标重合，仍保存为自由节点，导致监控拓扑实际断开、主线动画只到第二台。项目载入现把编辑器生成且精确落在唯一电力母线上的自由端点转换为母线节点，并合并同一母线的历史网络；同位置既有节点优先，母线交点歧义不猜测。当前 UPS L 由 17 个视觉重合网络规范为 11 个真实网络，入口母线动画可连续覆盖五台 UPS；不升级 Schema。

2026-09-02 分接单元 L 母线方向修正：设备子图的多条母线入口现在合并为同一个上游多入口供电边界，再统一计算到 Cabinet 的最短供电路径。成对环形母线不再因每个入口被误当成独立电源而产生相反方向冲突，四条主母线可按一致方向显示电流；未被最短路径使用的右侧冗余联络保持无流动。真实 Grid、Generator、Battery 的独立电源语义不变，不修改图纸数据或 Schema。

2026-09-02 母线人工监控方向增量：编辑模式纯母线单选或多选可在右侧把监控电流设为自动、起点到终点或终点到起点。人工方向始终约束拓扑传播；只有子图没有显式子线入口时，才兼容选择母线侧外部供电入口。母线实际参与有效供电路径时动画覆盖完整物理长度；父图断电或无有效目标时仍停流。画布不增加静态箭头或入口圆点；人工方向母线在编辑模式 hover 或选中时显示“入口/出口”文字，监控方向由电流动画表达；旋转保持原世界方向。Schema v30 持久化可选字段，旧项目默认自动。

2026-09-02 电力子图显式供电入口增量：Schema v31 允许在电力子图的逻辑子线上标记起点端或终点端为“外部供电入口”，单选和纯子线多选均可配置。只要子图存在至少一个显式入口，监控拓扑就只从这些真实进线端注入父级供电，不再把母线端点或自由边界自动当作 Source；多个入口合并为同一个上游供电边界，父图失电时全部停流。入口标记不改写子线通行方向，冲突时明确提示并按约束停流；未配置入口的 v1–v30 项目继续沿用自动兼容规则。

2026-09-02 子线节点端落母线自动接入：拖动节点、拖动子线段生成的节点端、混合移动或粘贴落点完成时，只要真实持久化电力节点精确落在唯一母线范围内，就原位转换为母线宿主节点并接入整条母线；同点已有母线节点时直接复用，同母线网络立即合并。二连与分流节点保留全部邻接子线；没有真实节点的自动路由穿越继续使用拱桥且不连通，多母线交点无明确宿主时阻止提交。不升级 Schema。

2026-09-02 电力子图母线与并行进线补齐：配置显式外部供电入口后，实际参与有效供电的自动方向母线从入口向两侧补齐全部 tap 区间及物理端点尾段，完整表达整条母线带电；同一已带电设备连接到母线的其他明确单向入线也同时进入动画，不再因 16px tap 距离差被最短路径裁掉。双向支路、Battery-group、普通环路、方向冲突与无目标网络仍不凭空流动；人工单向母线继续服从既有方向约束，不改变 Schema。

2026-09-02 冷却管路显式源头增量：冷却子线可把物理起点端或终点端配置为“水流源头”，单选和纯子线多选均可设置。每个有效源头按 `100 m³/h` 固定视觉基准向同一有效管网中其他可达开放自由端注入，经既有等阻力模型分流，并继续服从冷却类型、子线允许方向、阀门、CV 和设备内部回路约束。多个源头与水泵闭环贡献可线性叠加；没有可达开放端时不产生动画。实现复用既有端点字段，Schema 保持 v33。

2026-09-04 电力 A/B 主备与跨图继承增量：电力模板锚点可标记 A/B 输入，子图显式外部入口可标记 A/B 通道。双路受电图元与 Cabinet 在 A 可达时只使用 A，A 失电且 B 可达时自动使用 B，两路均失电时停电；锚点保持独立且不在设备内部并联。下探图纸继承父级实际通道，`ups-group` 两路均失电时对应 UPS 子图由 Battery-group 自动备电，父级恢复后退出。Schema v36 为既有算力 POD、动力 POD、UPS-group、Cabinet 的“电路 1/2”兼容迁移 A/B 角色；详细边界见 KD-146。

2026-09-04 MP 管路测量点增量：MP 不再作为带进/出口的串联通流设备，改用用户提供的单侧引线 SVG，并固定为一个底部“测量点”通用冷却锚点。用户从该锚点直接接到任意冷却管路即可，MP 不建立设备内部通路、不担任水流源头或回收端、不门控流动；旋转图元可改变接线方向。新建 MP 默认显示温度、压力和流量三项指标，仍支持实例改色。Schema v37 将旧项目中同一 MP 的双端管网合并到唯一测量锚点，保留管路连续性、线路属性、图元位置/颜色和既有指标。演示读数仍不代表工程水力或热力求解；详细边界见 KD-147。

2026-09-04 图元锚点即时转弯增量：所有图元锚点不再强制先沿边缘法向走出一个固定网格再允许拐弯。落在主画布网格上的锚点可从端点直接向外或沿边缘两个切向接线，但仍禁止线路向图元内部进入；缩放或旋转产生的非网格锚点只保留进入外部 8px 网格所需的最短正交过渡。锚点方向继续作为模板元数据和等价路径的软排序偏好，不再生成强制引出段。预览、正式路由和端部子线拖动统一遵循该规则，其他图元仍是硬障碍，母线法向接入规则不变。Schema 与持久化拓扑不变；详细边界见 KD-148。

2026-09-07 冷却图元 SVG 替换增量：CWP、CHWP、CT、PHE 使用用户最新附件替换 2026-08-27 的同名 PNG；CWP、CHWP、PHE 采用 SVG 根画布 80×128，CT 采用 96×96，CPD 继续保留 80×80 PNG。稳定 `assetKey`、名称、专业分类、水泵角色、运行状态、指标和接线语义不变。旧 200×80 CWP/CHWP/PHE 与 160×160 CT 项目在载入时按旧实例缩放迁移到新比例并尽量保持中心，边缘锚点按对应边比例吸附到新 8px 网格位置，保留 ID、名称、类型、流向/供电角色和线路引用；重复载入幂等。Schema 保持 v37，详细边界见 KD-150。

通用图元增量已于 2026-08-25 实现：素材库新增稳定键 `generic`，画布以默认灰色可改色虚线框、默认 `#121316` 且可改色的背景和框内白色设备标识呈现。它是普通 SVG 等比缩放与框外设备标识规则的受控例外，允许宽高分别按 8px 网格修改；旋转时框和锚点旋转，标识保持水平并通过省略与裁剪限制在框内。单选后可从右侧属性面板分别编辑虚线框颜色和背景颜色，并隐藏或恢复当前实例的虚线；关闭只隐藏描边，背景、标识、锚点和运行指标不变。运行数据继续复用现有四侧组合标签及名称/单位显隐能力。通用分类的新锚点按当前冷却/电力图纸选择兼容默认类型，已有明确类型保持不变。自由尺寸、标识、边框色、背景色和虚线显隐复用既有实例字段或属性容器，不因 2026-09-01 背景色增量升级 Schema。

## 第一阶段：编辑系统

### 已确认范围

- 从素材区拖入图元。
- 图元单选、多选和框选；框选可同时包含母线。
- 图元旋转、移动和缩放。
- 多选时每个图元和母线显示选中态，并通过整体包围框一起移动和旋转；仅包含图元时继续支持整体缩放。
- 图元贴齐网格。
- 画布不显示顶部/左侧标尺或鼠标 X/Y 坐标；中间区域全部用于绘图，保留自适应点阵和状态栏当前网格间隔。
- 画布使用离散鼠标滚轮直接缩放，macOS 触控板双指移动平移、捏合连续缩放，鼠标中键或按住空格+鼠标左键也可拖动平移；缩放比例与平移偏移仅在当前画布会话中生效，不写项目文档、本地存储或编辑历史，也不标记项目未保存。进入任一图纸时按全部业务内容自动适配一次，之后不因同图编辑或异步路由完成强制重置；左侧图纸树节点保持单行。
- 页面以铺满应用的画布为底，顶部栏、280px 左侧导航/素材、返回/命令 HUD 和 360px 右侧检查器均作为监控模式风格的悬浮覆盖层；不再设置会压缩画布的次级横向布局。左侧栏右边缘外侧提供无背景、无描边的左/右折线箭头收起/展开按钮，收起后画布尺寸保持不变；状态只在当前会话保留。
- 拖动画布内图元或从素材区拖入图元时，显示贯穿画布的十字对齐区域，并跟随当前 8px 吸附位置。
- 新建、保存和打开项目。
- 撤销/重做、复制/粘贴、删除和键盘快捷键。主画布中方向键按对应方向以 8px 步进移动全部已选图元、母线与节点；连续按键使用轻量预览并合并为一次历史提交。`Esc` 退出接线并清空图元、线路或母线选择；`Command/Ctrl+C`、`Command/Ctrl+V` 和 `Command/Ctrl+D` 统一作用于全部已选图元、母线和节点，其中显式节点带上直接相邻子线段；`Command/Ctrl+S` 在应用内全局保存当前项目。
- 使用 JSON 文件导入和导出项目。
- 图元素材由用户后续导入。
- 当前图元只使用 `src/assets/symbols/` 中登记的 SVG 或透明 PNG；插入尺寸采用素材目录登记的画布逻辑宽高，PNG 源像素可按整数倍提供更高分辨率。普通图元默认在图元旁显示独立的“设备标识”文字，单选后可在右侧属性面板独立隐藏或恢复；标签不增加背景或卡片外壳，也不计入图元尺寸、框选、吸附或线路避障，排布独立于主画布 8px 网格。通用图元例外地把设备标识始终显示在自身虚线框内，不提供该名称显隐开关。
- 普通图元标签默认由系统在画布坐标的上、右、下、左四侧自动选择，并只避让当前实际已连接锚点的出线区域；未参与接线的资产锚点不影响标签方位。用户单选一个图元后可拖动其标签改变方位，多选时标签不可选中或拖动。图元旋转不旋转标签，也不改变已选标签方位；设备标识内容继续只在右侧属性面板编辑。通用图元的框内标识不可拖动，旋转时同样保持水平并限制在旋转后的框内。
- `SwitchOn.svg` / `SwitchOff.svg`、`2WV_On.svg` / `2WV_Off.svg`、`CV_On.svg` / `CV_Off.svg` 分别归为稳定键 `switch`、`2-wv`、`cv` 的三个 On/Off 图元，缺省均呈现 Off；编辑属性面板与监控画布共享同一状态。Schema v23 以图元字段保存状态并纳入本地项目及 JSON 导入导出，兼容运行态表继续自动镜像；状态切换不写入编辑历史、不标记 dirty。2WV 开启时允许同回路双向传播，CV 按 Schema v24 的稳定入口→出口止回语义传播，二者关闭时均阻断内部通路。
- Generator、Grid、MP、Transformer、Tap-off Unit 支持在右侧属性面板修改单一图元实例颜色；Switch、2WV、CV 分别配置关/开状态颜色；通用图元使用两个独立控件修改虚线框颜色和背景颜色，并使用实例开关隐藏或恢复虚线描边。关闭描边时保留两项颜色、当前背景、框内标识、锚点和运行指标。色块入口展开应用内 HEX 选择器，不调用无法控制 RGB/HEX 模式的系统颜色面板。连续调整只进行轻量画布预览，确认后单次写入项目与撤销历史。其他图元继续保持素材原始配色。
- 未选择对象时，右侧属性面板按图元、母线、子线显示当前画布实际使用的全部颜色及对象数量。Switch、2WV、CV 的关/开颜色按设备拆成独立图元颜色槽列出；点击任一颜色只全局替换同类别、同设备、同颜色槽、同当前颜色的对象。默认色也参与汇总，保持素材原始配色且不支持实例改色的图元不进入图元颜色组。
- 素材专业分类以当前目录登记为准：CPD、TMU 属于冷却；FM、Battery-group、UPS-group、Cabinet、Tap-off Unit A、Tap-off Unit B、Tap-off Unit、算力 POD、动力 POD 属于电力；通用图元单列“通用”分类。Battery-group 与 Battery、UPS-group 与 UPS 分别使用独立稳定键；Battery-group 仅在 UPS-group 父级 A/B 均失电时于对应 UPS 子图临时取得备电 Source 角色。Tap-off Unit A/B 沿用原 Cabinet A/B 的稳定键与已有锚点/接线，32×32 Tap-off Unit 是两者的独立子图元，新 Cabinet 是 A 优先、B 自动备用且不向下游传播的计算机柜终点。
- POD 图元需要以高于单台设备的抽象层级表达：算力 POD 表示 AI 计算单元；动力 POD 表示面向大楼制冷系统、电梯等其他设备的电力/制冷基础设施单元。当前视觉方案试行英文缩写加立方体，待用户验收。
- 画布以 8px 世界单位为最小网格并使用单一样式点阵呈现；所有点大小、颜色和透明度一致。滚轮缩小时依次使用 8、16、24、32px 等 8px 倍数作为当前可见间隔，状态栏显示当前间隔，点阵原点保持在世界坐标。图元移动和尺寸仍贴合 8px 最小网格；普通图元缩放保持登记的逻辑宽高比并采用宽高同时落网格的离散步进，通用图元允许宽高独立落网格；旋转以 90° 为最小单位。

### 已确认工程与界面基线

- 沿用 AIDC 项目的二维编辑架构：Three.js / React Three Fiber 正交顶视 WebGL 场景、GLSL 网格和 SVG 交互覆盖层。
- 中键、空格+左键及触控板双指平移使用 SVG 与 WebGL 共享的命令式会话视口；逐帧合并只更新合成变换与 Uniform，React 负责低频 overscan 裁剪同步和手势结束状态，不承担每个输入帧的画布树协调。鼠标滚轮、触控板捏合和按钮缩放继续按帧更新 React 视口，保持屏幕空间元素与指针中心一致。
- 沿用 AIDC 项目的 UI 设计系统：以 typed Token / Recipe、真实 React 基础组件和确定性生成 CSS 为唯一视觉真源；不继续使用 Ant Design 作为界面组件基线。
- 画布标题与层级路径、返回、编辑/播放命令、缩放和状态信息作为极简 HUD 紧贴完整画布边缘；返回/路径无容器背景，命令与缩放只使用参考监控模式的 6px 无阴影小浮层，并保持画布手势和坐标系不变。
- 复用成熟交互模式和必要代码时复制、裁剪到本项目，不建立运行时跨项目依赖。
- 保留本项目独立的业务文档、Zod 校验、Dexie/IndexedDB 存储和 JSON 导入导出边界。

### 第一阶段后置项状态

- 线路连接和线路管理不属于第一阶段首轮实现；第一阶段现已验收通过，可以开始开展。
- 监控模式不属于第一阶段。

## 当前增量：图元锚点编辑器

### 已确认目标

- 支持为图元添加接线锚点，为后续线路连接提供端点。
- 锚点编辑画布使用 8px 最小网格。
- 锚点只能放置在 8px 网格点上。
- 锚点定义属于图元资产模板，同一资产的所有画布实例自动继承。
- 锚点只能放置在图元原始尺寸的边缘网格点上；拖动时可在四条边之间直接切换，出线方向由最终所在边缘唯一确定为垂直向外，用户不可修改。该方向用于表达端口朝向和等价路径的软排序，不强制线路先走一段法向引出线。
- 锚点类型必须明确。电路使用单一电路类型；冷却使用一次回路冷、一次回路热、二次回路冷、二次回路热、通用五种类型。
- 图元实例缩放或旋转时，锚点随图元几何变换；缩放后的锚点允许不落主画布 8px 网格，后续线路从实际端点出发并处理网格路由。
- 图元锚点编辑弹窗中的合法修改即时应用到当前项目；弹窗不设置取消或应用按钮，仅通过右上角关闭按钮退出编辑。
- 图元锚点编辑器自身只负责资产级锚点定义与编辑；线路连接使用这些稳定定义，并在下方“线路连接与管理”增量中独立实现。

### 已确认边界

- 图元四个角不允许放置锚点。
- 锚点名称可编辑；稳定 ID、坐标、系统派生方向和类型为必需字段。
- 锚点定义随项目 JSON 与 IndexedDB 持久化，SVG/PNG 源文件不写入锚点元数据。

## 当前增量：线路连接与管理

### 已确认目标

- 配置了锚点的图元在 hover 时显示可用接线锚点；点击锚点开始接线，临时线路随鼠标正交增长，点击兼容图元锚点或母线完成。同一图元锚点允许重复发起或接收多条线路。
- 线路类型包括电力、一次回路冷/热、二次回路冷/热和通用；通用冷却网络在接入专用类型后整体解析为该类型，失去最后一个专用端点后恢复通用。
- 四类专用制冷线路的默认预设色固定为：一次回路冷 `#FCBC00`、一次回路热 `#FB7800`、二次回路冷 `#1BA4FF`、二次回路热 `#00B387`（2026-09-07 更新）；预设统一用于未自定义线路、接线锚点和锚点编辑器，显式实例颜色继续优先。
- 制冷管路统一使用双层世界空间样式与相同材质：主要线路下层为 `10px`、`#262626` 并带 `x 0 / y 1 / 模糊 3 / #FFFFFF 50%` 内阴影，上层为 `2px` 一次/二次回路冷/热色或实例色；辅助线路下层收为 `4px`、上层保留 `1px`，并把显示色调整为 `20%` 原色与 `80% #70736F` 中性灰混合。两类均使用平头管体、圆头内芯与圆角连接；粗细只表达用户显式配置的视觉层级，不改变拓扑、流量或真实管径语义，也不从连接度自动推断。
- 除图元缩放或旋转导致的非网格锚点最短过渡段外，线路只沿 8px 网格水平或垂直行走；网格锚点可从端点直接向外或沿边缘两个切向转弯，禁止向图元内部进入，不要求固定长度的法向引出段。系统自动选择避开其他图元区域的实际最短路径，并以较少转折作为次级目标。实际长度和转折数相同时，以拐点到两端中点的累计距离作为主要造型评分，锚点朝向只用于等价路径的软排序；同网络已有路径不参与评分。
- 长度和转折数相同的正交路径优先让拐点靠近起点与终点的中部，避免折点无必要地贴近任一端。
- 不同线路网络允许正交交叉但默认不连通。普通子线穿越电力母线时始终由子线绘制拱桥；冷却主要线路与辅助线路交叉时始终由主要线路绘制拱桥；其余同级子线交叉继续由后创建线路跨越。实际 SVG/监控绘制同样按“母线→子线”和“辅助→主要”分层，不能让后绘制网络盖回桥面遮罩。不同网络共线重叠禁止。
- 非连接交叉的拱桥采用紧凑的真圆弧和加粗的局部画布色底衬；电力线路默认占用 8px 路径跨度并抬高 4px，底衬为 12px 屏幕线宽。制冷管路在编辑和监控模式均使用放大一倍的 `16 × 8px` 圆弧，主要/辅助线路分别使用 `18px / 12px` 世界空间画布色遮罩。拱桥只属于派生显示几何，不改变线路拓扑或项目 JSON。
- 编辑模式制冷管路的普通 90° 拐角使用 `8px` 世界空间真圆弧；相邻段不足时，半径最多取各相邻段长度的一半。同一网络在该点只有两个实际物理方向时仍视为普通弯头，包括二连节点和多条逻辑边共享的弯头；实际物理方向达到 3 个及以上时视为分叉并保持直角。紧邻拱桥的弯点不圆角。圆角只改变上层、下层、选择、命中和接线预览共用的派生显示几何，不改变 8px 路由点、节点、拓扑或保存数据。
- 当前已实现版本允许从图元锚点、母线、自由节点或显式绘线工具的空白网格点起线；兼容锚点、母线、另一网络节点和子线都可作为终点。接线落到子线时在 8px 落点创建或复用节点，同时保持原逻辑线路的颜色、标签、显隐和单向属性；用户显式连接同网络不同节点时可建立闭环，同点自连和重复边仍被拒绝。同一锚点的多条线路仍可形成不保存、不可交互的派生共同段。
- 点击独立支路选择对应逻辑连接；点击多条连接共用的主干选择整个线路网络。删除逻辑连接或网络后按剩余端点关系重新路由。
- 选中独立逻辑子线后可修改该边颜色并在重新选择后回显保存值；`Shift` / `Ctrl` / `Command` 单击可跨网络追加或移除逻辑子线，网络主干会先展开为网络内逻辑边参与选择。子线可与母线共同保留在一个线路混合选择中，通过同一颜色字段批量修改。选择内存在不同颜色时显示“多种颜色”，连续颜色输入只预览，确认后单次提交。
- 单选一条独立逻辑子线后可在右侧属性面板填写或清空可选标签，并通过实例开关隐藏或恢复非空标签。标签缺省位于目标端点的负侧；单选后可拖动到源/目标端点，并在端点附近横线的上/下侧或竖线的左/右侧切换。文字沿端点线段向子线内侧展开：横向使用左/右对齐与水平 padding，纵向使用顶/底对齐与垂直 padding，不越过子线端点。标签位置随派生路由方向更新，不保存折点坐标。
- 单选或纯子线多选后可配置双向、起点→终点或终点→起点通行；多选方向不一致时显示“多种方向”，一次修改批量作用于全部所选逻辑边并形成一个撤销步骤。母线与子线混合选择不显示方向字段。双向不显示箭头；单向在逻辑目的端显示固定屏幕尺寸箭头，电流或未来水流拓扑只能顺箭头穿过。方向配置属于逻辑边实例并随历史、复制、JSON 与本地保存持久化。
- 纯冷却子线单选、多选或网络选择后可批量设置主要/辅助级别；混合值显示“多种级别”。辅助线路以 `20%` 原回路/实例色与 `80% #70736F` 混合后的低对比色、`4px / 1px` 管体/内芯显示，原始颜色字段不变；电力子线与母线混选不显示该字段。编辑模式选中的冷却子线使用独立 `6px #B7B8B5` 高亮轨道，确保不同回路色、不同粗细和共享主干上均可辨认。
- 自动路由仍是默认行为；用户可平行拖动任意普通子线段创建或更新 8px 节点硬约束。自由节点可单独选择、移动、删除、双击起线，也可与图元和母线一起移动或旋转；路径与交汇角色按实际物理连接度派生，不保存两种对象类型。
- 共享主干的参与边使用相同节点；移动共享节点影响全部邻接边。删除二连节点恢复局部自动路由，删除三连及以上节点移除相关节点间边。选中节点间子线段后仍可从右侧属性面板“恢复自动布线”，收缩同一拆分组中可安全移除的二连路径节点，但不穿过真实分叉。
- 图元或母线变换若使未随选区移动的节点约束无解，则取消整个变换并只显示一条汇总 Toast；节点与对象共同选中时随选区移动或 90° 旋转。含节点的混合选择首版不支持整体缩放。
- 变换预检只阻止本次由有效变为无解的手动节点约束；图纸中已经无解的线路不阻止无关图元移动。任何几何变化都应重新尝试既有无解网络，使移开遮挡物后线路能够恢复，而不是永久复用旧无解状态。
- 线路支持选择、删除、撤销/重做；图元变换后自动重新路由，删除图元时清理关联支路和孤立拓扑。
- 正式网络路由在 Web Worker 中异步计算并采用 latest-wins 调度；交互和提交期间显示稳定快照的轻量预览，新建边先显示接线预览路径，最终结果到达后再无阻塞替换。

### 本轮边界

- 线路业务数据采用连通网络、图元锚点/母线 tap/自由节点和相邻节点边；不保存自动折点、派生分叉或 SVG 路径命令。
- 统一节点、手动改线、兼容子线接入与节点起线已实现：自动路由仍生成未约束区间和全部派生折点，用户只持久化少量自由节点硬约束。当前不支持任意中段/自由位置线路标签、含节点选区整体缩放或服务端协作；独立子线的受约束端点标签及选区内部拓扑复制已支持。

### 已实现：统一节点与节点起线

- 用户界面和数据模型把现有途径点、分流节点统一为一种“节点”；路径节点与交汇节点不是固定类型，而是按实际邻接物理子线段的连接度派生。
- 连接度为 1 的自由节点承担开路端点，连接度为 2 时承担路径约束，连接度大于等于 3 时承担分流/汇流；只有连接度为 0 的自由节点自动清理，图元锚点和母线 tap 等宿主端点不受影响。
- 节点单击选择、拖动移动、双击起线；编辑 hover 显示空心圆与“双击从节点接线”，普通编辑画面和监控模式不增加常驻标记。
- 从节点起线可连接兼容图元锚点、母线、现有节点或子线；新逻辑线路默认双向并使用类型默认色。显式连接同网络不同节点可建立闭环，自连和相同两节点间的重复边被拒绝。
- 删除连接度为 2 的节点会局部恢复自动路由；删除连接度大于等于 3 的节点会移除全部邻接物理线段且不恢复主管。
- 节点可与图元和母线共同移动或 90° 旋转；同点兼容节点自动合并，自由节点与已连接图元锚点精确重合时由锚点吸收节点并清理退化物理区间。拖动与图元锚点相邻的子线段时，手动节点遵循用户实际拖到的 8px 网格位置，不再沿锚点方向自动插入前一格引出点；子线段拖回原路径或锚点过渡位置时，系统会清除本次手动段节点，并在每轮重路由后继续回收导致线路立即反向的独占路径节点，直至恢复无折返的自动路径。共享节点和分支节点不参与该回收。冲突整次回滚并只提示一次，含节点选区继续不支持整体缩放。
- 颜色、标签、显隐和单向通行继续属于逻辑线路；节点只表达坐标、拓扑与物理分段。详细方案见 `UNIFIED_CONNECTION_NODE_DESIGN_BRIEF.md`。

### 已实现：自动拐角候选节点

- 自动路由生成的普通子线 90° 拐角在编辑 hover 时表现为可操作的候选节点，但默认不保存、不进入历史，也不把自动路径转成手动约束。
- 用户可直接拖动候选拐角，或双击候选拐角派生新子线；只有拖动或接线成功后，候选点才在同一事务中固化为统一节点。取消、非法目标或无解不留下节点。
- 未固化候选点随自动重路由移动或消失；普通交叉、不同网络视觉重合和拱桥显示几何不产生连接。详细方案见 `DERIVED_CORNER_NODE_DESIGN_BRIEF.md`。
- 实现不新增 Schema 字段；纯点击、`Esc` 与未完成接线不产生节点或历史，成功拖动或接线只形成一个可撤销事务。当前等待用户在复杂管路图中完成视觉与操作验收。

### 阶段 A 已实现待验收：制冷管路静态样式与方向

- 制冷管路首阶段已在编辑模式实现双层世界空间静态管道样式：主要线路为 `10px / 2px` 管体/内芯，辅助线路为 `4px / 1px`，两者共享带内阴影深色平头管体、圆头回路色内芯、`16px` 世界空间命中、普通弯头 `8px` 圆角与单向箭头缩放适配。制冷拱桥均为 `16 × 8px`，主要/辅助世界空间遮罩分别为 `18px / 12px`。下层平头直接止于图元锚点中心，共享物理区间由主要样式优先；三方向及以上分叉保持直角，避免圆角破坏连接语义。
- 管路方向分为内部定义方向、允许流向和实际流向三层。逻辑边的 `sourceNodeId → targetNodeId` 只提供稳定内部方向；现有 `flowDirection` 继续表达双向、正向或反向允许通行；未来运行态再派生正向、反向、停流或未知的实际流向。
- 节点没有方向。拆分子线和固化拐角时延续原线路的物理允许方向；移动、合并和自动重路由不得根据左右、上下、冷热类型、锚点名称或创建顺序猜测水流。
- 无真实遥测时，已使用确定性模拟 Provider：运行水泵出口作为闭环流量注入点，同一水泵入口作为回收点；冷却子线还可显式把一个物理端点配置为水流源头，向同一有效管网中其他可达的连接度为 1 的开放自由端注入。直接接线保持冷热类型隔离，设备内部按一次/二次回路族允许同侧冷热端传播且禁止 PHE 跨侧串水；同一族出现两组及以上完整且冷热集合一致的编号端口时，每个编号保持独立，不跨组串通。当前按统一无量纲阻力求解闭合或开放网络的有符号视觉流量。死端、无源开路、停泵、零功率、无可达出口、关阀和逆单向约束保持静态，多驱动对冲区间按净流量决定方向。
- 水泵出入口在现有锚点编辑器中按专用素材资产级配置：资产标记为水泵，冷却锚点分别标记唯一入口和出口；全部实例继承角色，旋转不互换。配置缺失或类型不兼容不阻止静态编辑和保存，但该水泵不驱动模拟并返回诊断。通用图元首版不提供实例级水泵角色或端口覆盖。
- 制冷视觉模拟使用固定 `100 m³/h` 水泵/显式管路源头基准流量；水泵支持 `0–100%` 输出功率，显式源头当前固定为 `100%`。流量经等阻力网络分配并控制动画方向与速度，满足视觉流量守恒，但不计算压力、扬程、真实阻力、管径或其他工程水力。水泵启停与输出功率按 Schema v35 进入实例快照并产生 dirty，但不进入编辑历史；派生流量、指标、趋势、动画和 Provider 仍不进入项目。水泵入口/出口角色、子线允许方向和源头端点属于持久化编辑配置。详细方案见 `COOLING_PIPELINE_DIRECTION_DESIGN_BRIEF.md` 与 `COOLING_VISUAL_FLOW_SIMULATION_DESIGN.md`。

2026-09-01 增量：母线接入点在用户口径中统一为“节点”，内部保留母线宿主约束。保存 offset 直接决定实际位置，路由不再自动滑移；节点可 hover、选择/框选、与图元和母线混合移动、沿母线单独移动、双击起线和删除，同一母线同一位置的接线复用一个节点。母线缩短不得越过节点，冲突时停止在约束位置并在单次手势中显示一条 Toast。该增量取代早期 tap 自动滑移与按数量压缩规则，不改变 Schema v29。

## 当前增量：电力母线

### 已确认目标

- 电力图纸支持独立母线对象；母线只保持水平或垂直，不产生折点，也不存在两个可接线端点。
- 母线以可拖入的线路工具创建，但不作为普通 SVG 图元或图元资产。
- 普通电力接线可以接入母线范围内任意 8px 网格点；同一母线上的全部支路天然连通。不同母线可以通过一条明确可见的普通电力子线互联，子线使用两个母线 tap 和一条普通线路边表达，不把母线直接隐式合并。
- 母线支持选择、移动、8px 步进长度调整、90° 旋转、删除和撤销/重做。保存的母线节点位置是实际位置；缩短不得越过节点，调整起点时反向更新 offset 以保持节点世界坐标，越界手势被约束并只显示一条汇总 Toast。
- 选中一条或多条母线后可批量修改颜色；连续颜色输入只预览，确认后单次提交，恢复默认时删除自定义颜色字段。已建立 tap 和当前候选接入点始终使用所属母线颜色。
- 母线可与普通图元一起被框选、显示独立选中态并进行整体移动或 90° 旋转；包含母线的混合选区不提供整体缩放。
- 普通接线未显式接入而正交穿过母线时始终由普通子线绘制拱桥表达不连通；共线重叠禁止。
- 母线节点固定在保存的合法 8px offset；用户可沿母线拖动或用方向键调整，同点接线复用一个拓扑节点。路由按完整路径长度和转折数选择折线路径，锚点方向只在等价方案中作为软偏好，且不改变节点位置。
- 真实持久化电力节点作为一个或多个子线的端点主动落到唯一母线范围时，自动原位转换为母线宿主节点并接入；同点既有母线节点直接吸收，同母线网络同步合并。被动路由穿越没有真实节点时不接入，多母线交点无明确节点归属时拒绝变换。
- 母线节点的法向两侧接线共享同一个宿主节点；普通线路真正穿越母线的拱桥点继续禁止接入，被动交叉不生成节点。
- 母线默认没有文字标签；单选一条纯母线时可在右侧属性面板填写或清空标签、配置标签文字颜色，并通过独立开关隐藏或恢复当前母线的非空标签。颜色缺省为 `#B6B8B4`，支持 HEX、即时预览与恢复默认；标签使用固定 14px 世界字号，可绑定母线起点或终点并放在端点方向的垂直两侧：水平母线为上/下，垂直母线为左/右。标签沿所选端点向母线内侧展开并保持 8px 轴向 padding 与 8px 法向间距；单选后可拖动选择四种方位，文字保持水平且不参与拓扑、框选、吸附和线路障碍。

### 本轮边界

- 母线首版仅用于电力线路；可进入普通图元的混合框选、移动、旋转、复制粘贴和 Duplicate，但不进入图元缩放或锚点编辑器。母线与其连接对象同时位于选区时复制内部 tap 和逻辑接线；连向选区外对象的 tap 与支路不复制。
- 母线选中后可从空心候选 tap 发起普通子线；同一母线不能自连，不同母线必须通过显式普通线路边连接。

## 监控模式：已确认能力与首轮状态

- 监控模式与编辑器建立明确的运行时边界：完整项目先派生单张图纸、资产、连接、下探关系等只读视图，Switch/泵阀状态、外部供电和高频指标通过独立运行态上下文及可替换 Provider 注入。当前应用必须反向使用同一边界，后续另一个 React 项目可复用只读查看器并把下探接到自身导航；编辑 Store、dirty、历史、会话视口和随机读数不得成为跨项目运行时输入。阶段方案见 `MONITOR_RUNTIME_ARCHITECTURE.md`。
- 在一次接线图上呈现设备状态、告警和运行流向等动画反馈。
- 提供趋势、指标或状态统计图表。
- 监控设备详情覆盖 Generator、单体 UPS 及按实例配置为“显示设备面板”的 Switch；Switch 缺省保持原 On/Off 控制，型号文本不决定交互。图元和子线标签的数据列均可打开 1 小时趋势浮层；当前趋势是可重现演示派生，真实历史查询仍待数据接口。
- 支持遥测数据模拟，生成温度、功率、压力、流量、开关状态和告警等运行数据，以便在真实数据源接入前开发、演示和验证监控流程；不包含电气潮流、暂态或冷却水力等物理计算。
- 编辑文档状态与高频运行数据需要分层管理，避免监控刷新污染图纸历史记录或保存数据。
- 电流与冷却水流的移动虚线使用独立 WebGL 运行态覆盖层，复用包含拱桥的缓存显示路径；不得通过逐帧修改 SVG 路径、React 状态或业务路由实现动画。监控模式按物理区间区分流动与静止：静态 SVG 内芯直接把流动区间绘制为业务色、无流动区间绘制为原业务色压黑 50% 的最终不透明色，不在完整原色线之上叠加静态覆盖；WebGL 只叠加流动区间的移动虚线。播放暂停不改变流动资格。
- 运行态按线路类型与样式批量绘制，每帧只更新统一时间或 GPU Buffer；遥测更新、视口裁剪、后台暂停和减少动态效果策略见 `MONITOR_FLOW_ANIMATION_DESIGN.md`。
- 首轮监控模式以 Grid、Generator 和 Battery 为电力 Source，以算力 POD、动力 POD 和 FM 为 Target；未配置单向约束的线路不以创建顺序猜测业务方向，已配置单向的逻辑边只能顺箭头传播。每个 Source 图元独立计算到各可达 Target 的最短路径，再取全部 Source 路径并集，邻近 Source 不得抢占另一 Source 的较长有效路径；同一区间出现相反推导方向时不动画。无 Target 整链和死端支路不动画。Switch、2WV、CV 的 On/Off 保存于 Schema v23 图元快照并镜像到兼容运行态存储，可从编辑属性面板或监控画布控制；Off Switch 使其两侧相邻普通子线不显示流动动画，Off 2WV/CV 在作为制冷阀门时切断内部水路。Generator 运行/停机与 Battery 充电/放电对 Source 激活的门控尚待后续运行态增量。
- 设备层电力子图把父图关联设备视为带通道的外部供电边界：双路受电设备按 A 可达优先、否则 B、否则停电选择唯一实际通道；用户标记并配置 A/B 的子线入口只在父级选择相同通道时成为 Source，未分通道的历史入口继续兼容。若整张子图没有显式入口，则回退为母线首个接入点和明确单向的自由输入端。Cabinet 与明确单向的自由输出端成为 Target；Cabinet 的 A/B 锚点互不内部连接且同一时刻只保留实际供电一路。监控模式单击已映射图元直接下探子图，显式 `drillDownDiagramId` 优先并可从右侧属性面板配置；结构、导航历史和播放状态保持只读会话语义。双向自由端不猜测方向，Battery-group 仅在 UPS-group 父级 A/B 均失电时为对应 UPS 子图提供自动备电，不反向给父图供电。
- 显式入口供电的设备子图中，至少一个 tap 参与有效路径的自动方向母线应覆盖完整物理长度；同一已带电设备从该母线接入的其他明确单向入线也应动画。该补齐不得扩散到双向冗余线、Battery-group、普通环路、关断设备或方向冲突区间。
- 每个普通图元实例可配置最多 5 项数值型或文本型运行指标及独立显隐；编辑模式对数值取模拟范围中间值、对文本取首个正常候选，稳定预览组合标签且不刷新。监控模式进入时立即生成模拟值，之后每 30 秒更新一次；文本候选按正常 90、次要 6、重要 3、紧急 1 的等级权重抽样，使正常占绝大多数。数值型支持上限、下限和正常区间外阈值，文本型由候选状态直接指定等级；两类指标在次要、重要、紧急时只给数值或文本状态增加黄、橙、红紧凑背景与黑色文字，指标名称和单位保持原样且不带底色，正常数据保持透明背景和中性色。数值单位以半角括号紧跟指标名称，例如 `有功功率(kW)`，读数列只保留当前值；设备名称、运行数据整体、全部指标名称与单位分别控制显隐，关闭指标名称与单位后只收紧组合标签并保留读数或文本状态及异常数据背景。配置随图元保存和复制，预览值与当前模拟值不进入项目文档。
- 首轮电力与制冷 WebGL 移动虚线均已实现。制冷按运行水泵出口到同一水泵入口建立有效闭合管网，也允许显式冷却子线源头向其他可达开放自由端建立开放管网流量；两类驱动执行相同的直接接线类型、设备内部回路族、子线允许方向和阀门门控，普通阀门开启后双向，CV 开启后仅允许入口流向出口。当前按统一无量纲阻力求解多驱动与并联管网的有符号视觉流量，符号决定方向、绝对值决定速度。监控模式允许单选泵阀并通过右侧运行面板启停、开关和调节水泵 `0–100%` 输出功率，但图纸结构保持只读。监控画布保留主要 `10px / 2px`、辅助 `4px / 1px` 的管体/内芯层级，只在有效内芯区间用独立 `cooling` 样式批次复用电路的深色 `6px / 6px` 移动虚线；覆盖宽度使用对应世界空间内芯宽度并随 zoom 匹配，电力覆盖继续使用固定屏幕空间宽度。不复用电力 Source→Target 最短路径拓扑，也不模拟压力。

## 成功标准

以下为当前可验证成功标准：

- 用户能在画布中拖入图元，并通过单选、多选或框选完成旋转、移动、缩放和贴齐网格操作。
- 每个普通图元默认显示设备标识；单选后右侧属性面板提供独立开关，只隐藏或恢复当前图元且不影响其他图元或母线标签。显隐设置随图元复制、撤销/重做、JSON 和本地保存持久化；显示时，未选中或多选标签不接管指针，单选图元后可拖到画布坐标的上、右、下、左侧。自动布局只避让实际已连接锚点的出线区域，未连线锚点不改变默认候选方位，接线增删后按当前拓扑重新布局。
- 通用图元以默认灰色可改色虚线框、默认 `#121316` 且可独立改色的背景和框内白色设备标识呈现；右侧可分别修改虚线框颜色、背景颜色，以及吸附 8px 网格的宽度与高度，并通过默认开启的实例开关隐藏或恢复虚线描边。关闭描边不改变背景、标识、锚点、运行指标和两项已配置颜色。旋转后标识仍水平，长文本以省略号和旋转内框裁剪保证不溢出；设备运行指标仍在四侧显示并受既有运行数据及指标名称/单位开关控制。锚点编辑器中新建通用图元锚点时，默认类型随当前图纸系统选择，冷却图为“通用”、电力图为“电路”；已保存锚点类型由用户明确控制。
- 图元产生实际 8px 网格位移后才显示十字对齐区域；多选拖动时每个图元各自产生一组，重叠区域只统一着色一次。纵向带宽等于图元当前显示宽度，横向带高等于图元当前显示高度；结束或取消拖动后消失，且不修改项目数据。
- 多选整体缩放保持普通图元登记的逻辑宽高比、通用图元进入手势前的自定义宽高比和全部对象相对位置；多选整体旋转围绕选区中心以 90° 步进进行。两类变换提交后，每个图元的 X、Y、宽、高均为 8px 整数倍。
- 框选能严格包含完整母线，并允许一个或多个母线与图元形成混合选择；混合选择整体移动和旋转后，各对象仍贴合 8px 网格，母线保持水平或垂直且长度不变。
- 用户可通过 `Esc` 清空主画布选择，通过 `Command/Ctrl+C`、`Command/Ctrl+V` 和 `Command/Ctrl+D` 把全部已选图元与母线复制为新选择，并通过 `Command/Ctrl+X` 剪切后移动到同专业的另一张图纸，通过 `Command/Ctrl+S` 保存当前项目且不触发浏览器默认“保存网页”；选区内部接线关系同步复制并自动重路由，选区外支路不进入剪贴板。跨图纸副本落在目标当前视口中心，同图纸副本沿用固定网格偏移。
- 用户可用上、下、左、右方向键把全部已选图元与母线沿对应方向移动 8px；长按或快速连按时画面按帧响应，停止后只执行一次历史提交和正式线路重路由，一次撤销恢复整段连续移动。
- 画布不挂载标尺或鼠标 X/Y 坐标显示；自适应点阵、状态栏网格间隔与滚轮缩放保持一致，移除项不占用绘图空间或参与指针移动更新。
- 滚轮缩放后，当前可见间隔是能保持至少 8px 屏幕点距的最小 8px 世界单位倍数，状态栏同步显示 8、16、24、32px 等当前间隔。
- 仅使用缩放按钮、鼠标滚轮缩放、触控板捏合/双指平移、中键平移、空格+左键平移或切图自动适配时，项目文档、更新时间和 dirty 状态均不变化，顶部继续显示“已保存”；切换图纸或刷新后无需恢复上一次视口，进入目标图纸时自动居中看全其稳定业务内容。
- 在当前最大真实归档的 POD A 图纸中，25% 缩放下中键、空格+左键或触控板双指短距离平移不触发逐输入事件的 React 画布重渲染，SVG、点阵与监控流动层保持同一视口；中键基准机器帧时间 P95 约 16.8ms。长距离平移按 120px 屏幕距离低频刷新 overscan 裁剪集合，手势结束后同步最终会话视口。
- 主画布无外层 padding、边框和圆角并铺满整个应用；返回、编辑/播放、缩放、标题/路径和状态 HUD 均位于画布内部，其中返回/路径透明，命令使用 AIDC 监控模式小型浮层，状态文字保持透明，不形成会改变画布尺寸的新布局层。
- 用户能新建、保存和重新打开浏览器本地项目，并通过 JSON 完成有校验的导入和导出，图纸层级及图元数据保持一致。
- 素材区分别只显示一个默认 Off 的 Switch、2WV 和 CV；旧项目中的 Switch On/Off 实例自动迁移到统一 `switch` 素材键，并保留自定义名称、实例属性和可合并锚点；既有 `2-wv`、`cv` 实例保持稳定键和锚点。
- 用户能从右侧属性面板流畅拖动颜色选择器并即时预览 Generator、Grid、MP、Transformer、Tap-off Unit 的单一实例颜色、通用图元彼此独立的虚线框颜色与背景颜色，以及 Switch、2WV、CV 各自独立的关/开状态颜色；编辑模式切换任一 On/Off 图元后立即显示对应素材和颜色。连续预览不写项目或触发线路重路由，最终颜色修改只占一个撤销步骤并随项目 JSON 和本地保存持久化；On/Off 状态本身随本地项目存档、JSON 导入导出和图元复制保留，状态切换不修改项目 dirty、更新时间或历史。
- 用户选中一条或多条母线、逻辑子线，或两者组成的线路混合选择后，可在右侧属性面板通过同一个应用内 HEX 选择器批量预览、提交和恢复颜色，不打开系统 RGB 面板；跨网络追加选择与网络主干展开后的逻辑边均正确生效，重新选择后显示已保存值，多色状态不伪装成默认色。母线 tap 与候选点同步所属母线颜色，整次批量修改只占一个撤销步骤，并随对象内部拓扑复制、项目 JSON 与本地保存持久化。
- 用户手动多选纯图元、纯母线或纯节点间子线后，可在右侧属性面板批量修改选区共同支持的标签显隐；图元与子线还可批量控制运行数据、指标名称与单位并用明确选择的整套模板覆盖指标，图元同一颜色模型可批量改色，Switch/2WV/CV 可批量修改关/开颜色和 On/Off。混合布尔值显示三态，混合指标不隐式合并；每次普通批量操作只占一个撤销步骤，每个目标指标 ID 独立，On/Off 继续不修改项目 dirty 或编辑历史。
- 用户单选独立逻辑子线后可填写、清空、隐藏和恢复该边标签；标签拖动可在源/目标端点和每个端点的两侧间切换，横向端点段对应上/下，纵向端点段对应左/右。路由变化后标签继续按所选端点与侧向贴合新的局部方向，且文字、显隐、端点和侧向随撤销/重做、内部拓扑复制、JSON 与本地保存持久化。
- 用户单选或纯子线多选后可把通行方向设为双向、起点→终点或终点→起点；混合方向正确回显为“多种方向”，批量修改只产生一次项目提交和一个撤销步骤，母线混合选择不误显示该字段。单向箭头始终位于对应逻辑目的端并随缩放保持固定屏幕尺寸。逆箭头的电力 Source→Target 路径不得进入动画，方向设置随撤销/重做、内部拓扑复制、JSON 与本地保存持久化，v15 及更早项目缺省迁移为双向。
- 画布空选时，右侧属性面板按图元、母线、子线列出当前实际颜色和使用数量；选择一个颜色后只全局替换同类别且当前颜色相同的对象，连续调整只做 SVG 原位预览，完成后形成一个可撤销历史事务。
- 用户能从素材 hover 编辑入口打开双栏锚点编辑器，在非角点的边缘 8px 网格点添加、跨四边移动和删除锚点，并在顶部修改名称与六种明确类型。
- 锚点修改即时应用且可撤销/重做；方向由边缘自动确定，项目保存、JSON 往返和旧版本迁移不会丢失锚点。
- 用户能从已使用或未使用的图元锚点、母线、自由节点或空白网格点创建合法接线；兼容图元锚点、母线、另一网络节点和普通子线都可作为终点。落到子线会在实际共享段创建或复用节点，并保留原逻辑线路属性；用户显式闭合同网络回路被允许，不兼容类型、同点自连、重复边和不同网络共线重叠会被拒绝。
- 同一锚点连接多个目标时，各逻辑边分别按最短、少转折和中点优先规则派生；从相同锚点出发或等价中点路线自然重合时形成无标记共同段，业务数据和画布均不出现固定分叉连接点。
- 用户可拖动任意普通子线段创建或调整节点；节点可单独或与图元/母线一起选择、移动、删除和 90° 旋转。共享节点的移动和按连接度删除对全部引用子线一致生效，恢复自动布线会正确清理路径引用和孤立点。
- 自由节点在编辑态 hover/选中时显示圆点，默认和监控模式无常驻标记。节点落到同一 8px 点时，同类型网络自动合并，不兼容类型整次回滚并提示；二连节点删除时局部恢复自动路由，三连及以上节点删除相关线路且不恢复主管。
- 图元或母线变换若使节点硬约束无解，整次变换被阻止且只出现一条汇总 Toast；项目数据、历史和线路几何不产生部分提交。没有节点约束的线路继续遵循最短、少转折和中点优先的默认自动路由。
- 拖动图元时线路端点逐帧跟随但不逐事件执行完整网络寻路，松手后只执行一次正式避障重路由；起线跟随以当前 8px 网格点为最小更新粒度，并在单个动画帧内合并重复输入。
- 在当前 67 图元、13 母线、73 逻辑边真实归档中，生产构建从未选中状态直接拖动已接线图元时，主线程不出现 50ms 长任务；正式路由在后台完成且旧任务结果不会覆盖新状态。
- 在当前最大真实归档的 125 图元、20 母线、67 网络、171 逻辑边场景中，移动一个已接线图元后的正式路由只重算受直接连接或空间走廊影响的网络，并复用其余线路；空闲时不挂载接线锚点，视口外静态对象不进入大图 SVG DOM。基准机器上的完整路由和增量路由耗时仅作回归对照，不作为跨设备固定承诺。
- 在“TMU到FM 01”的 52 图元、4 个大型合并网络、357 条逻辑边、201 个手动节点场景中，图元、母线或节点变换后的同步提交只校验本次新产生的手动约束冲突，不在主线程重试存档中原有的失效边；正式路由以受影响逻辑边而非整网络为失效粒度，原有失效边的局部恢复尝试留在 Worker。基准机单图元移动的同步校验应保持在 50ms 长任务阈值内，线路预览、节点合并和提交整理各自保持轻量。
- 拉伸母线时线路与 tap 使用逐帧轻量几何预览，松手后才执行一次完整路由；鼠标进入终点图元或合法锚点时不得搜索图元内部的不可达坐标，应直接预览到边缘或目标锚点。
- 接线虚线不得因起终点距离增大而耗尽交互搜索预算后消失；远距离预览可以使用更强的朝向终点启发式，连接完成后的正式 Worker 路由仍按完整精确评分生成。
- 自动路由不穿过图元区域，除合法锚点过渡外只走 8px 正交网格；不同网络普通交叉以无拓扑节点的拱桥呈现。
- 拱桥使用默认 8×4px 真圆弧并通过 12px 局部画布色底衬与下穿线路分离；可见线路和透明命中路径使用同一拱桥几何，且不使用滤镜、阴影或逐交叉 DOM 节点。
- 通用网络的解析类型随当前专用端点变化；线路拓扑随 JSON 与 IndexedDB 持久化，图元变换后能稳定重新路由。
- 用户能创建和编辑电力母线；普通电力接线可在任意合法网格点接入，同一母线支路保持拓扑连通，未连接交叉以拱桥表达。
- 用户缩短带节点的母线时，手柄停在最外侧节点约束处并给出一条 Toast；母线与节点几何可作为一步撤销或重做，节点不被比例压缩或重排。
- 母线接线保持用户确认的节点位置，自动路由只在固定端点之间求最短少折路径；图元锚点允许在端点立即切向转弯，默认出线方向只作为等价线路的软偏好，重复计算与重载结果稳定。
- 母线两侧接线落到同一 8px 网格点时复用同一个母线节点；不同宿主节点或图元锚点重合会阻止变换，未连接穿越点继续执行既有拱桥规则。
- 新建和旧项目母线默认无文字；用户填写母线标签后可在两个几何端点及各自垂直两侧形成的四种方位中显示，水平母线对应上/下，垂直母线对应左/右，并使用固定 14px 世界字号、8px 轴向 padding 和 8px 法向间距；标签颜色默认 `#B6B8B4` 且可按实例配置，清空文字后完全隐藏。右侧开关只控制当前母线，关闭时保留文字、颜色、端点和侧位；显隐、内容、颜色和方位均可撤销/重做并随复制、JSON 与本地保存持久化，但不产生可接线端子语义。
- 当长度、转折数和其他硬约束相同时，子线的折点组合优先接近两端中点，且不因同网络已有路径改变位置；结果保持确定性。
- 用户能从已选中母线发起普通子线连接另一条母线；两端 tap 联合滑移到最短少折位置，删除子线后网络按剩余路径正确拆分。
- 系统能把监控数据与图中设备可靠关联，并清晰表达设备状态与告警。
- 监控模式能在不修改图纸业务文档的前提下驱动状态动画和图表，并能在模拟数据与真实数据源之间替换。
- 用户在编辑模式为任意 Switch 实例选择监控点击行为后，监控模式按该实例打开 On/Off 控制或 MLVR 设备面板；设备标识改名不影响行为，配置经撤销/重做、JSON、IndexedDB 和 RuntimeBundle 往返保留。
- 监控模式未选中图元时不显示右侧面板；点击 Generator 或单体 UPS 显示对应 500px 设备面板，点击 `ups-group` 仍优先下探子图。UPS 线路画布使用项目现有素材和正交线路组成，不是截图。点击图元或子线标签数值时，360px 数据面板紧邻读数上方显示，不覆盖已选设备详情，空白处或 `Esc` 可关闭。
- 电力移动虚线只出现在至少一个 Source 图元沿全部子线通行约束到达可达 Target 的最短业务路径上，其中 Source 为 Grid、Generator 或 Battery，Target 为算力 POD、动力 POD 或 FM；较近 Source 不隐藏其他 Source 的有效路径。闭合但无 Target 的整条链路、有效主干旁的死端支路、Off Switch 两侧、逆箭头路径及方向冲突区间均无动画，同一 Source 的等长有效路径同时保留。
- 监控模式中，电力普通子线、母线与制冷内芯按最终显示几何的物理区间区分流动和静止；无流动区间直接使用原业务色压黑 50% 的最终不透明色，部分流动逻辑边只替换静止部分且共享段不重复绘制。静态层下方不得保留会从抗锯齿边缘透出的完整原色内芯。暂停播放不改变流动资格，编辑模式业务色与管路样式不受影响。
- 制冷水流不复用电力最短 Source→Target 裁剪；系统把每台有效运行水泵作为出口注入、入口回收，按等阻力网络求解物理区间的有符号视觉流量。直接接线兼容与设备内部水路分开判断，CT/PHE 同一回路冷热端可贯通而 PHE 一二次侧隔离；TMU 的完整编号冷热对按编号隔离，不允许回路 1 与回路 2 串通。TMU 回路 1 暂保持设备内部双向连接；回路 2 不在设备内部短接，而把“二次回路冷 2”作为外部闭环注入起点、“二次回路热 2”作为回收终点，只有外部有效路径闭合时才驱动。FM 固定为同编号回路冷端流入、热端流出，不允许热端倒灌冷端。显式源头不因上下游位置被自动改写为出口。对称外部闭环中因等阻力抵消而得到零净流量、但确实位于冷 2→热 2 有向循环且不是桥边的区间，使用最低可视流量保持中间环路动画；开放死端、桥边和不可达区间仍静止。无遥测验收使用固定、可复现的模拟状态，验证输出功率、停泵、关阀、单向约束、等价分流汇流、开路、并联水泵和净流向，不生成压力且不宣称工程水力精度。
- 用户能为单个图元或节点间子线配置、排序和删除最多 5 项数值或文本模拟运行指标，也能把明确选择的整套模板一次覆盖到手动多选的纯图元或纯子线，并分别控制对象名称、运行数据整体及全部指标名称与单位显隐；关闭名称与单位后所有读数或文本状态及异常数据背景继续显示。编辑模式组合标签立即用中间数值或正常文本状态稳定预览，监控组合标签每 30 秒更新。数值型正确执行三种阈值模式，文本型按候选等级正常优先抽样；两类指标只给异常数据使用黄/橙/红背景和黑色文字，标题与单位保持原样且无底色，正常数据保持透明中性，同时不因模拟值变化修改项目 dirty 或编辑历史。
- 绘图模型与监控模型具备可扩展性，能覆盖后续确认的 AIDC 电气设备和拓扑关系。
- 关键产品与技术决策可追溯，活动 TODO 与实际进度一致。

## 待确认范围

- 正式部署环境和浏览器兼容矩阵。
- 典型及最大图元数量、画布范围与性能目标。
- JSON 导出是否需要内嵌图元素材内容。
- 首版必须支持的电气图元与编辑属性。
- 实时/历史监控、告警、联动和权限的首版边界。
- 单张图纸最大图元数、同时动画数量、数据刷新频率和图表时间窗口。
- 数据源、通信协议、刷新频率与离线策略。
- 服务端版本管理和多人协作需求。

## 暂未承诺

待确认范围中的内容当前均不是已承诺功能。线路连接与管理已在第一阶段验收后进入实施；监控模式当前承诺 KD-054 至 KD-056、KD-065 至 KD-067 的电力流向/Switch/单向子线能力、KD-060/KD-062 的图元实例模拟指标、KD-079 的制冷管路模拟，以及 KD-141 的本地确定性设备/数据面板。实时遥测、服务端历史时序、物理水力求解、Generator/Battery Source 运行态门控、告警联动及后端协作仍需后续确认。确定新范围时，应同步更新本文件和关键决策记录。
