# TODO

2026-09-18 项目归档：新增 `scene-archives/WuHu AIDC 0918.json`，保留用户提供的原始内容；JSON 校验通过，Schema v43，含 14 张图纸、632 个图元、330 个连接网络。与 KD-205/206/207 对应实现、素材和文档一并纳入本次提交。

2026-09-18 水流默认虚线（KD-207）已实现，待用户视觉验收：应用、监控运行时及编辑画布冷却回退均默认虚线，电力仍默认圆点；原有手动切换、双系统会话记忆及宿主显式覆盖保留。现有监控流程测试通过，覆盖默认选项、跨系统切换及 dirty 保持；类型与补丁检查通过。

2026-09-18 冷却图元素材更新（KD-206）已实现，待用户视觉验收：10 个正常/关闭/故障 SVG 均与附件逐字节一致；默认尺寸为 CHWP/CWP 64×64、CT 128×128、PHE 80×80、CDU/TMU 48×48，关闭/故障素材与各自正常态同尺寸。图元目录和旧布局迁移配置已同步；92 项素材/项目/锚点/运行包测试及类型、设计系统、生产构建通过，覆盖旧 PNG 与 80×80 SVG 的默认/放大/旋转实例、锚点角色和接线 ID 保留、保存重载幂等。请检查新外观、插入尺寸、旧图纸占位及接线。取代 2026-09-14 本批对应素材的旧尺寸验收口径；项目功能目标和 Schema 不变。

2026-09-18 A/B 独立同时供电（KD-205）已实现，待用户视觉验收：取消 A 优先和单路互斥，父子图继承全部有效通道，运行时兼容旧单通道宿主覆盖；A/B 隔离、开关/方向和 UPS 双路失电备电保持。52 项专项测试、类型检查、设计系统检查及生产构建通过，包含真实归档 4 台 FM、五类受电设备、两级继承、断开/恢复和运行包往返；修正归档测试图纸名称。全量 588 项通过、1 项跳过、4 项失败，其中 3 项 DiagramMonitorCanvas 配色/指标断言已在修改前 HEAD 复现，另 1 项 App 锚点编辑用例并发超时后单独复跑通过。旧目标/决策/验收已归档；请复验双路同时流动、分别断开及跨图/备电表现。

2026-09-15 漏液区域迁移：仅“TMU到FM 01”从实际接有 TMU 和 FM 的管网中稳定选一个，保留关联 MP 告警；排除上游二次管网，取消 02 图纸漏液演示。取代下方旧范围验收条目，待用户复验监控显示位置。

2026-09-14 SVG 默认尺寸：按用户补充要求，CHWP/CWP/CT/PHE 默认统一为 80×80，恢复全目录 SVG 尺寸一致性断言；同步既有存档尺寸迁移以调整锚点并保留连接 ID。取代下条“保留旧尺寸”的临时处理，待用户复验。

2026-09-14 批量图元素材：按附件更新 22 个正常/故障/泵关闭 SVG，CPD 展示由 PNG 切为 SVG（保留旧文件）。本次仅替换外观，模板与实例尺寸、锚点和状态映射保持；部分新 SVG 为 80×80，仍在原实例范围内等比呈现。目标与决策不变，待用户视觉检查。

2026-09-14 冷却管路粗细（KD-204）：支持单选/多选配置管体总宽度，管腔和选中轮廓同步；留空恢复主/辅助默认。待用户复验编辑/监控、保存撤销、不同宽度交汇和跨线显示。

2026-09-10 电力子线平头（KD-203）：编辑、接线预览及监控底线改为 butt，母线和水路保持；待用户检查接线端点。目标与 Schema 不变。

2026-09-10 电路线宽口径澄清（KD-202）：母线/子线/光波保持与图元比例，统一随图纸同比缩放，取代此前固定屏幕像素的错误理解。待用户对照两张截图复验；自定义宽度值和 Schema 保持。

2026-09-10 固定电路线宽（KD-201）：编辑/监控取消世界层 CSS 缩放及合成优化，只保留 SVG transform；母线/子线显式固定描边。待用户复验缩放粗细、自定义粗细、动画和大图平移性能。目标与 Schema 不变。

2026-09-10 电力子线粗细（KD-200）：编辑右侧可配置 0.5–12px，支持批量与恢复默认；静态底线和光波同步。72 项面板/动画测试及类型检查通过；待用户复验保存重开、撤销、单选/多选、缩放和交叉位置。

2026-09-10 TMU 回路非均衡模拟（KD-199）：逻辑管路/FM 导通系数加入稳定差异，避免默认完全对称分流；保留关阀、停泵、死端限制。待用户复验当前 TMU到FM 图纸的停流段与汇流观感；历史存档回归不替代当前浏览器状态验收。

2026-09-10 PHE 素材：使用用户最新附件替换，SVG 仍为 80×128，保留尺寸、模板键和接线点；目标与关键决策不变，待用户查看外观。

2026-09-10 旁路状态修正：UPS-group-旁路 固定值仍为“旁路”，严重度读取旁路文本选项配置，不再强制正常；补充异常等级断言。项目目标不变。

2026-09-10 UPS-group-旁路：默认模拟器按设备标识精确匹配，该设备已有“供电方式”指标固定显示“旁路”，不追加指标、不改变显示开关或其他 UPS；待用户复验。

2026-09-10 额外异常数值修复：取消模拟器为红黄覆盖追加指标及旧默认指标注入/替换逻辑，只保留编辑配置指标和可见性。修复上轮只保留显示开关但仍追加数值的遗漏；待用户复验算力 POD、UPS L、POD A。

2026-09-10 数据可见性（KD-199）：取消模拟准备阶段自动显示异常数据，保留编辑开关 true/false/未配置值及故障状态；新双异常和旧冻结分配均覆盖。待用户复验隐藏异常设备数据后切换监控。

2026-09-10 冷却光波节点/分叉连续性（KD-198）：修复实际动画同速、相位却按原始流量倍率拆分的问题；保留跨线距离及不同管网隔离。待用户复验不同流量分支连续通过；不等长汇流和闭环闭合的相位接缝仍受既有策略限制。

2026-09-09 数据弹窗按 Figma 2988:3241 校准：修正时间选择器、图表留白/高度、刻度密度/字体、基线及曲线粗细，复用 AIDC 原语和 Token。保留数据与跟随定位，待用户视觉验收。

2026-09-09 指标弹窗跟随定位（KD-194）：平移/缩放画布同步投影原数据锚点，保留弹窗大小和边界避让；待用户操作验收。

2026-09-09 每页红黄异常（KD-197）：已完成单设备双指标异常、隐藏/空指标运行时补充及导出分配保留。20 项模拟/运行包测试与类型检查通过，包含全部设备类别连续五次刷新覆盖；待用户逐页验收，旧运行包需重新导出。

2026-09-09 修复文字无法点选/拖动：透明命中矩形显式设置 pointer-events=all，避免继承画布的禁用命中设置，接入原图元选中/移动处理；编辑显示移动光标。补充命中属性与事件回归测试，待用户验证实际拖动。目标、决策不变。

2026-09-09 存档打开异常：已复现并修复 MP 接入电力线路后重开触发锚点类型不兼容的问题；Schema 语义校验按所属图纸解释 MP 类型，与编辑/运行时一致，不改存档或删除接线。补充双系统传感器重开、原存档及截图对应两条线路添加传感器的回归测试，待用户重开实际存档复验。

2026-09-09 文字图元（KD-196）：已接入通用素材库、右侧文字样式配置及编辑/监控共享渲染；样式随实例保存导出。23 项针对性测试与类型检查通过，待用户检查文字显示与编辑体验。

2026-09-09 MP 通用传感器（KD-194）：通用分类、按图纸适配单测量端口、电力新实例温度默认值及接线规范化兼容已完成。81 项相关测试与类型检查通过，待用户复验同一项目冷却/电力同时使用 MP、保存重开及原漏液标识。

2026-09-09 制冷动画已改为配置速度：三种模式均由 `cooling.baseSpeed` 直接控制，不再随流量变化，主画布和设备小图同步；保留零流量门控。

2026-09-09 补齐子线圆点底色：修复独立线路分组入口漏传动画模式，切换模式时同步重算子线底色，待用户复验。

2026-09-09 修复圆点沿斜接网格插值变形：使用独立矩形覆盖及圆心归属，保留 GPU 动画；圆点模式底色与无流动一致。待用户复验截图中的长线与转角。

2026-09-09 圆点替代箭头已实现：双系统独立大小/间隔配置，电路默认圆点、冷却默认虚线（KD-207 取代旧光波默认），Tab 切换独立保留模式；设备小图与宿主默认同步。22 项定向测试、类型检查、设计系统检查及生产构建通过（保留既有大包提示）。待用户验收圆点形状和缩放表现。

2026-09-09 虚线间隔同色低不透明度已实现（KD-193 补充），两系统各自支持 `gapOpacity`，默认 0.25；待用户检查明暗对比。

最后更新：2026-09-09

## 当前

2026-09-09 新增虚线动画（KD-193）：播放旁可选“虚线”，电路/水路的速度、线长、间隔在 `FLOW_DASH_ANIMATION_STYLES` 独立配置。已补模式切换且不修改项目的回归，待用户检查流向、暂停冻结、缩放及两套参数效果。

2026-09-09 供电方式告警已修正：正常模拟选择主路并清除异常等级，异常模拟选择旁路并采用已配置异常等级（未配置时为次要）；缺少旁路候选时主路仍正常。15 项演示运行时测试及类型检查通过，待用户复验。

2026-09-09 漏液视觉加强：红色闪烁周期从 2 秒缩至 1 秒，两侧描边向外加粗一倍（主要每侧 1→2px，辅助 0.8→1.6px），保持原管腔与水流；仅作用于既定两张图的疑似漏液区域，待用户复验。

2026-09-09 漏液收敛（KD-191）：仅 TMU到FM 01/02 各一个管网红色闪烁，相关 MP 增加“疑似漏液”状态行，其他图纸不显示漏液。待用户检查区域与传感器对应关系。

2026-09-09 疑似漏液（KD-190）：方案 A 三次配色已更新；每张冷却图固定一个非空网络以现有红色侧壁两秒闪烁，管腔/流动保持，无详情和精确漏点。待用户验收区域范围、闪烁可读性及新三次配色。

2026-09-09 每页一个故障（KD-189）：默认模拟每张非空设备图纸固定一个在线故障，优先故障素材，隐藏指标不排除。待用户逐页检查故障位置稳定及电流/水流持续；旧导出包需重新导出以使用新分配。

2026-09-09 故障素材（KD-188）：九类图元已接入监控三级异常外观切换及正常恢复；故障不触发断电/断水。待用户验收实际故障外观、恢复和持续流动。

2026-09-09 电力母线接点隐藏（KD-187）：取消编辑和监控的常驻接点圆点，保留编辑交互提示与实际连接；专项 DOM 测试及类型检查通过。相关测试共 5 项通过、3 项失败，失败为用户已调整的无电流压暗和波长参数与旧断言不符，本次未修改这些参数。待用户复验母线/子线交汇及节点操作。目标与 Schema 不变。

2026-09-09 光波宽度分离（KD-186）：电力光波与线路等宽，冷却保留 60% 管腔宽度；分别通过 `waveWidthRatio` 修改。待用户复验电力子线/母线等宽和冷却表现，目标与 Schema 不变。

2026-09-09 FM 上下接口对调（KD-185）：已补齐实例开关、路由/画布同步和 Schema v42 保存兼容。60 项接口、属性面板及 SVG DOM 测试与类型检查通过；待用户复验对调、恢复、手动节点冲突、保存重开及监控显示。

2026-09-09 分接单元 A/B 已统一替换为 48×48 Tap-off Unit-group，素材栏单入口，旧实例保留锚点/接线/下探。80 项目录、项目及下探测试和类型检查通过，待用户检查旧图纸与新插入效果。

2026-09-09 母线批量指标（KD-176）：已补齐纯母线多选的模板复制、空白配置和批量显隐，47 项属性面板测试及类型检查通过；待用户检查模板应用、一次撤销及保存恢复。目标与 Schema 不变。

2026-09-09 标签置顶（KD-175）：图元/母线/子线文字及运行数据移到动画之上的独立 SVG，保持同帧视口同步与现有交互。7 项相关测试、类型检查和构建通过。待用户复验动画穿过标签、缩放平移、标签拖动和指标点击。项目目标与 Schema 不变。

2026-09-09 标签避让简化（KD-174）：图元标签仅避让自身已连接锚点出线走廊，不再避让其他图元/标签/线路。27 项标签测试与类型检查通过；待用户复验密集图纸，允许重叠且相邻对象移动不再挤动标签。目标与 Schema 不变。

2026-09-09 标签列间距微调：名称与数据列额外间距改为 2px（100% 缩放），保持左/右对齐与文字内边距；26 项标签测试通过，待用户验收。项目目标不变，详见 KD-173。

2026-09-08 TMU 对调显示回归已修复：圆点与命中区域原先仍使用模板坐标，现与路由统一使用实例位置。新增 0°/90° 实际 SVG 圆点对调及恢复测试，6 项接口相关测试、类型检查通过；请复验画布接口显示。项目目标与 Schema 不变。

| 优先级 | 状态 | 任务 | 完成定义 |
| --- | --- | --- | --- |
| P1 | 待用户验收 | 液冷编辑选中反馈（KD-184） | 检查主要/辅助管路单选、多选和整网选择时两侧中性亮边连续清晰、管腔提亮；取消选择恢复，复验转弯/分叉/跨线，监控和电力保持。 |
| P1 | 待用户验收 | 双动画切换与实心三角箭头（KD-177 / KD-178 / KD-180 / KD-181） | 监控播放按钮旁切换光波/箭头；默认光波，切换不产生未保存、不改变播放状态。检查电力/制冷实心三角箭头的可见度与朝向、沿实际流向连续移动、转弯/分叉、短管、跨线遮挡、停流隐藏、暂停冻结、缩放和设备面板小接线图；箭头为 5×4 图纸单位、20 单位间距，密度翻倍；随图纸缩放，不固定屏幕尺寸。检查箭头直接使用线路/管路颜色、不混白（KD-183），不再闪烁，移动基准速度 48 单位/秒、暂停冻结位置、恢复继续，文字仍在动画上层。最新移动箭头增量：32 项相关测试、类型检查及生产构建通过，待用户视觉验收。此前验证：44 项相关测试通过；追加暂停几何保持测试后，8 项箭头/共享场景测试通过；类型检查、设计系统检查和生产构建通过。 |
| P1 | 待用户验收 | 指标名称左对齐与分类数据列宽（KD-173） | 图元、子线、母线名称左对齐，文本/数值各自右对齐；复验 UPS L 的长供电状态不再撑宽 L1/L2/L3 数字列，告警底色和命中区域匹配。527 项测试通过、1 项跳过，类型检查、设计系统检查及构建通过。 |
| P1 | 待用户验收 | 母线数据、图元字号与完整场景取景（KD-172） | 检查母线运行指标与三项显隐、数据列紧凑间距、监控默认播放和暂停后切图保持；空选图元标签字号可保存且不影响母线/子线/SVG；跳转后全部图形、线路与文字位于面板外可见区域，读数刷新不反复缩放。526 项测试通过、1 项跳过；类型检查、设计系统检查及生产构建通过。 |
| P1 | 待用户验收 | 电力子线与母线加粗（KD-171） | 检查编辑子线 2.25px、母线 6px，选中态 3.75px/7.5px，接线预览和监控一致；光波 1.8px/4.8px 保持内缩，复验连接、跨线遮挡及设备面板小接线图。 |
| P1 | 待用户验收 | 制冷轮廓管腔与同色长拖尾（KD-164 / KD-165 / KD-166 / KD-167 / KD-168） | 检查主要/辅助管路两侧描边、暗底色、分叉连通、圆角、跨线遮挡、接线预览与选择；确认无内阴影、颗粒或辉光。运行时拖尾为管路本色且不混白，KD-182 液冷光波调整为 96 图纸单位长拖尾、2 单位连续亮度过渡、168 单位/秒，缩放时同一管段波动数量及相位不重排；动画为管腔宽度的 80%，与两侧描边保留间距，尾部不透明度最低 50%、头部 100%，转弯/分叉活动条带无黑色断口，亮头前进、拖尾向后线性渐隐，短管串联及跨线遮挡后不重启拖尾、同速分叉继承节点距离，正反向及并联/汇流符合实际流向，停泵关阀停流，暂停冻结、继续播放平滑恢复；缩放后方向与层级可读。电力光波与移动箭头参数保持；电力主画布及设备面板小接线图使用同色 50%–100% 渐变光波，KD-170 更新为 48 单位波长、1 单位过渡、168 单位/秒，无电流线路保留原色 50%；检查 A/B 色与自定义色、母线连续性及断电停流。该增量 33 项相关测试、类型检查、设计系统检查及生产构建通过，等待用户视觉验收。转弯接头与 50% 尾部增量验证：22 项相关测试、类型检查及生产构建通过。此前连续性增量验证：24 项相关测试、类型检查、设计系统检查及生产构建通过，等待用户复验动画。旧材质验收条目已归档。此前验证：全量首轮 508 项通过、2 项超时、1 项跳过；含超时用例和新增转角条带测试的 41 项专项复验全部通过，类型检查、设计系统检查及生产构建通过。 |
| P1 | 待用户验收 | 监控母线活动段类型色回归 | 通电与停电部分统一取类型绑定色，避免 A 路母线活动部分错误显示旧绿色；未分配保留自定义色，不修改存档或供电逻辑。6 项监控/配色测试和构建通过。全量 507 项通过、1 项跳过，另 3 项冷却样式断言与当前管芯宽度/压暗色值不一致，本次未改动。 |
| P1 | 待用户验收 | TMU 实例上下接口对调 | 单选 TMU 切换上下接口位置，不旋转图元；检查接线跟随、其他实例不变、手动约束冲突拒绝、撤销/重做、复制、保存及监控加载。全量 505 项测试通过、1 项跳过；追加项目往返测试后 4 项接口专项测试通过，类型检查及构建通过。 |
| P1 | 待用户验收 | A/B 分类后普通设备断流修复 | 限制网络通道推导作用域，普通开关/变压器内部正常导通；新增 A、B 两种分类的闭合导通/断开停流回归，全量 501 项测试通过、1 项跳过，类型检查通过。 |
| P1 | 已定位，待修复 | TMU 通用接口未推导冷/热导致停流 | 对 `WuHu AIDC 0904.json` 的“TMU到FM 01”只读诊断：原始仅 2 条活动边，TMU 接口均为通用；拓扑 family 已继承网络，但 thermalSide 未继承。内存对照按各实例所接网络明确接口后为 250 条活动边，三次网络 136 条边全部流动。后续应从特定网络派生通用接口的冷/热语义，保留明确接口约束及不同回路隔离；不批量修改共享模板或存档。 |
| P1 | 待用户验收 | 项目级 A/B 与冷却回路配色 | 空选右侧编辑八种类型色，切换图纸并检查线路、母线、模板锚点、画布锚点及监控内芯同步；A/B 单选多选整网修改，明确接口锁定，母线无接线时也可配置。已分配线路不可独立改色，恢复未分配后恢复旧自定义色；检查保存刷新、复制、撤销类型修改和 RuntimeBundle。A/B 来源识别按 KD-205 独立判断并允许同时供电，不因改色/分配类型制造供电。 |
| P1 | 待用户验收 | 缩小跨线遮罩 | 冷却主/辅助遮罩宽度 12px/8px，固定屏幕底衬 8px，冷却局部半长及动画避让半径 6px；检查缩放、编辑和监控播放时的跨线留白。 |
| P1 | 待用户验收 | 三次回路模板锚点颜色修复 | 补齐模板编辑器独立的锚点配色映射，三次冷为紫色 `#A970FF`、三次热为红色 `#FF4D6D`，不再落到默认灰色；未改变类型或模板数据。 |
| P1 | 待用户验收 | 主要冷却外管收细 | 主要外层从 10px 改为 8px，内芯 2px 与辅助 4px/1px 保持；检查编辑、监控和接线预览。 |
| P1 | 待用户验收 | 冷却管网回路类型编辑 | 选中冷却子线，在右侧回路类型修改所属整片网络；检查多选混合值及批量操作。连接任意特定回路接口时禁用且提交层拒绝，全通用端口/自由节点网络可改；预设色跟随，自定义色保持，撤销、保存往返稳定。 |
| P1 | 待用户验收 | 三次回路与 TMU–FM 隔离 | 将对应 TMU/FM 端口及管线改为三次冷/热，确认紫色 `#A970FF` / 红色 `#FF4D6D`；监控从 TMU 冷端经 FM 回热端，二次不串流，断开回流后停流。检查端口类型选择、存档往返及旧二次回路 2 兼容；Schema v38 不自动改型旧图纸。 |
| P1 | 待用户验收 | 冷却外层不透明加深色 | 主要外层为 30% 配置色＋70% 黑色，辅助为 15% 配置色＋85% 黑色，整体不透明；编辑、监控及接线预览检查重叠不再透色，内芯和跨线遮罩保持。取代此前已验收的透明方案。 |
| P1 | 待用户验收 | 直线跨线与局部遮罩 | 电力和冷却跨线无拱桥隆起，上层保持直线并遮挡下层；检查编辑、监控播放/暂停、主要/辅助、子线/母线、手动层级及缩放。普通转角圆角不变。冷却外层配置色调整已于 2026-09-07 用户验收闭环。 |
| P0 | 进行中 | 收敛 AIDC / 接线图共享 UI 单一真源 | 准备层已完成：业务导入统一为 `@aidc/ui`，锁定 AIDC `024e391` 的 schema v9 清单、schema v2 主题及核心文件哈希，并加入本地边界检查和相邻仓库漂移审计。下一步在 AIDC 上游化 `hideLabel`、IconButton 默认 title、NumericField 提交/默认范围、Switch mixed、Tab 键盘导航五项能力，更新 ComponentLab/Style Studio/契约测试并提取真实共享包；随后删除本项目基础组件、公共 Token 与 `components.css` 副本，仅保留线路 Domain/Pattern |
| P0 | 进行中 | 提取接线图运行时并接入 AIDC 工作区 | RuntimeBundle v2、专用监控画布、确定性模拟和宿主 presentation 已具备；本轮新增指标、设备态、图元和水泵派生流量回调。下一步补资产 resolver、运行时样式入口和 peer dependency 包构建，在 AIDC 增加 3D/接线图工作区切换、SingleLineInspector、运行状态 Store、图纸绑定表和数据 Provider 适配，并保证只挂载一个 R3F Canvas |
| P1 | 待用户验收 | 验收 CWP、CHWP、CT、PHE 新 SVG | 在冷却素材区确认 CWP、CHWP、PHE 为附件中的 80×128 竖版 SVG，CT 为 96×96 SVG，CPD 仍为原 PNG；新增实例检查图形、比例、透明背景、选择框、标签和缩放。打开含旧 200×80 CWP/CHWP/PHE、160×160 CT 的项目，确认实例迁移到新比例且中心位置可接受，旋转、设备标识、指标、水泵状态和输出功率保持；原锚点仍在对应边缘合法网格点，名称、类型、入口/出口角色及线路连接保留并自动重路由。最后复验撤销/重做、复制、保存刷新、JSON 与 RuntimeBundle 往返。 |
| P1 | 待用户验收 | 验收图元锚点即时转弯 | 分别从上、右、下、左图元锚点接线，确认网格锚点可在端点立即沿边缘任一切向转弯，不再被迫先沿法向走出一格；同时确认首段不能进入图元内部，也不会穿过其他图元。缩放或旋转图元后，非网格锚点只生成到外部 8px 网格的最短正交过渡。复验接线预览、完成后的正式线路、图元移动/旋转重路由，以及拖动锚点相邻子线段均不自动插入额外引出节点；撤销/重做、保存刷新结果稳定，母线接线仍保持法向进入。 |
| P1 | 待用户验收 | 验收 MP 单锚点管路测量模型 | 在冷却素材区确认 MP 为 32×32 单侧引线新素材。新增 MP 只应有一个底部“测量点”锚点，从该锚点直接点到一次/二次、冷/热任意现有管路后可成功接入，原管路保持连续，MP 不应出现入口/出口或截断动画。旋转 MP 确认引线和锚点方向同步改变；锚点模板编辑器中不能新增、删除、拖动、改类型或设角色。新 MP 默认显示温度、压力、流量，实例改色仍生效。导入 v36 `WuHu AIDC 0904` 或同类旧项目，确认旧 MP 位置、尺寸、旋转、颜色、标签和指标保留，前后管网合并且水流动画不因 MP 停止；保存、JSON 与 RuntimeBundle 往返保持 |
| P1 | 待用户验收 | 验收 CWP/CHWP 运行状态持久化 | 将 CWP、CHWP 或其他图元显式配置为水泵，在编辑模式单选后确认右侧显示“水泵运行状态”和 `0–100%` 输出功率，且不显示派生模拟流量。在编辑面板设为停止和非 100% 输出，切到监控模式后确认同一状态回显、停机素材与流量门控生效；反向在监控修改后切回编辑也应回显。任一模式修改都应让项目显示未保存，但不增加画布撤销步骤。手动保存并重新打开后保持，项目 JSON、图元复制和 RuntimeBundle v2 往返保留两个实例字段；v34 及更旧存档仍默认“运行 / 100%”，宿主显式覆盖优先，高频读数、趋势和动画相位不进入存档 |
| P1 | 待用户验收 | 验收监控设备详情与数据面板 | 进入监控模式、切换图纸及点击画布空白处时，确认空选状态不显示右侧面板。在编辑模式分别把两个任意命名的 Switch 配置为“控制开关”和“显示设备面板”，确认监控点击后前者只显示 On/Off、后者只显示 MLVR 面板，且改名不改变行为；保存刷新、撤销/重做、JSON 与 RuntimeBundle 往返后配置保留。点击 Generator 和单体 UPS 确认右侧打开 500px 详情，`ups-group` 仍下探；对照 Figma `3028:3395` 验收 20px 标题、状态/图纸/时间、无卡片底色的三列摘要、452×160 紫/蓝/青三相趋势、日/周/月切换、31px 四列告警表、半透明面板及 24px 节奏。UPS 线路画布应为 452×360 的只读小画布，并显示 Supply、Rectifier、Inverter、AC-AC Converter、Battery、Load、由正式路由器生成的 7 条正交线路及主路/电池的 5 条绿色运行支路；播放时确认运行支路叠加与主画布一致的移动虚线，暂停后停止移动并保留静态运行色。旁路、主路、负载和电池的文字应与图元配置标签的字体、描边、行高和列对齐一致，正常数据读数为白色，三相每行同时显示独立电压、电流，且图元与数据的整体边界位于画布正中央；确认它没有选择、拖动、缩放或接线入口。分别点击图元和子线标签数值，确认 360×233 数据面板在读数上方出现，40px 标题栏、76×24px 范围下拉、坐标/虚线网格和蓝色面积层与参考一致，其中“24小时”不得与箭头重叠或溢出；数值随 Demo Runtime 刷新、设备面板保留，并可用空白处或 `Esc` 关闭 |
| P1 | 待用户验收 | 验收 AIDC 监控模式视觉统一 | 在编辑、监控两种业务状态下确认：左面板均从顶栏下方开始且为 280px，常规右侧检查器均为 360px / 24px，监控设备详情是 KD-141 限定的 500px 例外；两侧面板背景、30px 模糊、12px 圆角和无阴影与参考 `97ecddf` 监控模式一致。检查树搜索、28px 行高、参考 SVG、低对比 hover/选中、层级缩进以及编辑态黑色管理工具组；检查编辑态素材格无常驻描边。检查返回按钮、透明路径区、6px 命令/缩放浮层，以及右侧眉题、标签、22px 对象标题和描述层级。最后复验新增/重命名/拖放/删除、素材插入、属性编辑、监控只读、键盘焦点、禁用和错误状态均未变化 |
| P1 | 待用户验收 | 验收五个新增电力图元 | 在电力素材分组确认 Supply、Load、AC-AC Converter、Rectifier、Inverter 均为独立 48×48 图元，名称、图形和比例与附件一致；分别双击/拖入并检查移动、90° 旋转、网格缩放、复制、设备标识、运行指标、锚点编辑、接线、保存刷新和 JSON 往返。Supply 与 Load 新实例应默认“运行”，编辑与监控右侧均显示“运行 / 待机”状态切换，待机颜色和运行颜色可分别修改、恢复默认及批量设置，切换后画布颜色立即对应；待机时演示有功功率与负载率为零且不告警。保存刷新、JSON 和 RuntimeBundle v2 往返后状态及两套颜色保持；其余三个不得出现状态或改色入口。五类设备均不应自动成为电力 Source/Target、因待机截断拓扑或建立父子绑定 |
| P1 | 待用户验收 | 验收 UPS L 演示校准 | 进入监控模式 UPS L，确认 5 组锂电的“剩余电量”均持续高于 80%；正常备电时间约等于 `2.0 h × SOC / 100`，同一组 SOC 升降时备电时间同向变化并至少显示 1 位小数；页面异常若落在备电时间，应在保持 SOC 关联的同时表达可用容量折减。确认 POD A 左侧 `ups-group` 带电时，UPS L 的外部进线、母线、5 台 UPS 并行输入与输出恢复电流动画；父图断电时仍全部停流。检查次要/重要/紧急异常数据底色为更鲜明的黄/橙/红，标题无底色且数据文字保持黑色 |
| P1 | 待用户验收 | 验收可移植拟真演示运行时 | 在监控模式逐页检查：每张有设备且未显式隐藏全部数据的非空图纸只有 `1–3` 个稳定异常；已配置指标按 Grid、Generator、Transformer、UPS、Battery、Switch、Supply、Load、Tap-off Unit、Cabinet、CDU、TMU、泵、阀、CT、PHE、MP、WMT、CPD 档案显示合理范围，旧手工阈值不再制造错误告警。无指标异常设备只在运行态补最多两项数据；关闭、待机或停机时功率、流量、负载率按设备语义归零，阀位接近零且不告警，离线显示 `--`、图元压暗并停止相关安全支路。播放后数据缓慢变化，暂停冻结，往返页面异常对象不变。导出 RuntimeBundle v2 后在另一宿主零配置打开，确认异常、严重度和初值复现；人工状态覆盖和真实 Provider 注入仍优先。已完成全量 `460` 项测试、`1` 项跳过，设计系统契约、TypeScript、补丁格式与生产构建通过；最终数值观感和逐页动画由用户验收 |
| P1 | 待用户验收 | 验收大型冷却图节点与图元编辑性能 | 打开“TMU到FM 01”，在 52 图元、4 个大型网络、357 条逻辑边、201 个手动节点及历史失效线路共存的完整图纸上，分别拖动接线图元、修改 X/Y、连续方向键移动、拖动单个节点、框选多个节点以及节点与图元混合选区；确认指针移动持续跟手，松手后不再出现约 0.75 秒主线程停顿，旧线路先以轻量几何稳定跟随、正式避障结果随后异步替换。确认本次新增手动约束冲突仍整次拒绝并汇总提示，历史失效边不会阻塞提交且相关障碍移开后仍可由后台恢复；撤销/重做、节点合并、保存刷新结果正确。再缩放和平移后重复操作，确认冷却管体、圆角、分叉、拱桥与选择态没有闪烁或残影。专用基准继续记录 dirty edge、同步校验、正式增量、预览、合并与提交整理耗时 |
| P1 | 待用户验收 | 验收冷却管路显式水流源头 | 在任意冷却图纸单选及纯子线多选，检查右侧“水流源头”的不作为源头、起点端、终点端和混合值；在没有水泵的开放管网标记入口，确认监控水流从该端以 `100 m³/h` 基准流向其他可达开放自由端，等价双出口各分得 `50 m³/h`。反转子线方向、关闭阀门或让 CV 逆向，确认冲突提示和停流；无可达开放端时保持静态。再复验多个源头、源头与水泵贡献叠加，以及节点分割/合并、反转、复制、撤销/重做、保存刷新和 JSON 往返后端点身份保持 |
| P1 | 待用户验收 | 验收 TMU 双回路隔离与 FM 冷进热出动画 | 在“TMU到FM 01”确认回路 1 只在冷 1↔热 1 内双向成环；回路 2 从 TMU 冷 2 向外流出，依次经过冷管、MP、FM 冷端→热端、热管后回到 TMU 热 2，TMU 内部不直接短接冷 2/热 2。确认 4 台 FM 的冷热接管及中间闭合环路均有正确方向和动画；底部长管右端若仍是连接度为 1 的开放死端，应继续保持静态。断开任一外部区间、关闭阀泵或阻断单向约束时，整条回路 2 停流；热 2 不能倒灌冷 2，回路 1/2 不串通。显式源头不会因位于下游而被自动改成出口；编号不完整的 CT/PHE/阀门/水泵保持原规则 |
| P1 | 待用户验收 | 验收电力 A/B 独立同时供电（KD-205） | 在楼宇、算力 POD、UPS-group、FM 与 Cabinet 检查：双路有效时同时显示电流；断开 A 只停 A，断开 B 只停 B，恢复后两路同时活动；两路均失电后普通设备停电。保持 A/B 锚点隔离、开关与线路方向限制，不回灌另一条失电支路。原单路主备验收已归档。 |
| P1 | 待用户验收 | 验收子线节点端落母线自动接入 | 在电力图纸把开路端点、二连路径节点和三连以上分流节点分别拖到母线没有既有节点的位置，确认松手后原位出现母线节点、全部邻接子线保留且整条母线拓扑连通；再把节点拖到既有母线节点，确认同点合并。用子线段拖动、节点与图元/母线混合移动、方向键和粘贴复验相同行为及单步撤销。让无真实节点的普通线路自动穿越母线，确认仍显示拱桥且不连通；把节点放到两条母线交点，确认变换被阻止且只提示一次，保存刷新与 JSON 往返后接入关系保持 |
| P1 | 待用户验收 | 验收图元模板锚点跨边拖动 | 打开除 MP 固定测量点外的普通图元接线锚点编辑器，在上、右、下、左四边各选一个已有锚点，分别一次拖到另一条边；确认预览始终吸附最近的非角点 8px 网格位置，跨边后方向线立即垂直新边向外，名称、类型、水泵/止回阀入口出口角色及稳定锚点身份保持。复验角点附近不抖动、重复位置拒绝、撤销/重做、关闭重开、项目保存与 JSON 往返 |
| P1 | 待用户验收 | 验收电力子图显式外部供电入口 | 在 UPS L 等电力子图单选及纯子线多选，检查右侧“外部供电入口”的不作为入口、起点端、终点端与混合值；把真实进线的外侧端标为入口并确认父图带电时动画先经过该进线，再进入母线、UPS 与输出，其他未连接母线不得自行带电。把通行方向设为阻断入口，确认出现冲突提示且监控停流，修正方向后恢复。再验证多入口合并、父图失电全部停流、清除全部显式入口后恢复历史自动回退；顶层电力图不显示该字段，冷却图显示独立的“水流源头”而非“外部供电入口”。节点分割/删除二连节点、复制、撤销/重做、保存刷新、JSON 往返及 v30 存档迁移后入口语义保持 |
| P1 | 待用户验收 | 验收母线人工监控电流方向 | 在编辑模式分别单选水平、垂直母线及纯母线多选，检查右侧“监控电流方向”的自动、左右/上下端方向、混合值与批量设置；人工方向母线 hover 或选中时应显示入口/出口文字，但不显示箭头或入口圆点，监控模式不显示编辑文字。旋转、移动、复制、撤销/重做、保存刷新和 JSON 往返后方向保持。进入有父级供电的分接单元 L，确认人工母线完整长度按指定方向流动、支路继续服从子线与开关约束；断开父级供电后母线停流。恢复自动后回到相邻接入区间最短路；旧 v29 存档打开后全部母线默认自动 |
| P1 | 待用户验收 | 验收 POD 下探、A/B 通道集合继承与 UPS 锂电接管 | 父设备两路有效时子图两路入口同时注入；仅一路有效时只保留对应入口；两路均失电时普通子图停流，对应 UPS 子图启用电池且不回灌父图，任一路恢复即退出备电。复验分接单元各自 A/B 输出、跨多级图纸、旧未分类入口、保存与 RuntimeBundle，以及旧单通道宿主覆盖。导航、播放与 dirty 语义保持。 |
| P1 | 待用户验收 | 验收框选/多选节点复制相邻子线段 | 分别单选、Shift/Ctrl/Command 追加多选及框选自由节点、母线接入节点，并与图元/母线组成混合选区后执行 Copy/Paste 与 Duplicate；确认每个显式节点直接接触的全部子线段随副本生成，下一跳支路、未选图元和未选母线不被复制，未选宿主一侧保留为同位置自由端点。检查颜色、标签、显隐、方向、冷却主辅、跨线层级和指标配置保留且 ID 独立；同图副本不重叠、跨图居中、原显式节点对应副本继续选中，整次操作可一次撤销。节点剪切应提示暂不支持 |
| P1 | 待用户验收 | 验收手动多选对象批量编辑共同属性 | 分别手动多选同类图元、母线和子线，确认右侧显示共同支持的颜色、标签显隐、运行数据显示、指标名称与单位及整套指标配置；Switch/2WV/CV 还应出现运行状态与关/开颜色。检查混合值使用“多种”或三态开关，点击只修改目标字段；取消颜色预览恢复各对象原值，确认和普通开关各只需一次撤销。给所选对象配置不同指标后，确认必须明确选择模板或空白配置，应用后内容一致但指标 ID 独立且整次可一次撤销。批量切换 On/Off 后刷新应保留，同时项目继续显示“已保存”；非几何属性不得引起线路跳动 |
| P1 | 待用户验收 | 验收 Battery-group 电力图元 | 在电力素材分组确认同时存在独立的 Battery 与 48×48 Battery-group，名称、图形和比例正确；双击和拖入后检查移动、90° 旋转、网格缩放、复制、设备标识、指标、锚点编辑、接线、保存刷新和 JSON 往返。打开旧项目确认自动补入该素材且既有 Battery 实例、锚点、接线和监控 Source 行为不变；Battery-group 首版不应出现改色、On/Off 或自动 Source 行为 |
| P1 | 待用户验收 | 验收图纸切换自动适配全部内容 | 分别从层级树、面包屑、返回、新建后跳转及删除当前图纸后的回退进入空图、小图、POD A 和 1 号楼等大图，确认视窗自动居中并以安全留白看全图元、母线及独立线路节点；小图不放大，超大图可低于 25%，异步布线完成后没有二次跳动。自动适配后继续缩放和平移应自然，且项目保持“已保存” |
| P1 | 待用户验收 | 验收 Tap-off Unit 子图元 | 在电力素材分组确认 32×32 Tap-off Unit 紧跟 Tap-off Unit A/B，名称、图形和比例正确；双击和拖入后检查移动、90° 旋转、网格缩放、复制、设备标识、指标和保存刷新。通过右侧“图元颜色”修改、恢复默认并撤销/重做，确认透明轮廓完整且刷新后颜色保留；通过锚点编辑器为它单独添加电路接口并完成接线，确认不会继承或修改 A/B 的锚点；删除或移动 A/B 时子图元不自动联动，符合首版独立实例边界。旧项目打开后应自动出现该素材，JSON 往返不丢失实例与颜色 |
| P1 | 待用户验收 | 验收 Cabinet 与 Tap-off Unit 改名 | 在电力素材分组确认同时存在 Cabinet、Tap-off Unit A、Tap-off Unit B 三个独立 48×48 图元，素材方向和名称正确；分别双击、拖入、移动、旋转、缩放和复制。打开含旧 Cabinet/Cabinet A/Cabinet B 默认实例的项目，确认名称更新为 Tap-off Unit A/B，原锚点和接线保持，自定义设备名称不变；新 Cabinet 应自动出现但没有继承两种分接单元的锚点。最后检查锚点编辑、设备标识、指标、保存刷新和 JSON 往返 |
| P1 | 待用户验收 | 验收水泵停机态素材 | 把 CHWP、CWP 或其他冷却图元配置为水泵并进入监控模式，确认默认运行时仍显示各自原素材；关闭“水泵运行状态”后立即换为本次提供的深色泵 PNG，重新开启后恢复原图。分别检查不同缩放和 0°/90°/180°/270° 旋转实例，确认停机图在原画布框内等比居中，锚点、线路、标签、指标和选择范围不移动；返回编辑模式应始终显示原素材，普通设备和阀门不受影响 |
| P1 | 待用户验收 | 验收 CDU 横版图元 | 在冷却素材分组确认 CDU 已换为本次提供的横版 SVG；双击和拖入后默认逻辑尺寸均为 192×96，图像比例正确且无额外底色或拉伸。打开包含旧 96×96 CDU 的项目，确认默认实例升级为 192×96，主动缩放和 90° 旋转实例保持原缩放比例及显示外接框位置，既有锚点仍在对应边缘且原接线可继续连接。再检查选择、移动、网格缩放、设备标识、复制、锚点编辑、保存刷新和 JSON 往返 |
| P1 | 待用户验收 | 验收整条子线跨线层级 | 在电力和冷却图纸中分别让一条节点间子线跨越多条不同网络子线，单选后把“跨线层级”依次设为下层、自动、上层，确认该整条子线的全部未连接交叉同步换层且路径不跳动；主要/辅助在自动态保持原业务顺序，人工上下层可覆盖它。追加多选不同层级应显示“多种层级”，批量设置只形成一个撤销步骤。再检查真正节点/分叉不出现拱桥，子线跨母线始终由子线在上；节点拆分、删除二连节点、复制、保存刷新、JSON 往返与 v28 历史保持层级。监控播放与暂停时遮罩、管体、内芯和移动虚线应使用相同上下顺序，不出现底线或母线动画穿透桥面 |
| P1 | 待用户验收 | 验收 TMU 冷却图元 | 在冷却素材分组确认 TMU 已换用用户于 2026-09-07 提供的 SVG；双击和拖入后逻辑尺寸为原 SVG 的 48×48。打开曾保存前几版 TMU 的项目，确认旧 48×64、72×96、64×96 默认实例迁移为 48×48，主动缩放实例保持原尺寸，边缘锚点仍位于 8px 网格边界。再检查选择、移动、90° 旋转、网格缩放、设备标识、复制、锚点编辑、保存刷新与 JSON 往返；首版应无预置锚点、无水泵/阀门/止回阀角色，也不出现改色或 On/Off 控件 |
| P1 | 待用户验收 | 验收子线标签运行指标 | 恰好单选一条电力或冷却子线，配置数值和文本指标，确认最多 5 项、编辑态稳定预览、监控态约 30 秒刷新及三级告警与图元一致。分别关闭“显示子线标签”“显示运行数据”“显示指标名称与单位”，确认名称、整组指标和指标标题独立显隐；隐藏名称后读数或状态仍可显示。拖动标签切换源/目标端点与侧向，确认组合标签跟随且不越过端点；在线上固化节点、删除二连节点、复制/Duplicate、保存刷新及 JSON 往返后，指标只保留一份且副本独立生成模拟值。旧 v27 及更早项目打开后外观不变，保存为 Schema v28 |
| P1 | 待用户验收 | 验收持续式线路绘制工具 | 确认只有点击画布 HUD 折线按钮后才能从空白网格点起笔；连续单击空白点新增正交线段且没有双击完成逻辑。分别从空白点、图元锚点、母线、节点和子线起笔，落到已有对象后确认当前线结束而工具仍开启。显式闭环后应继续等待下一次起笔；被动穿越不并网并保留拱桥。`Esc` 或再次点击按钮退出，已落段线保留，单独起点不留孤立节点，一次撤销移除整次工具会话的变更。最后验证电力/冷却默认类型、开路端点编辑、保存/刷新、JSON 往返和监控无流动语义 |
| P1 | 待用户验收 | 验收节点混合移动与节点分段子线 | 框选或 Shift/Ctrl/Command 追加选中多个节点、图元和母线，分别从任一已选图元、母线或节点拖动并用方向键移动，确认整个混合选区保持相对位置、统一吸附 8px 网格且一次撤销完整恢复。在线路上固化多个节点后，确认点击、删除、颜色、通行方向及冷却主要/辅助级别只作用于相邻节点之间的子线段；共享重合区只选中实际经过该段的边，不扩大到整网。恢复自动布线应只收缩同组二连路径节点，不穿过三连以上分叉。最后打开 v25 及更早含 `routeNodeIds` 的本地历史和 JSON，确认迁移后节点、标签、方向、主辅级别、电力/水路动画均保留，保存重开后 Schema v28 不再写回跨节点旧边 |
| P1 | 待用户验收 | 复验 17:06 存档与节点混合旋转 | 重新打开 17:06 本地历史，确认 `route-waypoint-1516e94d-1a7c-4b6c-bf8d-581b1015b986` 不再触发“节点必须位于 8px 网格点”，线路拓扑和手动路径仍在；随后混选图元、母线和节点做 90° 旋转，保存并重开，确认节点始终落在 8px 网格且不再产生同类错误。外部导入一个非编辑器前缀的越网格节点仍应被拒绝 |
| P1 | 待用户验收 | 验收拱桥业务层级 | 在编辑与监控模式分别让普通电力子线正交穿越但不接入母线，确认拱桥始终位于子线且缩放、平移或等待异步路由后不会消失；再让冷却主要—辅助、主要—主要、辅助—辅助线路分别以两种创建顺序和不同网络交叉，确认监控态每个跨桥都完整显示“遮罩、外管、活动或无流量内芯”三层，不出现底线贯穿、外管断口或同角色内芯盖回遮罩。主要—辅助始终由主要跨越；同级交叉继续按既有稳定顺序跨越。把交叉双方互换主要/辅助级别，确认拱桥立即换边但路径不重算、不跳动。最后检查真正节点/分叉不出现拱桥，动画沿最终带桥路径连续且保存数据不新增字段 |
| P1 | 待用户验收 | 验收标签运行数据独立列宽 | 分别为图元和节点间子线配置多条长短不同的数值或文本指标，并使用明显更长的设备标识、子线标题及指标名称；确认数据列只取最长数值/状态的宽度，全部行右边界一致，异常背景也使用统一数据列宽，长名称不会拉宽数据列。复验子线源/目标端点两种方向、图元四侧布局、隐藏名称与单位、正常与三级异常，以及只有标题的 14px 母线标签布局不变且没有重叠或裁切 |
| P1 | 待用户验收 | 验收图纸层级树管理与删除 | 用户在编辑模式分别检查冷却与电力树：从园区、楼宇、POD、设备图新增子图或同级图，行内重命名后 Enter/失焦提交、Escape 取消；拖到目标中部成为子图，拖到上/下边缘调整同级顺序，并用 `Alt+方向键` 验证键盘排序、提升和缩进。hover 非根图纸行时确认最右侧出现删除图标，点击后立即删除目标及全部下级图纸；删除当前路径中的父图时应回到其原上级，图元、母线和线路同步消失，删除其他分支不切换当前画布。根图不显示删除图标。分别点击冷却/电力标题与含子图节点的箭头确认可独立收起/展开，再用普通 `←/→` 验证键盘折叠；切换或新建图纸后当前路径应自动展开，折叠不应出现“未保存”。确认根图、跨专业、循环和超过四层会被拒绝，保存/刷新及 JSON 往返保留管理结果；监控模式不显示新增、重命名或删除入口且不可拖动，但仍可折叠导航 |
| P1 | 待用户验收 | 验收 macOS 触控板双指平移与捏合缩放 | 用户分别在 macOS Chrome 与 Safari 的编辑/监控模式检查：双指水平、垂直和斜向移动应自然平移，慢速、快速及惯性阶段不应突然切换为缩放；捏合应围绕指针平滑缩放，方向、灵敏度和最小/最大缩放符合预期。再连接普通鼠标，尤其检查 macOS 像素模式小步进滚轮仍执行缩放，并在 POD A 低缩放连续操作，确认图元、线路、点阵和监控动画同步、页面不滚动、浏览器本身不缩放且项目保持“已保存” |
| P1 | 待用户验收 | 验收触控板空格拖动画布 | 用户分别在编辑和监控模式用触控板检查：指针位于主画布时按住空格显示抓手，空格+左键连续拖动与手指跟随，松开左键可再次拖动，松开空格恢复原工具。重点从图元、母线、子线及画布空白处开始拖动，确认不会触发对象移动、框选、接线或状态切换；再检查属性输入框可正常输入空格，纯平移不出现“未保存”，大图流畅度与中键平移一致 |
| P1 | 待用户验收 | 验收五个 PNG 图元与 PHE 横版迁移 | 用户在素材库和画布检查 CHWP、CWP、CPD、CT、PHE 的透明背景、清晰度、颜色和缩放效果，确认前四者插入尺寸与原布局一致。打开包含旧 PHE 的项目，确认原横向显示改用 200×80 新素材，标准接口落在新图上下端口、既有线路仍连接并完成自动重路由，设备标识和指标位置可接受；再检查新插入 PHE、旋转、缩放、锚点编辑、保存/刷新及 JSON 往返。重点确认 PHE 宽度由 160 增至 200 后未与邻近设备产生不可接受的遮挡 |
| P1 | 待用户验收 | 验收自动拐角候选节点与起线 | 用户在编辑模式将指针移到普通子线的不同 90° 自动拐角，确认只在 hover 时显示节点圆点，直线、端点、普通交叉和拱桥不误提示。纯单击应只选择子线且撤销历史不变化；直接拖动应在松手后固化一个可选节点；双击应立即进入接线，可连接兼容锚点、母线、现有节点或子线。接线中按 `Esc`、落到非法目标或路由无解时不留下节点或线路；成功连接应一次撤销同时移除新支路和源节点。最后在共享主干拐角、自动重路由及复杂冷却管路中确认共享点只生成一个节点、未固化候选会随路由移动或消失，监控模式没有候选标记 |
| P1 | 待用户验收 | 验收统一节点与节点起线 | 用户在旧图纸中确认原途径点与分流点均只显示为“节点”，编辑态未 hover 时无常驻标记，hover 显示圆点与“双击从节点接线”。分别单击、追加、框选、拖动、方向键移动、混合移动/90° 旋转节点，并从路径节点和三连节点双击起线，连接兼容锚点、母线、另一网络节点或子线；确认新线默认双向，用户显式连回同网络其他节点时可建立闭环，但同点自连和重复边仍被拒绝。再检查节点同点合并，以及把端部子线拖出时节点保持用户实际拖到的 8px 网格位置、不自动插入前一格引出点且线路没有短回折线头；拖回起止锚点时临时节点被回收并恢复自动路径。重点复验 POD A“旁流旁滤设备”旁原 `1008→1000→1008` 短回折，确认重做拖出和拖回手势均无残留，而共享/分支节点保留。不兼容或无解时整次回滚并仅一条 Toast。删除二连节点应局部恢复自动布线，删除三连及以上节点移除相关线路且不恢复主管，一次撤销完整恢复。最后检查复制/Duplicate、跨图纸粘贴、保存/刷新、v20 历史、JSON 往返及监控动画保留节点和逻辑线路属性，分支动画只覆盖通往有效 Target 的物理区间 |
| P1 | 待用户验收 | 验收大图中键平移跟手性 | 用户在 POD A 低缩放下分别检查编辑与监控模式的短距离、连续长距离中键平移，确认图元、子线、母线、点阵和监控虚线始终同步且没有回弹、撕裂或明显卡顿；松开中键后视口位置正确，新增进入画面的对象正常出现，并确认纯平移仍不触发“未保存” |
| P1 | 待用户验收 | 验收子线单向通行约束 | 用户分别单选、多选不同方向的独立逻辑子线及选择线路网络，检查右侧方向一致时回显当前值、不一致时显示“多种方向”，批量选择明确方向后所有箭头一起更新且一次撤销全部恢复；确认与母线混选时不显示方向字段。再检查双向无箭头、单向箭头位于配置末端且缩放后尺寸稳定；进入监控模式检查顺箭头 Source→Target 路径有动画、逆箭头路径无动画，并确认方向冲突可被明确单向约束消除。最后检查复制/Duplicate、跨图纸复制、保存/刷新和 JSON 往返后方向保留，旧项目子线继续默认为双向 |
| P1 | 待用户验收 | 验收数值/文本运行指标与三级告警 | 用户为不同图元分别配置 1–5 项数值或文本指标，检查类型切换、文本候选状态增删及等级选择；确认数值单位按 `有功功率(kW)` 使用半角括号紧跟名称、读数列不重复单位，编辑模式以范围中间值或首个正常文本状态稳定预览且不刷新，设备名称、运行数据整体及全部指标名称与单位三个开关互不耦合。指标名称与单位应始终保持原样且无底色；正常数据透明且保持中性色，次要、重要、紧急数据分别使用黄、橙、红紧凑背景与黑色文字，并取消数据的深色描边。关闭名称与单位后确认读数或文本状态及异常数据背景保留、徽标自身宽度不变且随仅值标签重新对齐；最后检查不同缩放、相邻多项异常、监控模式 30 秒刷新、文本模拟正常优先、三类数值阈值，以及复制、粘贴、Duplicate、跨图纸复制和 JSON 保存后配置保留且副本模拟值独立 |
| P1 | 待用户验收 | 验收四区工作区与画布贴边 HUD | 用户检查页面只剩顶部栏、左侧导航/素材栏、中间画布和右侧属性栏四个一级区域；画布直接铺满中间区域且无外层 padding、边框、圆角、顶部/左侧标尺或鼠标坐标；返回、标题/路径、对象统计、编辑/播放、缩放和状态信息均贴近完整画布边缘，除按钮外无背景，并确认不遮挡常用图纸操作。再检查左栏右边缘外侧的左/右折线箭头按钮无背景和描边，在编辑/监控模式都能收起菜单并扩大画布，收起后可从画布左边缘展开 |
| P1 | 待用户验收 | 验收监控模式与 On/Off 图元状态/颜色 | 用户检查顶部 Tab、监控模式只读、滚轮缩放/中键平移/层级导航；分别检查 Switch、2WV、CV 在编辑属性面板和监控画布中的 On/Off 切换、刷新后恢复、关/开颜色独立配置与回显，以及空选总览按设备和状态分槽替换且不跨设备误改。保存、导出并重新导入项目，确认三类图元状态保持；切换状态本身不出现未保存提示且撤销几何编辑不会回滚状态。确认 2WV/CV 默认 Off、On/Off 素材与颜色同步切换，2WV On 后双向、CV On 后只允许入口→出口，Off 均阻断；再确认 Grid、Generator、Battery 均可作为 Source，算力 POD、动力 POD、FM 均可作为终止传播的 Target，Off Switch 两侧停流且 On 后按有效路径恢复，单向子线、方向冲突、播放/暂停和中性深灰虚线效果保持原规则；反馈视觉与业务问题进入修复清单 |
| P1 | 待用户验收 | 验收母线端点文字标签 | 用户检查母线默认无文字、填写后使用固定 14px 世界字号；右侧“标签颜色”默认 `#B6B8B4`，HEX 输入、取色预览和恢复默认均只影响当前母线标签，hover、拖动选中及监控模式不覆盖自定义色。水平母线可在起点/终点各自的上/下侧、垂直母线可在起点/终点各自的左/右侧显示，标签沿端点向母线内侧展开并保持 8px 轴向 padding 与 8px 法向间距，清空后隐藏。确认右侧开关只控制当前母线，关闭后保留文字、颜色、端点和侧位，再次开启可恢复；仅单选一条纯母线时可拖动切换四种方位，旋转后文字保持水平，复制、撤销/重做及保存后内容、显隐、颜色和方位不丢失 |
| P1 | 待用户验收 | 验收图元设备标识标签 | 用户检查标签默认显示，单选普通图元后右侧属性面板开关只控制当前图元且不影响其他图元或母线标签；显示时使用 10px 世界字号、与图元保持 2px 默认视觉间距及独立于主画布网格的 2px 精细避让步进，低缩放随画布缩小。对同一图元分别验证锚点未连线时不避让、接线后避让对应 24px 出线走廊、删除接线后恢复按无连接拓扑布局；确认只有单选后可拖动、旋转不改变文字方向或侧向、多选不可拖动，反馈问题进入修复清单 |
| P1 | 待用户验收 | 验收通用图元 | 用户检查素材库“通用”分类可插入灰色虚线框、默认 `#121316` 背景和框内白色设备标识；分别用右侧宽度、高度字段及画布右下角手柄改变长宽比例，确认两维独立吸附 8px 网格。分别修改、即时预览并恢复虚线框颜色和背景颜色，确认两者互不覆盖；复验虚线框颜色的空选全局同色替换。关闭“显示虚线框”后确认只隐藏描边，当前背景色、标识、锚点、指标和原边框颜色保留，重新开启后恢复。分别旋转 90°、180°、270°，确认短标识始终水平完整、长标识出现省略号且不溢出虚线框；也需检查浅色背景下白色标识的实际可读性。再配置数值/文本运行指标并切换名称与单位显隐，确认指标仍在四周显示且不重复框外设备名称；最后检查复制、撤销/重做、保存/刷新和 JSON 往返后尺寸、两项颜色、虚线显隐、标识与指标保留 |
| P1 | 待用户验收 | 验收电力母线与宿主节点 | 用户检查母线拖入、无描边小尺寸实心节点、节点 hover/单击/追加/框选、沿母线拖动与方向键、和图元/母线混合移动、双击起线及删除全部接线；确认垂直于母线的拖动被约束，同一母线同点接线复用一个节点，自由节点碰到母线节点后被吸收，不同母线节点或图元锚点宿主冲突会整次回滚。再检查复制/粘贴/Duplicate 只保留选区内部节点与接线，缩短母线停在最外侧节点并仅一条 Toast，母线移动/旋转携带节点，未连接穿越仍为拱桥，保存/刷新和撤销/重做保持位置 |
| P1 | 待用户验收 | 验收图元接线与自动路由 | 用户检查已用锚点再次接线、同锚点多线、自然共同段与自动无标记分叉、实际最短长度、等长时少转折、锚点端点即时切向转弯，以及锚点方向只在同分路径中提供稳定软偏好；确认线路不进入端点图元内部，也不因端点边界豁免穿过重叠的其他图元。同长同转折时拐点应稳定居中且不因起点侧或目标侧已有主干而偏移；再检查独立支路/共同主干选择删除、六类型兼容、正交避障、母线节点保持保存位置且路由不私自滑移，以及不同网络拱桥，反馈问题进入修复清单 |
| P1 | 待用户验收 | 验收图元拖动与起线跟随性能 | 用户在已有多支路线路的图纸中检查单选/多选图元移动、缩放、旋转、母线拉伸和从锚点起线的跟手程度，重点检查悬停终点锚点；松手后的最终线路继续正确避障，并保持最短、少转折和中点拐点规则 |
| P1 | 待用户验收 | 增强非连接交叉拱桥 | 用户检查孤立及密集交叉的 8×4px 真圆弧、12px 局部画布色底衬和缩放下辨识度；反馈问题进入修复清单 |

## 下一步建议

| 优先级 | 状态 | 任务 | 完成定义 |
| --- | --- | --- | --- |
| P2 | 待办 | 整理用户后续导入的图元素材 | 由用户完成视觉分类/验收；项目侧记录文件、用途、授权、格式和可配置属性 |
| P2 | 待办 | 扩展性能基线与兼容矩阵 | 已建立当前最大真实归档的路由与 DOM 基线；待用户确认目标设备、浏览器、典型/上限规模后，补充多档硬件帧时间、内存、GPU 与长时监控动画测试 |
| P2 | 待办 | 定义监控数据与动画性能边界 | SVG 静态层与 WebGL 批量运行态层职责已确定；继续明确刷新频率、单图最大图元/线路数、同时动画数量、目标设备、帧时间与降级阈值 |
| P2 | 待办 | 扩展正式监控数据源适配接口 | 冷却侧已用 `CoolingRuntimeProvider` 与确定性 `MockCoolingRuntimeProvider` 验证替换边界；后续把 Switch/2WV/CV On/Off、电力源、指标和其他冷却运行态收敛到正式遥测适配层，并定义失联、过期、质量码与刷新策略 |
| P2 | 待办 | 完成 RuntimeBundle 资产交付与发布策略 | 在宿主资产 URL 解析器和可选 SVG/PNG 内嵌之间确定方案，提供兼容升级与首屏体积边界；再把 runtime core / React 入口发布为工作区包或私有包，并在目标 React 项目验证素材、Router、播放和真实 Provider |
| P2 | 待办 | 按运行态门控 Generator 与 Battery Source | 定义 Generator 运行/供电及 Battery 放电、充电、待机状态的数据字段与优先级；只有运行中的 Generator 和放电中的 Battery 加入动画 Source，停机、充电或待机不驱动供电流向，并与未来真实遥测适配接口共用状态模型 |

## 阻塞项

暂无。

## 已完成

| 日期 | 任务 | 结果 |
| --- | --- | --- |
| 2026-09-08 | 替换 CHWP/CWP 及共用关闭素材 | 运行图分别更新，关闭时共用 `CHWP CWP Off.svg`，尺寸均保持 80×128；待用户检查监控开关泵的素材切换。 |
| 2026-09-08 | 替换 UPS-group 与 CDU | 使用最新附件，均登记为 SVG 原始 48×48；CDU 旧尺寸实例与锚点同步迁移。待用户检查新素材与旧图纸接线效果。 |
| 2026-09-08 | 隐藏监控开关状态标签 | Switch 标签不再重复显示正常开启/关闭文字，保留设备标识、其他数据与异常；待用户验收。 |
| 2026-09-08 | 允许母线两端轴向出线 | 水平和垂直母线的两个端点均支持沿轴向向外连接子线，同时保留垂直方向；正式路由、预览和接入候选同步。待用户检查实际端点起线与落线效果。 |
| 2026-09-07 | 替换 TMU、FM、UPS-group、Cabinet 素材 | 四份用户附件已逐字节保存为正式 SVG。TMU 由 PNG 切换为 SVG，并按附件根画布改为 48×48；旧 PNG 资产路径载入后同步到 SVG，既有 48×64、72×96、64×96 默认尺寸迁移到 48×48，主动缩放实例保持；FM、UPS-group、Cabinet 分别保持 64×64、48×48、48×48 逻辑尺寸及现有稳定键。图元实例、锚点、接线和运行语义不变，Schema 保持 v37。目录与迁移定向 76 项、全量 488 项测试通过，1 项跳过；设计系统/AIDC 边界、TypeScript、生产构建和补丁格式通过，仅保留既有大包提示。 |
| 2026-09-07 | 更新冷却链路四类默认配色 | 一次回路冷/热、二次回路冷/热依次更新为 `#FCBC00`、`#FB7800`、`#1BA4FF`、`#00B387`；业务默认值、主题源文件和生成 CSS 同步，覆盖编辑/监控线路、锚点与颜色预设，显式实例颜色保留。22 项颜色与线路表现定向测试、主题生成一致性、设计系统/AIDC 边界及补丁格式检查通过；待用户验收实际配色。 |
| 2026-09-07 | 用最新 SVG 替换 CWP、CHWP、CT、PHE | 四份用户附件已逐字节保存为正式 SVG，并移除对应 PNG；CWP、CHWP、PHE 的目录和新建尺寸更新为 80×128，CT 更新为 96×96，CPD PNG 保持不变。旧 200×80 / 160×160 项目载入时按最近等比网格档位迁移实例并保持中心附近位置，边缘锚点按原边和比例映射，ID、类型、角色与接线引用保留，重复解析幂等。Schema 保持 v37。附件哈希一致，定向目录/迁移 76 项及全量 487 项测试通过、1 项跳过；`WuHu AIDC 0904` 实际存档迁移与路由性能回归通过，设计系统/AIDC 边界、TypeScript、生产构建和补丁格式通过，仅保留既有大包提示，待用户视觉与实际图纸验收。 |
| 2026-09-04 | 消除“TMU到FM 01”图元移动后的主线程路由停顿 | 真实 `WuHu AIDC 0904` 审计确认该图为 52 图元、4 个大型网络、357 条逻辑边、201 个手动节点和 34 条历史失效边；旧提交会整网同步重路由并重试全部历史失效边。现把正式增量失效粒度降到逻辑边，同网未受影响边直接复用；历史失效边按局部包围区进入 Worker 恢复候选，主线程只校验本次新产生的手动约束冲突，子线段调整也只校验相关逻辑边。专用基准增加 dirty edge、预览、合并和提交整理分段计时：同机同步校验由约 0.75 秒降至约 34ms，全部 52 个接线图元扫描 P95 约 40ms、最慢约 46ms，预览约 3ms、合并和提交整理各约 2ms。全量 486 项测试通过、1 项跳过，性能基准、TypeScript、设计系统/AIDC 边界、生产构建及补丁格式通过，仅保留既有大包提示，待用户实际操作验收。 |
| 2026-09-04 | 取消所有图元接线的强制法向引出段 | 网格锚点现在可从端点直接向外或沿边缘两侧切向接线，只禁止进入图元内部；缩放或旋转产生的非网格锚点使用最短正交过渡。端点自身边界可按接线路径放行，但其他图元仍是硬障碍，重叠阻挡不会被误解除；母线法向接入保持不变。接线预览、正式路由、图元变换和端部子线拖动已统一，拖动不再自动插入一格引出节点。Schema v37 与持久化拓扑不变。路由/增量/手动节点定向 62 项、全量 485 项测试通过，1 项跳过；设计系统/AIDC 边界、TypeScript、生产构建和补丁格式通过，仅保留既有大包提示，待用户实际图纸验收。 |
| 2026-09-04 | 将 MP 改为单锚点管路测量传感器 | 用用户附件替换 MP SVG，目录固定一个底部通用冷却“测量点”锚点，锚点编辑器不再允许把 MP 改回多端或泵阀设备；新建实例自动配置温度、压力、流量三项指标。Schema v37 可把旧双锚点 MP 前后管网合并到唯一测量节点，保留线路属性、图元配置和既有指标。图元目录、迁移和 `WuHu AIDC 0904` 46 个 MP 的真实存档审计已通过；全量 484 项测试通过、1 项跳过，TypeScript、设计系统/AIDC 边界、生产构建和补丁格式通过，仅保留既有大包提示。 |
| 2026-09-04 | 补齐 TMU 回路 2 对称中间环路的动画 | 真实“TMU到FM 01”中间横向连通段属于冷 2→热 2 的闭合循环，但完全对称的等阻力求解会给出零净流量，导致结构上有效的中间环路静止。现仅对位于有效有向起终点走廊且不是无向桥边的零流量区间赋予最低可视循环流量；开放死端和桥边仍不补动画。归档回归明确验证 4 台 FM 的 8 条冷热接管和 4 条中间环路连接均活动，同时底部连接度为 1 的开放长支路保持静态。定向 18 项、全量 471 项测试通过，1 项跳过；TypeScript、设计系统契约、生产构建和补丁格式通过，仅保留既有大包提示。 |
| 2026-09-03 | 纠正 TMU 双回路误串并建立回路 2 外部边界驱动 | 用户确认 TMU 的“二次回路冷/热 1”是一条回路，“二次回路冷/热 2”是另一条回路，且冷 2 是起点、热 2 是终点。旧求解器先把四口两两互连，随后又误把冷 2→热 2 做成 TMU 内部短接，均无法表达经过 FM 的外部环路。现按编号隔离两套回路；回路 1 保持内部双向，回路 2 不生成内部水路，而以冷 2 注入、热 2 回收，仅在外部有向路径完整时驱动固定视觉流量。此前错误的下游源头仲裁继续保持撤回。最小回归验证 TMU 冷 2→FM 冷→FM 热→TMU 热 2，真实 `WuHu AIDC 0831` 归档回归确认“TMU到FM 01”中 4 台 FM 的 8 条冷热接管全部进入活动流量集合。全量 471 项测试通过、1 项跳过，TypeScript、设计系统契约、生产构建和补丁格式通过，仅保留既有大包提示，等待用户实际图纸验收。 |
| 2026-09-03 | 建立 AIDC 合入与 UI 单一真源准备层 | 明确 AIDC 为基础 UI 唯一真源，接线图业务的 10 处组件引用统一改经未来包名 `@aidc/ui`，TypeScript/Vite 暂时映射本地实现；新增 AIDC `024e391` 提交、主题/清单版本和 14 个核心文件哈希锁，`check:aidc-ui` 防止业务绕过包入口，`audit:aidc-ui` 可检测相邻 AIDC 仓库漂移。RuntimeBundleViewer 新增 presentation 透传，输出指标、设备态、图元和水泵派生流量供 AIDC 同款右侧检查器消费；全量 469 项测试通过、1 项跳过，设计系统契约、AIDC 跨仓库审计、TypeScript、生产构建和补丁格式通过，仅保留既有大包提示。共享包提取、五项能力上游化、资产 resolver 和 AIDC 实际接入仍在进行中。 |
| 2026-09-03 | 修复 TMU 到 FM 冷热端动画反向 | 根因是冷却求解器只保留一次/二次回路族，普通设备内部冷热端统一双向连通，FM 因而可以从热端反向穿到冷端。现对稳定键 `fm` 建立同回路冷端→热端的单向内部水路，冷源能够穿过 FM 后由热端流出，热源不能倒灌冷端；其他设备规则和项目数据不变。新增正向通过与反向阻断回归；全量 469 项测试通过、1 项跳过，TypeScript、设计系统契约、生产构建和补丁格式通过，仅保留既有大包提示。当前开发页热更新后动画路径由 397 条收敛到 343 条且无控制台错误，等待用户视觉验收。 |
| 2026-09-03 | 修正 UPS 数据颜色与数据面板下拉溢出 | UPS 只读线路画布的正常读数改用纯白 `--panel-ink`；同时修正复合标签行被通用告警选择器误判后覆盖成黑色的问题，非正常值仍沿用告警底色和黑字。数据面板时间范围下拉改为通过共享 `SelectField` Recipe Token 收紧到 76×24px，并为最长的“24小时”保留足够内容宽度。新增无截图 Playwright 计算样式与几何回归，验证白色 SVG 读数、文字实测宽度、控件尺寸和浮层无横向溢出；3 条设备/数据面板流程、全量 468 项测试通过，1 项跳过，设计系统契约、TypeScript、生产构建和补丁格式通过，仅保留既有大包提示。 |
| 2026-09-03 | 保存 CWP/CHWP 水泵启停和输出功率 | Schema v35 为每个 `pump` 图元实例增加可选启停与 `0–100%` 输出字段；编辑与监控右侧面板现复用同一水泵控制组件，写入同一实例快照并标记 dirty，保存、JSON、复制和 RuntimeBundle v2 均保留，旧存档默认运行/100%，显式宿主覆盖优先。编辑态不显示派生流量，指标、趋势和动画仍为运行时数据；领域、Store、App 保存交互、双模式属性面板及 Bundle 往返回归已补齐。定向 66 项、全量 468 项测试通过，1 项跳过；设计系统契约、TypeScript、生产构建和补丁格式通过，待用户操作验收。 |
| 2026-09-03 | 将 UPS 线路图改为画布式数据驱动渲染 | 新增固定视窗、无交互的 `ReadOnlyDiagramScene`，把 UPS 内部 7 个图元、11 个端点/节点和 7 条电气边建模为 `DiagramElement` / `ConnectionNetwork`；真实素材、普通线路、8px 正交路由、监控静态流动与播放态移动虚线分别复用主画布原语。主画布原私有线路基线同步抽到 `DiagramScenePrimitives`，编辑行为、命中层和冷却选择底层保持不变。新增只读复合标签行，同一相位的电压、电流保持独立值但使用图元配置标签的字体、描边、行高、列对齐和告警语法，不修改全局指标 Schema；图元和标签按合成边界整体居中，路由坐标继续保持 8px 网格。小场景输出 0 条无效路由、5 条绿色活动支路，不修改全局素材锚点，也不引入选择、用户视口或持久化。全量 466 项测试通过、1 项跳过；设计系统契约、TypeScript、生产构建、补丁格式及 2 条无截图 Playwright 设备面板流程通过，仅保留既有大包提示，等待用户视觉验收。 |
| 2026-09-03 | 联动设备趋势周期与时间轴 | 设备面板的日/周/月不再只改变采样点，而是统一驱动 MLVR/Generator 的线电压、线电流、有功功率及 UPS 负载率趋势：日覆盖最近 24 小时并显示 `HH:mm`，周覆盖最近 7 天、月覆盖最近 30 天并显示 `MM-DD`。组件回归验证四类图表共享跨度配置，Playwright 真实验证 Switch 配置进入面板后日/周/月横轴文本和跨度属性同步变化；全量测试、设计系统契约、TypeScript、生产构建和补丁格式检查通过。 |
| 2026-09-03 | 修正设备面板趋势周期控件样式与横向溢出 | 根因是面板局部样式只设置了容器宽度，通用 `SegmentedControl` 的每项 52px 最小宽度和 28px 高度仍生效，三项实际宽度至少约 164px、总高 32px。现按 AIDC 配方改为固定 112×28、2px 容器内边距、三项等分、24px 子项、黑色容器和 4px 选中态，并允许趋势标题工具栏在窄宽度换行。新增无截图 Playwright 回归，真实验证编辑配置、监控打开、控件/按钮尺寸及页面 `scrollWidth`；该回归、全量 462 项单元测试（1 项跳过）、主题、设计系统契约、TypeScript、生产构建和补丁格式检查均通过。 |
| 2026-09-03 | 修复 Switch“监控点击行为”选择无法生效 | 根因是画布提交层的 `elementsEqual` 未比较 Schema v34 新增的 `monitorInteraction`，下拉变更虽然触发了属性写回，却被误判为无变化并由受控值还原。现已把 `monitorInteraction` 和同类持久化字段 `onOffState` 纳入相等判断，因此选择“显示设备面板”会进入项目提交、撤销/重做及持久化链路。新增字段级回归；全量 462 项测试通过、1 项跳过，主题、设计系统契约、TypeScript、生产构建及补丁格式检查通过。 |
| 2026-09-03 | 新增并精修监控设备详情与标签数据面板 | Schema v34 为 Switch 增加实例级监控点击行为，旧实例默认保持 On/Off 控制；配置为设备面板的 Switch、Generator 和单体 UPS 使用 AIDC/Figma 参考的 500px 详情。第二轮按 Figma 100% 画布及 AIDC 源码收紧为连续三列摘要、452×160 紫/蓝/青面积趋势、31px 四列告警表、452×360 UPS 线路画布和 360×233 数据浮层。UPS 使用现有电力图元与正交接线，`ups-group` 下探不变；图元/子线标签趋势和设备当前值均不写项目，右侧面板只在单选图元时挂载，空选状态完全隐藏。全量 461 项测试通过、1 项跳过，主题 173 个 Token、设计系统契约、TypeScript、生产构建及 `git diff --check` 通过；按项目约定未做本地应用截图验收，已列入用户视觉验收。 |
| 2026-09-03 | 为 Supply 与 Load 增加运行/待机状态及独立颜色 | 两类图元登记为默认运行的 `running-standby` 双态，编辑单选/多选与监控右侧面板均使用运行/待机文案并分别配置两套颜色；旧实例缺失状态时按运行恢复。实现复用既有双态项目快照、IndexedDB 兼容表和 RuntimeBundle v2，不增加 Schema 字段；待机进入演示运行方式后有功功率与负载率归零且不告警。Switch、2WV、CV 的关/开界面及语义不变，Supply/Load 仍不自动成为 Source/Target 或拓扑门控。目录、属性面板、监控切换、项目/Bundle 往返、颜色汇总和待机指标定向回归通过；全量 456 项测试通过、1 项跳过，TypeScript 通过，等待用户操作与视觉验收。 |
| 2026-09-03 | 统一为 AIDC `97ecddf` 监控模式组件与风格 | 根据用户澄清，编辑与监控业务不再使用两套外壳。左侧统一为 280px / 28px 监控树，使用参考搜索、展开/园区/楼宇/POD/叶节点 SVG 和 40%/76%/100% 层级；编辑管理能力进入黑色 28px 工具组，素材格移除常驻描边。右侧统一为 360px / 24px、90% 面板、30px 模糊、无阴影，并新增复用的检查器标题 Pattern。返回改用参考 `back.svg`，路径容器透明，命令/缩放改为 6px 无阴影监控浮层。删除分模式重复尺寸、编辑树/路径背景和面板阴影 Token，生成 169 个 Token / 185 个 Recipe。全量 452 项单元/组件测试通过、1 项跳过；设计系统契约、TypeScript、生产构建、补丁检查及 1 条无截图布局/计算样式端到端回归通过。等待用户视觉验收。 |
| 2026-09-03 | 新增 Supply、Load、AC-AC Converter、Rectifier、Inverter 电力图元 | 五份 48×48 SVG 已按附件原样加入正式素材目录，并使用独立稳定键登记为电力图元。Supply 与 Load 已由最初单色普通图元进一步扩展为运行/待机双态和两套独立颜色，其他三个保持固定多色素材；五项均补齐确定性演示设备档案，版本升为 `aidc-demo-profile-2026-09-r4`。五类设备无预置锚点、自动 Source/Target 或父子绑定，Schema v33 与 RuntimeBundle v2 格式不变。正式素材 SHA-256 与附件逐一一致；目录、尺寸、状态/改色入口、档案覆盖和 RuntimeBundle 版本定向回归通过；此前全量 452 项单元/组件测试通过、1 项跳过，设计系统契约、类型、补丁格式和生产构建通过，仅保留既有大包提示。等待用户视觉与操作验收。 |
| 2026-09-03 | 校准 UPS L 拟真数据、电流动画与告警色 | 根因是 UPS L 的父级已换成 `ups-group`，但供电下探只匹配旧 `ups`，子图因而被误判无外部电源。现左右 UPS 子图同时识别新旧资产键；`battery-group` 拆分为独立锂电档案，SOC 正常区间为 `82–98%`且不被默认故障或离线拉低。备电时间现由同一 SOC 快照按满电 `2.0 h` 额定值派生，三级异常通过可用容量折减表达，并在运行时至少保留 1 位小数；原项目指标精度不回写。告警 Domain Token 调整为 `#FFF200` / `#FF8A00` / `#FF1744`，黑色数据文字和无底色标题不变。档案版本升为 `aidc-demo-profile-2026-09-r3`，Schema v33 与 RuntimeBundle v2 格式不变。当前存档的父级供电、电流拓扑与 5 组 SOC 无截图数据核验通过；448 项单元/组件测试通过、1 项跳过，设计系统契约、类型、主题、补丁格式和生产构建通过；等待用户视觉验收。 |
| 2026-09-03 | 二次校正 AIDC 沉浸式面板与左树 | 在画布铺满、64px 顶栏和 4px 贴边 HUD 布局不变的前提下，删除首轮统一渐变、常驻内描边和编辑树左侧强调线。编辑树改用 `rgba(29,31,34,.95)`、32px 整行深灰状态、10px 名称、反相选中图标与末端拖动提示；监控树改用 `rgba(28,28,28,.9)`、无阴影、28px 低对比行状态和无边框本地搜索。路径与右侧检查器改用各自单色 Token，运行信息块、属性开关和只读元数据移除外框，实际表单字段和焦点/错误状态保留。设计 Token 为 180 个、Recipe 185 个；448 项测试通过、1 项跳过，设计系统契约、类型、生产构建、补丁检查和 2 条无截图计算样式/布局回归通过；等待用户视觉验收。 |
| 2026-09-03 | 迁移 AIDC `97ecddf` 设计系统 | 建立 `[data-aidc-theme]` 作用域内的 typed Token/Recipe 真源与确定性生成链，按 Foundation、Semantic、Domain、Component 分层，共生成 138 个 Token 和 185 个 Recipe；补齐 Button、IconButton、TextField、NumericField、SelectField、Switch、SegmentedControl、Tab、StatusTag 九个视觉原语及 Pressable/TabList 语义辅助，并让 Topbar、树/素材入口、画布 HUD/返回、右侧属性与监控面板复用共享组件。新增组件清单、契约检查和 `DESIGN_SYSTEM_PORT.md`；432 项单元/组件测试通过、1 项跳过，类型、主题契约和生产构建通过，相关无截图端到端流程通过。完整编辑器端到端套件仍有 3 项位于本次 UI 组件迁移链路之外的失败：监控画布测试继续读取已卸载的编辑画布 testid、UPS 标题选择器同时匹配 UPS-group，以及真实场景性能阈值波动；视觉验收按项目约定由用户完成。 |
| 2026-09-03 | 让标签运行数据列只由最长数据决定 | 图元与节点间子线的指标布局改为分别测量名称列和数据列；所有数值/文本状态行共享最长数据宽度、统一右对齐和异常背景宽度，超长设备标识、子线标题或指标名称不再参与数据列宽计算。子线起始/结束锚定方向和母线 14px 四态标题布局保持不变。新增图元与子线长名称、长短数据交叉回归；全量 428 项测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过。Schema v33、RuntimeBundle v1、指标配置、告警和持久化不变，等待用户视觉验收 |
| 2026-09-03 | 完成共享场景/路由目录迁移并切换产品内监控画布 | 连接几何、三类标签布局、素材目录和场景基础实现全部迁入 `src/scene/`，正式路由调度、Worker 与 Hook 迁入 `src/runtime/`；`src/runtime/`、`src/scene/`、`src/monitoring/` 产品代码对 `editor/` 的直接导入归零，旧编辑路径保留兼容重导出。当前 `App` 编辑模式继续使用 `DiagramCanvas`，监控模式已改用 `DiagramMonitorCanvas`，缩放、播放、运行态选择与下探回归通过。全量 426 项测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过；仅保留既有大包提示。Schema v33、RuntimeBundle v1、存档和持久化不变；剩余资产交付、包发布与目标项目接入作为独立后续任务 |
| 2026-09-03 | 迁移第一批场景基础模块 | 将二维几何/视口、点阵密度、滚轮/触控板分类、线路外观、跨线排序、颜色工具和通用图元计算迁入 `src/scene/`，WebGL 点阵组件 `GridSurface` 也改由场景层实现；监控、共享场景和产品代码直接使用新入口，旧 `editor/` 文件保留兼容重导出，编辑几何包装层只继续承载选择平移。模块守卫禁止纯基础模块反向导入 `editor/`、React 或 Three，GridSurface 作为 WebGL 表现层例外。全量 424 项测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过；Schema v33、RuntimeBundle v1、存档、画面与交互不变，后续继续迁移连接几何、标签、素材和正式路由 Hook |
| 2026-09-03 | 完成共享线路场景模型与母线节点原语 | 新增纯数据 `connectionScene`，让编辑画布与跨项目监控画布统一线路渲染分组、最终带桥显示路径、冷却管壳、静态监控线路、桥下动画排除、冷却节点和连接锚点派生；新增不依赖 React/Three 的 `flowPresentation`，承载静态流动常量、类型、压暗颜色和分组，`FlowAnimationLayer` 兼容重导出原符号。母线节点也收敛到 `DiagramScenePrimitives`，编辑颜色预览 ref 和节点交互环继续由编辑侧注入。新增 4 项纯场景模型回归并扩展共享 DOM 与双入口模块守卫；全量 424 项测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过。Schema v33、RuntimeBundle v1、存档与可见交互不变；下一步迁移 `editor/` 纯模块并完善资产及发布边界 |
| 2026-09-03 | 建立第一批编辑/监控共享场景原语 | 新增无状态 `DiagramScenePrimitives`，让 `DiagramCanvas` 与 `DiagramMonitorCanvas` 共用普通图片/通用框图元主体、图元/母线/子线标签与指标行、母线基线、Symbol 颜色滤镜、冷却管内阴影滤镜、监控静态线路、桥面双层遮罩、冷却管壳和线路方向箭头；编辑锚点及标签拖动使用 children/稳定 ref 注入，监控下探与运行态选择仍由画布负责。新增共享 DOM、交互可选性、滤镜/静态线语义和双入口模块守卫回归；全量 420 项测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过。Schema v33 与 RuntimeBundle v1 不变，后续继续收敛线路组装派生模型、母线 tap 等剩余静态表现并迁移纯模块目录 |
| 2026-09-03 | 从跨项目入口移除整套编辑画布依赖 | 新增约 1.7 千行、职责单一的 `DiagramMonitorCanvas`，直接消费运行时视图/上下文并保留正式 Worker 路由、视口裁剪、SVG 图元/母线/线路/标签、WebGL 点阵与流动动画、下探/运行对象选择，以及中键、空格+左键、触控板平移/捏合和滚轮缩放。`DiagramRuntimeViewer`、`RuntimeBundleViewer` 和 `runtime/react.ts` 不再导入 8173 行 `DiagramCanvas`、编辑命令或写回回调，ref 收敛为三个缩放方法；新增模块边界守卫和只读场景 DOM/导航/选择/缩放回归。全量 416 项单元/组件测试通过、1 项跳过，类型、主题和生产构建通过；当前 App 保持既有画布，Schema v33 与 RuntimeBundle v1 不变。后续需收敛双路径共享表现原语，并迁移仍位于 `editor/` 的无状态几何模块 |
| 2026-09-03 | 建立 RuntimeBundle v1 与独立 React 宿主入口 | 新增独立格式版本的 `aidc-diagram-runtime` 包：从一张或多张入口图纸保留下级子树、必要祖先、双线路结构根、业务画布内容、已使用资产、On/Off 快照和冻结下探映射，并校验来源项目、入口、来源图元、目标图纸及名称一致性；项目扩展、编辑选择、历史、dirty、会话相机、随机读数与渲染对象不进入包。提供创建、序列化、解析、下载、单图恢复 API，以及可受控/非受控导航、运行态覆盖和 Provider 注入的 `RuntimeBundleViewer`；核心与 React 入口继续分离。新增 3 项运行时包/宿主契约测试；全量 414 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过。项目 Schema 保持 v33；资产 v1 仍随运行时代码部署，独立只读场景与资产解析/内嵌已列入下一步 |
| 2026-09-03 | 建立可迁移的监控运行时边界 | 新增 `src/runtime/` 公共入口：完整项目可纯派生单图运行时视图与下探关系，运行状态统一为上下文，指标 Provider 支持快照、订阅或轮询，冷却 Provider 可注入，电力/冷却拓扑与泵流量指标覆盖从画布抽为 Hook，并提供监控只读 React 查看器。现有 `App` 与 `DiagramCanvas` 已反向消费核心接口，兼容途径点派生也移出编辑器专属实现；视觉、交互、默认 30 秒模拟、泵阀/外部供电、项目 Schema v33 与持久化均不变。新增运行时切片、Provider 注入、阀门状态适配和 React 查看器契约回归；411 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过。架构、目标、决策、设计系统和产品约束已同步至 KD-127 与 `MONITOR_RUNTIME_ARCHITECTURE.md` |
| 2026-09-03 | 主画布改为纯黑并保留点状网格 | 编辑与监控画布的 WebGL 着色器及 CSS 回退层统一使用 `#000000` 背景；自适应单级点阵的 8px 最小世界单位、点样式、可见间隔和原点保持不变，不影响全局主题或通用图元背景。406 项单元/组件测试通过、1 项跳过；纯黑计算样式与 WebGL 点阵层无截图端到端断言、类型、主题、补丁格式和生产构建通过，等待用户视觉验收 |
| 2026-09-03 | 优化“TMU到FM 01”大型图纸节点拖动 | 根因是节点拖动每帧都会为全部图元、母线、连接网络和途径点创建新集合，并在每条预览边内重复建立整网节点索引；由此使约 307 条子线的圆角、拱桥、冷却管体和 React/SVG 缓存一起失效。现改为未变化集合与网络结构共享，预览节点索引每个网络只建立一次，未变化的交叉点、分叉集合、线路显示路径和冷却外管逐边复用；只有被拖节点的直接邻接边更新。真实 `WuHu AIDC 0831` 的“TMU到FM 01”基准为 4 个网络、307 条边，80 帧连续预览平均 `1.304ms/帧`，每帧仅 3 条邻接边改变；全套 406 项通过、1 项跳过，类型、主题和生产构建通过，等待用户实际拖动和视觉验收 |
| 2026-09-02 | 冷却管路端点可配置为水流源头 | 所有冷却图纸的子线右侧属性新增“水流源头”，支持起点端、终点端、清除、纯子线批量和混合值；复用既有端点身份，因此节点分割/合并、反转、复制、撤销/重做和保存往返保持一致。每个有效源头按 `100 m³/h` 基准流向其他可达开放自由端，经等阻力模型分流，并与水泵贡献叠加；子线方向、普通阀门、CV 与内部回路继续门控，无出口时停流。Schema 保持 v33。定向 102 项通过；全套 403 项在分组及超时用例复跑中通过、1 项跳过，类型、主题、补丁格式和生产构建通过。并行全量首次有 2 项既有 App 交互用例触发 5 秒超时、单独均通过；单 Worker 另遇 1 次 Vitest worker 启动超时，对应 2 项单独通过。等待用户视觉与实际图纸验收 |
| 2026-09-02 | 监控模式从 POD A 的 CDU-01 下探 TMU到FM 01 | 冷却 CDU 现按设备标识中的编号匹配同一 POD 下直接“TMU到FM”子图，前导零不影响匹配，显式 `drillDownDiagramId` 仍优先；非 CDU、编号不一致和编辑模式行为不受影响。现有 `WuHu AIDC 0831` 归档的 `CDU-01` → `TMU到FM 01` 无截图端到端点击流程通过；403 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过，等待用户操作验收 |
| 2026-09-02 | 新增 UPS-group 电力图元 | 用户附件已原样保存为 48×48 `UPS-group.svg`，显示名 UPS-group、稳定键 `ups-group`、分类为电力。它与既有 UPS 并列为独立普通图元，首版无预置锚点、实例改色、On/Off 或子图下探关系；旧项目通过安装资产同步自动补入，既有 UPS 元数据、实例、接线和监控语义不变，Schema 保持 v33。正式素材 SHA-256 与附件一致；397 项单元/组件测试通过、1 项跳过，UPS-group 菜单登记与 48×48 插入端到端断言、类型、主题、补丁格式和生产构建通过；按项目约定等待用户视觉验收 |
| 2026-09-02 | 支持母线标签文字颜色配置 | Schema v33 为母线增加可选六位 HEX `labelColor`；单选母线的右侧属性面板新增“标签颜色”，默认 `#B6B8B4`，支持应用内取色、即时画布预览和恢复默认。自定义色在 hover、标签拖动选中及监控视图中保持，只影响标签文字；固定 14px 字号、四态方位、母线本体与拓扑不变。396 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过；按项目约定等待用户视觉验收 |
| 2026-09-02 | 放大母线标签并支持端点垂直两侧布局 | Schema v32 为母线增加可选 `labelSide`；标签使用 14px 世界字号，按最近端点和指针所在法向侧形成四态布局，水平映射上/下、垂直映射左/右，并像子线标签一样沿端点向线内展开。旧项目缺省使用负侧；标签方位继续随预览、历史、复制和项目保存持久化，不产生端子或拓扑语义。393 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过；按项目约定等待用户视觉验收 |
| 2026-09-02 | 修复 Cabinet 双路供电被误判为主备切换 | 根因是 `cabinet-device` 虽为 Target，但构图仍把同一机柜的不同锚点内部连通，较短进线可穿过机柜到达另一输入端，导致较长的真实进线被最短路裁掉。现将 Cabinet 统一为顶层与设备子图的计算机柜终点，每个锚点独立回溯且不允许内部穿越；两路可达时同时动画，单路可达时机柜仍带电且只显示有效一路，失电母线进线不会因另一锚点带电被补亮。当前分接单元 L 非视觉拓扑核验 40 台 Cabinet、每台 2 条进线全部进入活动集合，失败 0；新增双路、单路失电和顶层终点 3 项回归。392 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过，等待用户视觉复验 |
| 2026-09-02 | 子线节点端落母线自动接入 | 统一节点碰撞提交现会把本次移动、手动改线或粘贴产生且精确落到唯一母线范围的真实电力节点原位转换为 `busbar-tap`，保留节点 ID、连接度及全部邻接子线，并立即合并同母线既有网络；同点已有 tap 继续吸收复用。开路、二连和分流节点均覆盖；派生折点与无节点被动穿越不接入，多母线交点歧义阻止提交。新增 4 项定向测试；全量 389 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过，等待用户操作验收 |
| 2026-09-02 | 补齐显式供电子图母线全长与并行设备进线 | 根因是严格最短路径只保留入口到目标所需的母线区间，并在同一 UPS 的双输入中选择离右侧入口近 16px 的容量线，导致母线尾段及 TR04-UOS01/02 静态。现对配置显式入口且参与有效路径的自动方向母线补齐全部 tap 区间和两端物理尾段；同一已带电设备的其他明确“母线→设备”单向入线也进入动画。双向支路、Battery-group、普通环路、关断与方向冲突不扩展，人工母线方向和无显式入口旧规则不变。385 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过；按项目约定未做视觉验收，等待用户复验 UPS L |
| 2026-09-02 | 支持图元模板锚点跨四边拖动 | 锚点拖动从“锁定初始边”改为在四条边全部合法非角点 8px 位置中选择最近点，一次手势可直接切换边缘并自动更新外向方向；等距时优先当前边以减少角点附近抖动，重复位置仍拒绝。稳定 ID、名称、类型和入口/出口角色随移动保留，整次拖动即时应用并继续进入既有撤销历史；Schema 与持久化结构不变。383 项单元/组件测试通过、1 项跳过；CHWP 锚点由上边 `8,0` 跨到右边 `200,40` 的无截图端到端流程、类型、主题、补丁格式和生产构建通过，等待用户实际操作验收 |
| 2026-09-02 | 增加电力子图显式外部供电入口 | Schema v31 为电力子图的逻辑子线增加可选起点端/终点端入口；右侧属性面板支持单选、批量混合值与清除，并在通行方向阻断入口时提示。父图带电且存在显式入口时，拓扑只从这些入口端组成的同一外部边界注入，停用母线和自由边界自动 Source；父图失电停流，没有显式入口的 v1–v30 项目继续兼容旧规则。节点分割只保留实际端点段，反转翻转端点，二连节点合并不把内部入口迁移到外端；383 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过，视觉与 UPS L 实际入口配置等待用户验收 |
| 2026-09-02 | 增加母线人工监控电流方向 | Schema v30 为母线增加可选起点→终点/终点→起点方向；右侧属性面板支持单选上下文文案、纯母线批量和混合值，自动状态删除显式字段。人工方向约束母线传播，并在没有显式子线入口时选择设备子图兼容外部入口；只有实际参与有效供电路径时才按指定方向输出完整物理长度，父图断电不制造电流。画布不显示静态方向箭头或入口圆点；编辑 hover/选中显示入口/出口文字，监控模式只由电流动画表达方向；旋转保持世界方向。临时页面无截图 DOM 验证方向配置和无供电压暗状态；379 项单元/组件测试通过、1 项跳过，类型、主题和生产构建通过，等待用户在分接单元 L 配置实际四条母线并视觉验收 |
| 2026-09-02 | 修复分接单元 L 母线无电流 | 根因是四条外部母线入口此前被当成四个独立电源，成对环形联络允许不同入口反向穿越，同一母线段因此同时获得正反方向并被冲突保护隐藏。现将同一子图的全部外部入口归并为一个多节点供电边界并执行一次多源最短路；真实 Grid、Generator、Battery 仍保持独立 Source。当前图纸 DOM 验证四条主母线进入活动流动集合，未参与最短供电路径的右侧冗余联络保持压暗并处于播放状态。376 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过；等待用户视觉复验移动虚线方向与连续性 |
| 2026-09-02 | 修复 UPS L 后半段主线无电流 | 根因是第三至第五台 UPS 的 6 个输入端点只在坐标上落到母线，仍保存为独立自由节点；此前外部供电兜底又把它们误当成局部 Source，造成 UPS 输出有动画而母线后半段停流。载入兼容现将编辑器生成、精确落在同图唯一电力母线且不是内部途径点的自由端点转换为母线节点，合并同母线网络并去重同位置节点；多母线交点保持不猜测。当前存档 UPS L 从 17 个网络规范为 11 个，DOM 验证入口母线从第一台至第五台连续处于活动色并可播放，电池支路继续压暗。375 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过；等待用户视觉复验移动虚线连续性 |
| 2026-09-02 | 实现 POD A 监控下探与电力子图动画 | 监控画布增加只读图元下探：当前 POD A 的 4 个 Tap-off Unit A 映射“分接单元 L”，左侧 4 台 UPS 映射“UPS L”，并保留显式 `drillDownDiagramId` 优先入口。设备子图从父图关联设备实际带电状态派生外部供电资格；母线首个接入点及明确单向自由输入端作为 Source，Cabinet 及明确单向自由输出端作为 Target，继续服从子线方向。真实当前项目无截图 DOM 验证得到 8 个正确下探入口；分接单元 L 的 82 个主显示线路组、UPS L 的 13 个主显示线路组进入活动色，未参与主供电路径的分支保持压暗。374 项单元/组件测试通过、1 项跳过，主题、类型和生产构建通过；等待用户视觉验收移动虚线、父图断电联动及左右设备组映射 |
| 2026-09-02 | 图元标签只避让实际已连接锚点 | 画布从当前显示连接拓扑派生每个图元实际参与边端点的锚点集合，标签布局只为这些锚点生成 24px 出线走廊；资产中存在但未接线的锚点不再改变默认候选方位，连接预览、提交、删除和图纸切换引起拓扑变化时自动重新布局。手动标签方位、其他图元与标签避让、通用图元框内标识、Schema、dirty 和历史保持不变。366 项单元/组件测试通过、1 项跳过；已连接/未连接锚点并列的定向无截图端到端流程、主题、类型、补丁格式和生产构建通过，等待用户实际图纸验收 |
| 2026-09-02 | 框选/多选节点复制直接相邻子线段 | Copy/Paste 与 Duplicate 的剪贴板拓扑由“仅两端对象均选中”扩展为并入显式节点的一跳相邻边；未选图元/母线端转换为原坐标自由端点，避免外部对象和下一跳支路级联进入副本。节点单独选择可启用复制，节点/线路联合边界参与同图错位和跨图居中，粘贴后恢复显式节点选择；节点剪切保持明确不支持。属性、逻辑组和指标配置保留，新节点、边、网络、逻辑组及指标均使用独立 ID，Schema 保持 v29。366 项单元/组件测试通过、1 项跳过；节点复制定向无截图端到端流程、主题、类型、补丁格式和生产构建通过，等待用户实际框选与复杂分支验收 |
| 2026-09-02 | 新增 Battery-group 电力图元 | 用户附件已原样保存为 48×48 `Battery-group.svg`，显示名 Battery-group、稳定键 `battery-group`、分类为电力。它与既有 Battery 并列为独立普通图元，首版无预置锚点、实例改色、On/Off 或电力 Source 角色；旧项目通过安装资产同步自动补入，既有 Battery 元数据、实例、接线和监控语义不变，Schema 保持 v29。正式素材 SHA-256 与附件一致；357 项单元/组件测试通过、1 项跳过，Battery-group 定向无截图端到端流程、主题、类型、补丁格式和生产构建通过；等待用户视觉与实际图纸验收 |
| 2026-09-01 | 图纸切换时自动适配全部内容 | 所有通过 `diagramId` 或文档 epoch 发生的图纸进入路径统一执行一次会话视窗适配：以旋转后图元外框、完整母线和全部自由线路节点计算稳定业务边界，保留 48px 屏幕留白并居中，小图最多 100%，空图恢复 100% 标准视窗；超大图允许低于常规 25% 手动缩放下限，后续放大可平滑回到常规区间。适配不依赖异步路由结果，因此不会在 Worker 返回后再次跳动，也不写项目、dirty、历史、IndexedDB 或 JSON。356 项单元/组件测试通过、1 项跳过；自动适配与原纯会话视窗 2 条无截图端到端流程、主题、类型和生产构建通过，等待用户实际图纸验收 |
| 2026-09-01 | 新增 Tap-off Unit 子图元并开放实例改色 | 用户附件已原样保存为 32×32 电力图元 `Tap-off Unit.svg`，显示名 Tap-off Unit、稳定键 `tap-off-unit`，并在素材目录中紧随 Tap-off Unit A/B。首版将“子图元”定义为业务语义关系：实例可独立插入、移动、旋转、缩放、复制、配置标识、指标和单一颜色，不自动嵌入、跟随或继承 A/B；默认无预置锚点，可通过既有锚点编辑器独立配置。改色复用右侧应用内 HEX 控件、轻量预览、恢复默认、撤销/重做、全局颜色总览和 `properties.color` 持久化。旧项目载入后自动补充该素材，未新增项目字段，Schema 保持 v29。正式素材 SHA-256 与附件一致；356 项单元/组件测试通过、1 项跳过，改色定向端到端、主题、类型、补丁格式和生产构建通过。当前完整端到端套件 27/30，另 3 项失败均为真实场景性能基线断言（主线程时长、路由数量阈值、视窗渲染次数），不经过 Tap-off Unit 颜色链路，留待对应并发改动收敛；等待用户视觉与实际图纸验收 |
| 2026-09-01 | 新增 Cabinet 并将原 Cabinet A/B 改名为 Tap-off Unit A/B | 用户附件已原样保存为 48×48 电力图元 `Cabinet.svg`，显示名 Cabinet、稳定键 `cabinet-device`，首版无预置锚点。原 `Cabinet A.svg` / `Cabinet B.svg` 保留文件与历史键 `cabinet` / `cabinet-b`，显示名更新为 Tap-off Unit A/B。旧默认实例名 `Cabinet`、`Cabinet A`、`Cabinet B` 定向更新，自定义名称、资产锚点和既有接线保持；新 Cabinet 自动进入旧项目且不继承分接单元配置，重复载入幂等。Schema 保持 v29。正式素材 SHA-256 与附件一致；353 项单元/组件测试通过、1 项跳过，29 条无截图端到端流程、主题、类型、补丁格式和生产构建通过；等待用户视觉与实际旧项目验收 |
| 2026-09-01 | 增加图纸树行内级联删除 | 编辑模式的非根图纸行在 hover、键盘聚焦或触控环境下显示 24px 行尾删除图标，点击即删除目标及完整子树；根图和监控模式不渲染入口。领域层拒绝根图删除并返回稳定子树 ID，Store 在单次变更中同步清理图元、母线和连接网络；当前图纸被删时回到目标原上级，其他分支删除保持当前画布与选择。删除更新 `updatedAt`、dirty、本地保存与 JSON，Schema 保持 v29。348 项单元/组件测试通过、1 项跳过，类型、主题、生产构建与定向无截图端到端流程通过；Impeccable 检查未新增问题，仍仅报告两项本轮未触碰的既有 advisory，等待用户操作与视觉验收 |
| 2026-09-01 | 为水泵增加停机状态素材 | 用户提供的 1000×403 RGBA PNG 已原样保存为辅助资源 `PumpOff.png`，不进入素材库、不新增 `assetKey`。监控模式中任意配置为 `pump` 的冷却图元停止后切换该资源，运行及编辑状态恢复设备原素材；替换继续使用实例原画布框和等比居中规则，不改变尺寸、旋转、锚点、接线、标签、指标、命中或水力门控。当时泵状态保持会话态、Schema v29；该存储边界已由 2026-09-03 的 KD-142 / Schema v35 取代。正式素材 SHA-256 与附件一致；345 项单元/组件测试通过、1 项跳过，29 条无截图端到端流程、主题、类型、补丁格式和生产构建通过；等待用户视觉验收不同泵型与旋转缩放观感 |
| 2026-09-01 | 替换 CDU 并调整为 192×96 横版图元 | 正式 `CDU.svg` 与用户附件逐字节一致，SVG 根画布和素材目录逻辑尺寸均为 192×96。新插入 CDU 使用新尺寸；保存资产仍为旧 96×96 的项目在载入时一次性迁移全部 CDU 实例与锚点，分别保留横纵缩放、旋转后的显示外接框位置、锚点 ID/类型/方向和既有接线引用，重复解析幂等。未新增项目字段，当前 Schema 保持 v29。344 项单元/组件测试通过、1 项跳过，28 条无截图端到端流程、主题、类型、补丁格式和生产构建通过；等待用户视觉与实际旧项目验收 |
| 2026-09-01 | 增加整条子线跨线层级 | Schema 升至 v29，节点间 `ConnectionEdge` 可显式保存下层或上层，缺省自动。纯子线单选、多选在右侧批量设置三态；最终顺序为上层、自动、下层，同层继续按冷却主要/辅助及稳定创建顺序。节点拆分继承，二连合并按上层、下层、自动保留人工意图，复制、历史和项目往返保留字段。编辑 SVG、监控静态与 WebGL 动画使用同一顺序；下穿动画在桥面范围切断，电力母线保持固定底层。修改层级只重派生交叉、不重跑 A*，不改变拓扑和流量。342 项单元/组件测试通过、1 项跳过，27 条无截图端到端流程、主题、类型、补丁格式和生产构建通过；等待用户视觉与实际图纸验收 |
| 2026-09-01 | 新增并更新 TMU 冷却图元 | TMU 曾使用用户最新提供的 640×960 RGBA PNG，后于 2026-09-07 替换为 SVG；当前按 SVG 原始画布登记为 48×48。稳定键与冷却分类保持不变，继续使用普通图片图元能力；首版无预置锚点、无冷却设备角色、无改色及 On/Off 状态。旧目录中恰好保持 48×64、72×96 或此前 64×96 默认尺寸的实例迁移为 48×48，主动缩放实例保持原尺寸，边缘锚点按比例映射到新边界并吸附 8px 网格；迁移重复执行幂等，Schema 保持 v28。正式素材 SHA-256 与最新附件一致；333 项单元/组件测试通过、1 项跳过，26 条无截图端到端流程、主题、类型和生产构建通过；等待用户视觉验收 |
| 2026-09-01 | 恢复节点分段后的冷却管路圆角 | 根因是 Schema v26 后普通拐角可由两条节点间边组成，而原算法只识别单边内部折点。显示层现把自由二连节点的四分之一圆弧拆为两条邻边各自持有的半弧，并在圆弧中点连续拼合；主要/辅助管体、内芯、选择/命中、监控静态区间和 WebGL 动画共用同一采样几何。短段自动收紧，直线二连、图元锚点、三向/四向分叉与桥旁节点不处理；数据边界、方向、流量、Schema 和历史不变。圆角派生只处理 overscan 可见边并按几何签名复用未变化路径，真实场景拖动全套回归中最大主线程任务 68ms、完整手势约 205ms。330 项单元/组件测试通过、1 项跳过，25 条无截图端到端流程、类型、主题、补丁格式、Impeccable 机械检查和生产构建通过；等待用户在实际冷却图纸验收圆角观感 |
| 2026-09-01 | 为子线标签增加运行指标配置 | Schema 升至 v28，节点间 `ConnectionEdge` 可保存与图元相同的最多 5 项数值/文本指标、运行数据显隐和指标名称与单位显隐。恰好单选一条子线时复用现有指标编辑器；编辑稳定预览、监控立即生成与全图 30 秒刷新、三级告警和只显示读数/状态的布局均与图元一致。标签名称与指标三类显隐互不耦合；节点拆分只在标签端点段保留整组显示数据，二连收缩继续保留，复制线路时重建指标 ID。v1–v27 默认外观不变。328 项单元/组件测试通过、1 项跳过，25 条无截图端到端流程、主题、类型、补丁格式、Impeccable 机械检查与生产构建通过；等待用户在实际电力/冷却图纸验收布局和告警观感 |
| 2026-09-01 | 增加持续式线路绘制工具 | 画布 HUD 新增显式折线工具；开启后可从空白 8px 网格点、锚点、母线、节点或子线起笔，连续单击空白点逐段绘制。落到已有拓扑或显式闭环时只结束当前线，工具继续等待下一次起笔；`Esc` 或再次点击按钮退出，单独起点不保存。一次工具会话合并为一个撤销步骤。Schema v27 起允许连接度为 1 的自由端点和显式同网络闭环，当前 v28 继续兼容；同点自连、重复边和退化自环仍拒绝。328 项单元/组件测试通过、1 项跳过，25 条无截图端到端流程、主题、类型和生产构建通过；等待用户视觉与实际图纸操作验收 |
| 2026-09-01 | 节点参与混合移动并将子线按持久化节点分段 | Schema 升至 v26，v1–v25 的 `source → routeNodeIds → target` 载入后展开为相邻节点边；新拖段、固化拐角和子线接入在提交时同样分段。各段继承颜色、方向和冷却主要/辅助角色，标签只保留在配置端点段，`logicalConnectionId` 仅用于恢复自动布线时收缩同组二连节点。共享区点击改为选择实际参与边，不再扩大到整个网络。节点、图元、母线和兼容点复用统一平移函数，拖动已选对象或方向键移动保持相对位置；分段后的子线拖回既有自动路径时只收缩同组二连节点，不误删分叉。323 项单元/组件测试通过、1 项跳过，23 条无截图端到端流程、主题、类型与生产构建通过；等待用户实际图纸验收 |
| 2026-09-01 | 为通用图元增加实例背景颜色 | 单选通用图元后，右侧属性面板新增“背景颜色”应用内 HEX 控件，默认 `#121316`，支持连续轻量预览、确认提交和恢复默认；背景与 `properties.color` 虚线框颜色及 `genericBorderVisible` 显隐独立，隐藏边框仍保留背景。显式背景保存到 `properties.genericBackgroundColor`，六位 HEX 校验、复制与 JSON/IndexedDB/历史沿用既有实例属性链路，Schema 保持 v25。背景、颜色面板、复制和定向解析相关 43 项测试、主题检查、Vite 生产打包及通用图元无截图端到端流程通过；工作区现有 `routeNodeIds` 类型与 v20 迁移断言不一致仍阻断全量类型、全量测试及包含类型检查的完整构建，与本功能无关。等待用户视觉验收背景色与白色标识的对比度 |
| 2026-08-31 | 进一步收细冷却辅助线路 | 辅助管体由 `6px` 收至 `4px`，保留 `1px` 回路色内芯以兼顾低缩放可读性；辅助拱桥世界空间遮罩同步由 `14px` 收至 `12px`。主要线路继续为 `10px / 2px` 与 `18px` 遮罩，辅助选择高亮仍为独立 `6px`，固定屏幕和世界空间命中范围不变。样式值继续由设计 Token 与领域常量统一管理，不修改 Schema、路由、拓扑、流量、方向或速度。316 项测试通过、1 项跳过，类型、主题、生产构建与补丁格式检查通过；等待用户视觉验收 |
| 2026-08-31 | 修复拱桥实际上下绘制顺序 | 拱桥归属数据原本正确，但 SVG 仍按网络创建顺序整组绘制，后置辅助网络会盖回主线桥面遮罩。现把冷却网络按边角色拆为辅助、主要稳定批次，编辑态始终辅助先绘、主要后绘；监控静态层在辅助内芯后补绘仅属于主要跨辅助的桥面遮罩，再绘制主要内芯。电力母线仍先于普通子线，子线跨母线规则不变。新增相反创建顺序的稳定角色排序回归；316 项测试通过、1 项跳过，类型、主题与生产构建通过，等待用户复验实际上下层视觉 |
| 2026-08-31 | 修复节点越网格导致本地历史无法打开 | 根因是混合选区旋转只对图元和母线吸附网格，自由节点与兼容途径点绕半网格中心旋转后可能保存为 `8n + 4`，随后被正确的 8px Schema 校验拒绝。旋转路径现对两类点统一吸附；载入器在正式校验前只修复编辑器生成的 `route-waypoint-*`、`connection-node-*` 有限坐标，保留节点 ID、边引用与拓扑，其他非法数据继续报错。Schema 保持 v25。新增半网格旋转及用户报错 ID 的缓存兼容回归；316 项测试通过、1 项跳过，类型、主题与生产构建通过，等待用户重开 17:06 存档复验 |
| 2026-08-31 | 固定拱桥业务层级 | 普通电力子线未连接穿越母线时始终由子线绘制拱桥；不同冷却网络的主要线路与辅助线路交叉时始终由主要线路绘制拱桥，同级子线继续按稳定创建顺序。修复异步路由快照误删 `busbar:*` 交叉记录；修改主辅级别只重新派生交叉集合并复用原路由几何，不触发 A*。编辑、监控和动画继续共用最终带桥路径，Schema 保持 v25。314 项单元/组件测试通过、1 项跳过，类型、主题、生产构建和补丁格式检查通过；等待用户视觉验收 |
| 2026-08-31 | 将图元指标名称与数值列间距收紧为 0px | 保留多指标统一的名称列与右对齐数值列，只把决定组合标签宽度的最宽一行列间距从 8px 调整为 0px；短名称行继续服从同一数值列。新增稳定布局常量和最宽行精确间距断言，不修改标签字号、行高、告警徽标、四侧避让、Schema 或持久化。310 项单元/组件测试通过、1 项跳过，类型、主题和生产构建通过，指标编辑/监控无截图端到端流程通过；等待用户视觉验收 0px 密度 |
| 2026-08-31 | 增强冷却管路选择态并增加主要/辅助线路 | Schema 升至 v25，冷却逻辑子线新增可选主要/辅助视觉角色；纯冷却单选、多选和网络选择可在右侧批量设置，混合值显示“多种级别”，旧项目缺省主要。辅助线路最终调整为 `20%` 原回路/实例色与 `80% #70736F` 混合，并使用 `6px / 1px` 管体/内芯；主要线路保持 `10px / 2px`。编辑与监控静态层一致，动画覆盖跟随对应内芯宽度，但原始颜色、拓扑、方向、流量和速度算法不变；共享区间和节点收缩均由主要线路优先。编辑选择态使用不透明 `6px #B7B8B5` 中性轨道。308 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式和生产构建通过，等待用户视觉与项目往返验收 |
| 2026-08-31 | 增加图纸层级树管理 | 编辑模式可从当前上下文新增下一层图纸或设备同级图，并立即行内命名；当前图可重命名。非根图纸支持原生拖拽，目标中部接收为子图，上下边缘调整同级顺序；`Alt+方向键` 提供排序、提升和缩进。冷却/电力线路标题及含子图节点现可独立收起/展开，普通 `←/→` 提供键盘入口，当前路径按需自动展开；该会话状态不写项目或 dirty，监控模式同样可用。纯领域变换按稳定 ID 重建深度优先顺序并归一化子树层级，拒绝根图移动、跨专业、循环、第二根和超过四层。管理操作复用现有 IndexedDB/JSON 持久化，Schema 不变。新增领域、Store、组件和浏览器回归；310 项单元/组件测试通过、1 项跳过，类型、主题和生产构建通过，22 条无截图端到端流程全部通过。Impeccable 机械扫描未发现本次层级树新增问题，仍报告两项本轮未触碰的既有样式 advisory；等待用户操作与视觉验收 |
| 2026-08-29 | 将运行指标异常数据改为告警色背景与黑色文字 | 次要、重要、紧急数值或文本状态分别使用既有黄、橙、红告警色紧凑背景，数据文字统一为黑色并取消深色描边；指标名称与单位保留原中性文字、画布描边和透明背景，正常数据继续透明中性，设备名称也不纳入底色。隐藏名称与单位时，异常数据徽标自身尺寸不变，只随仅值标签重新对齐。标签缓存现将告警等级纳入判等，文字不变而等级变化时也会立即刷新底色。未修改 Schema、告警判断、模拟或持久化。新增告警前景 Token、数据边界单测与正常/异常 DOM 断言；294 项单元/组件测试通过、1 项跳过，类型、主题、补丁格式、生产构建及 1 条无截图定向端到端流程通过，等待用户视觉验收 |
| 2026-08-29 | 修复 macOS 普通鼠标滚轮被误判为触控板平移 | 根因是初版只用标准 `deltaMode/deltaX/deltaY` 的步进大小区分设备，部分 macOS 鼠标同样产生像素级小步进，因而进入双指平移分支。分类器现在优先识别 Chromium/WebKit 保留的 `120` 基准离散滚轮 tick，并按 `devicePixelRatio` 兼容 Retina；只有缺少该信号时才回退到连续增量启发式。新增小 `deltaY`、1×/2× 像素比及触控板反例单测，浏览器回归同时断言缩放发生、`wheel` 被取消且页面 `scrollY` 不变。294 项单元/组件测试通过、1 项跳过，主题、类型、补丁格式、生产构建及 Impeccable 机械检查通过，20 条无截图浏览器流程通过；等待用户以当前鼠标及真实触控板复验 |
| 2026-08-28 | 增加 macOS 触控板双指平移与捏合缩放 | 画布改用原生非被动 `wheel` 入口：`Ctrl+wheel` 作为指针中心连续捏合，连续像素级/横向滚动作为双指平移，离散大步进或非像素滚轮继续缩放；非捏合手势按 `160ms` 会话锁定判定，横向信号可消除初始歧义。双指平移累计亚像素位移并复用命令式 SVG/WebGL latest-wins 热路径，手势尾部才提交 React；捏合使用有界指数增量并保持指针下世界坐标不漂移。新增分类单测、编辑/监控视口与保存状态回归，以及真实 POD A 连续 12 次触控板事件期间 React 渲染次数不变、尾部仅一次同步的性能断言。293 项单元/组件测试通过、1 项跳过，Impeccable 机械检查零问题，主题、类型、补丁格式和生产构建通过，20 条无截图浏览器流程通过；等待 macOS Chrome/Safari 真实触控板验收方向、灵敏度与惯性手感 |
| 2026-08-28 | 增加空格+鼠标左键临时抓手平移 | 编辑与监控主画布在指针悬停或画布聚焦时，按住空格显示 `grab`，再以左键拖动进入 `grabbing`；手势在捕获阶段优先于图元、母线、子线、接线和框选，并与中键共用命令式 SVG/WebGL 会话视口、逐帧 latest-wins 与 120px 裁剪同步热路径。松开空格、窗口失焦、指针结束或取消均正确收尾；输入框、下拉和可编辑文本保留空格，纯平移不写项目、dirty、历史或持久化。同步修正一条仍假定监控模式无属性面板/直接点图切换 Switch 的过期端到端断言。289 项单元/组件测试通过、1 项跳过，主题、类型、补丁格式和生产构建通过，20 条无截图浏览器流程通过；Impeccable 机械扫描只报告两项本次未触碰的既有 advisory，等待用户以真实触控板验收手感 |
| 2026-08-27 | 用同名 PNG 替换五个冷却图元 SVG | CHWP、CWP、CPD、CT、PHE 已切换到用户新增的透明 PNG，前四者保持原逻辑尺寸，PHE 改为 200×80 横版。新增旧 PHE 一次性迁移：实例旋转减 90°并保持显示包围框左上位置，四个标准锚点对齐新图上下接口，ID、类型和接线引用保留；重复载入幂等。旧同名 SVG 已从活动素材目录移除，Schema 保持 v24。289 项测试通过、1 项跳过，主题检查、类型检查和生产构建通过；125 图元、67 网络、171 逻辑边的真实归档以当前 PNG 目录迁移并完成全量/增量路由，无无效边。等待用户视觉与真实项目线路验收 |
| 2026-08-26 | 将无流量线路改为原色压黑 50% 并消除圆角/边缘叠色 | 初版用 `#000000`、50% Normal Blending 覆盖原线，在制冷圆角采样三角带的防缝搭接处发生重复混合，产生鳞片和棋盘格；改为不透明 WebGL 最终色后，抗锯齿边缘仍会混入下方完整原色内芯。最终实现按 sRGB 通道预先计算“原色 × 50%”的 HEX 暗色，监控静态层按流动/静止物理区间直接绘制原色或暗色 SVG，并隐藏原普通子线、母线和制冷内芯；WebGL 只保留 `#303238`、50% 的移动虚线。圆角、分叉和共享段不重复变黑，放大时暗色边缘也没有原色底层可透。新增最终色值、静态分组和原始线宽回归；全量 287 项测试通过、1 项跳过，主题、类型、补丁格式与生产构建通过，等待用户视觉验收 |
| 2026-08-26 | 修复制冷动画缩放后与内芯粗细不一致 | 根因是制冷静态内芯使用世界空间 `2px`，而复用的 WebGL 覆盖仍按电力固定屏幕空间 `2px` 绘制。现为流动路径增加显式世界空间宽度，Shader 按 zoom 计算实际屏幕宽度；制冷移动虚线随内芯缩放，后续无流动最终暗色改由同一世界空间 `2px` SVG 内芯直接绘制。电力普通子线动画覆盖 `2px` 与母线动画覆盖 `4.5px` 固定屏幕宽度保持不变，静态原始宽度分别恢复为 `1.5px` 与 `4px`。新增线宽坐标空间回归；全量验证结果见后续完成项 |
| 2026-08-26 | 统一电路与水路无流动线路样式 | 监控模式现从普通子线、电力母线与制冷最终显示几何中扣除全部有效流动物理区间，一条逻辑边可只置暗部分区间，反向引用和共享段不会重复叠色。初版静态覆盖曾与动画虚线共用 `#303238`、50%，随后先由“原色压黑 50%”修订取代，再由当日最终渲染修正改为 SVG 直接分区着色；WebGL 不再绘制静止线。播放暂停只移除移动虚线，不改变流动资格。新增几何回归，全量验证结果见后续完成项 |
| 2026-08-26 | 实现制冷视觉流量与泵阀运行面板 | 监控模式开放可控设备单选和右侧运行面板，水泵可启停并调节 `0–100%` 输出功率，阀门可开关；点击不再直接改变工况，图纸结构仍只读。固定 `100 m³/h` 基准，通过稀疏等阻力网络求解有符号视觉流量，支持等价并联分配、汇流、多泵净流向、CV/子线单向和逻辑边节点分段阻力稳定；流量驱动动画速度并覆盖泵“流量/flow”指标，不模拟压力。新增组件与拓扑回归，全量 280 项测试通过、1 项跳过；主题、类型、补丁格式和生产构建通过，等待用户真实图纸视觉与操作验收 |
| 2026-08-26 | 将 CV 改为只允许单向通过的止回阀 | Schema 升至 v24，新增 `check-valve` 资产角色。CV 开启时拓扑只建立入口→出口内部有向边，关闭时完全阻断；2WV/普通阀门仍保持开启后双向。新项目 CV 自带顶部入口与底部出口，v23 及更早标准 CV 自动迁移并保留锚点 ID、图元与接线；非标准配置不猜测。锚点编辑器支持止回阀入口/出口配置，定向拓扑、运行态、迁移、素材与组件测试已覆盖 |
| 2026-08-26 | 将 On/Off 图元状态纳入存档和导出 | Schema 升至 v23，Switch、2WV、CV 在 `DiagramElement.onOffState` 中保存 Off/On；本地保存、JSON 导出/导入、图元复制及已保存项目的即时状态镜像保持一致。旧文档仅在字段缺失时读取兼容 `monitorSwitchStates` 表，显式快照优先；状态切换不更新 `updatedAt`、dirty 或撤销历史。单元/组件、类型、生产构建与无截图端到端导出验证通过，等待用户实际存档往返验收 |
| 2026-08-26 | 将 2WV、CV 改为与 Switch 一致的 On/Off 图元 | 用户新增的 `2WV_Off/On.svg`、`CV_Off/On.svg` 已分别归并到稳定键 `2-wv`、`cv`，素材库默认显示 Off。编辑属性面板与监控画布共享可自动保存的 On/Off 状态和分离关/开颜色；旧单色载入时复制到两槽。空选颜色总览按设备与状态隔离，避免相同颜色跨 Switch/2WV/CV 替换。2WV/CV 配置为制冷阀门后以同一状态门控水流，其他非双状态泵阀会话态保持。266 项单元/组件测试通过、1 项跳过；四份 SVG XML、主题、类型、补丁格式和生产构建通过，等待用户视觉与操作验收 |
| 2026-08-26 | 修复真实 POD A 六台水泵无制冷动画 | 根因包含两层：直接接线兼容被误用于设备内部，切断 CT/PHE 同回路冷热端；修正后，首版全量简单路径枚举又在并联图中组合爆炸并使 6 台泵全部 `path-search-limit`。现将设备内部连通改为一次/二次回路族隔离，并用逐物理区间的简单见证路径替代全量枚举。新增 CT/PHE、跨侧隔离和 14 层并联回归；真实“冷却链路 / 园区总图 / 1号楼 / POD A”非截图验证为 6/6 台有效泵、播放后 61 条显示路径进入 WebGL 流动层。262 项测试通过、1 项跳过，主题、类型、Impeccable 机械检查、补丁格式及生产构建通过；剩余方向冲突区间按既定规则保持静态，视觉效果由用户验收 |
| 2026-08-25 | 实现制冷闭合回路模拟与首版动画 | Schema 升至 v22；锚点编辑器支持普通设备/水泵/阀门资产角色及水泵唯一入口/出口，旧 v21 项目不猜测角色。新增确定性 `CoolingRuntimeProvider`，默认泵运行、`50%` 流量、阀门打开，监控画布可点击泵阀切换会话状态且不写项目。闭合回路求解覆盖类型兼容、单向约束、阀门门控、并行路径/多泵并集、开路与方向冲突；监控双层管体沿圆角和 `16 × 8px` 拱桥叠加独立 `cooling` 样式移动虚线，流量缩放速度。259 项单元/组件测试通过、1 项跳过，主题、类型、补丁格式及生产构建通过；等待用户视觉与真实图纸操作验收 |
| 2026-08-25 | 确认制冷动画首版视觉方案 | 首版不制作流光、液滴或粒子效果；监控模式在 `2px` 回路色内芯上复用电路的深色 `6px / 6px` 移动虚线和 WebGL 批处理骨架，保留独立制冷样式入口。制冷仍按水泵出口到同一水泵入口的有效闭合回路计算，不复用电力 Source→Target 最短路径；详细边界记录为 KD-081 |
| 2026-08-25 | 为制冷普通弯头增加圆角并保留分叉直角 | 编辑模式制冷管路普通 90° 拐角使用 `8px` 真圆弧，短线段按两侧长度一半自动收紧。完整网络按坐标上的唯一物理方向数派生分叉：二方向弯头含二连节点和共享重合线路仍圆角，三/四方向分叉保持直角；紧邻拱桥处也保持直角。下层合并管体、上层内芯、选择/命中路径和接线预览共用显示几何，电力、监控、路由和保存数据不变。247 项单元/组件测试通过、1 项跳过，主题、类型和生产构建通过；等待用户视觉验收 |
| 2026-08-25 | 收细制冷下层并调整端帽与拱桥遮罩 | 编辑模式制冷下层由 `12px` 改为 `10px`，端帽由圆头改为平头；图元锚点端显示内缩由 `6px` 改为 `0`，使平头直接止于锚点中心，接线预览同步。制冷 `16 × 8px` 拱桥的世界空间遮罩从 `15px` 加大为独立 `18px` Token，透明命中继续保持固定屏幕 `20px` 和世界空间 `16px`。上层 `2px` 内芯、电力线路、监控模式和持久化数据不变。243 项单元/组件测试通过、1 项跳过，主题、类型和生产构建通过；等待用户视觉验收 |
| 2026-08-25 | 微调制冷内芯与拱桥尺寸 | 编辑模式制冷管路的回路色上层由 `4px` 收细为 `2px`；制冷拱桥通过独立显示半径由默认 `8 × 4px` 放大为 `16 × 8px`，外管与内芯共用同一桥接几何。电力线路、监控模式、`12px` 外管、命中和持久化数据不变。243 项单元/组件测试通过、1 项跳过，主题、类型和生产构建通过；等待用户视觉验收 |
| 2026-08-25 | 支持隐藏通用图元虚线框 | 单选通用图元后，右侧属性面板新增默认开启的“显示虚线框”实例开关。关闭时只将专用矩形描边透明度设为零，近黑背景、框内设备标识、锚点、运行指标、图元命中区域和边框颜色配置保持；重新开启恢复原颜色。设置复用 `properties.genericBorderVisible`，随现有历史、复制和项目持久化链路保存，Schema 保持 v21。238 项单元/组件测试通过、1 项跳过，通用图元无截图端到端显隐流程、Impeccable 机械检查、主题、类型和生产构建通过；等待用户视觉与操作验收 |
| 2026-08-25 | 修复通用图元新建锚点无法连接 | 根因是锚点默认类型只识别“冷却”分类，新增的“通用”分类被兜底成“电路”，在冷却图纸中因此被兼容性检查禁用。现改为冷却/电力专属分类保持原规则，通用等中性分类按当前图纸系统选择 `cooling-general` 或 `electrical`；已有明确类型不自动改写。新增单元测试及自由宽高、旋转后实际起线落线的无截图端到端回归。237 项单元/组件测试通过、1 项跳过；20 条无截图端到端流程中 19 条整套通过，既有自动拐角 hover 用例受整套运行指针时序影响失败但单独复跑通过；主题、类型和生产构建通过。修复前已有锚点需在编辑器中手动切换类型 |
| 2026-08-25 | 新增自由尺寸通用图元 | 素材库新增稳定键 `generic` 的“通用图元”，画布以默认 `#777777` 可改色虚线框、近黑背景和框内白色设备标识专用渲染；属性面板分别编辑吸附 8px 网格的宽高，右下角手柄按本地坐标自由改变比例。框与锚点随 90° 旋转，标识保持水平并同时使用省略与旋转内框裁剪；框外标签只保留运行指标，继续支持指标名称与单位总开关。复用既有字段，Schema 保持 v21。236 项单元/组件测试通过、1 项跳过，19 条无截图端到端流程、主题、类型和生产构建通过；等待用户视觉与操作验收 |
| 2026-08-25 | 修复既有无解手动线路阻止无关图元移动 | 定位到 POD A 设备标识“Bug”的 MP 没有接线，但与另一台已接线 MP 重叠并遮挡其底部锚点，使带两个手动节点的 `connection-edge-a226…` 处于无解。无解边没有派生几何，原增量影响走廊无法在 Bug 移开后把该网络标脏；变换预检又把旧无解边重复算成本次冲突。现有任意几何变化都会重试既有无解网络，预检只统计本次由有效转为无解的手动约束；移开 Bug 后图元正常提交且线路恢复，旧无解线路也不再阻止无关对象移动。新增增量路由单测与真实手势端到端回归。230 项单元/组件测试通过、1 项跳过，18 条无截图端到端流程、主题、类型和生产构建通过；用户项目页已恢复到冷却 POD A 的 43 图元、33 网络状态 |
| 2026-08-24 | 实现自动拐角候选节点与起线 | 普通子线正式正交路由的内部 90° 拐角可在编辑 hover 时按需派生候选节点，纯点击仍只选择子线。候选点可直接拖动物化路径节点，或双击以延迟源节点开始接线；连接兼容锚点或另一条子线时，源节点与新逻辑边在同一事务固化，`Esc`、未完成和失败路径零残留。同一网络且共享实际转向的逻辑边复用一个节点，已有真实端点优先；不新增 Schema、保存字段或常驻拐角 DOM。229 项单元/组件测试通过、1 项跳过，17 条无截图端到端流程、主题、类型和生产构建通过；等待用户视觉与复杂管路操作验收 |
| 2026-08-24 | 修复子线拖出/拖回锚点时的折返线头 | 第一层修复统一碰撞事务的锚点吸收与纯节点移动提交；按用户原图复查后进一步定位到“旁流旁滤设备”旁实际路径为 `…1008→1000→1008…`，属于手动端点落在锚点投影后，路由器为满足锚点方向生成的非零长度反向折返。端部子线拖出时，手动节点现沿原锚点方向向线路内部收进一个网格，形成正常引出段而不越过拐点；拖回时删除本次手动段节点，并在每轮重路由后迭代回收导致线路立即反向的独占路径节点，直至恢复自动路径。逻辑边端点、共享节点与分支节点受保护。新增锚点引出点单元回归，并把双锚点端到端流程改为整段拖出/拖回且精确断言无回折路径；真实 MP 旋转锚点及 8px 旧短回折回归继续通过。224 项单元/组件测试通过、1 项跳过，16 条无截图端到端流程、主题、类型和生产构建通过；等待用户在原图重做拖出/拖回手势复验 |
| 2026-08-24 | 实现统一节点与节点起线 | Schema 升至 v21，原图纸级途径点和 `junction` 统一迁移为网络内 `node`，逻辑边用有序 `routeNodeIds` 保持手动路径且不拆散颜色、标签、显隐或单向属性。节点支持单击选择、拖动/方向键/混合变换及双击起线，可连接兼容锚点、母线、另一网络节点或子线；同网络闭环拒绝。同点兼容节点自动合并；二连节点删除时收缩并恢复局部自动路由，三连及以上节点删除相关线路。复制/粘贴、历史、Worker/主线程、v1–v20 缓存修复和监控物理区间动画已贯通；节点双击改由指针层识别，避免画布 pointer capture 吞掉原生 `dblclick`。218 项单元/组件测试通过、1 项跳过，14 条无截图端到端流程、主题、类型和生产构建通过；等待用户视觉与复杂管路操作验收 |
| 2026-08-24 | 修复 v19 本地缓存中的退化分流自环 | 定位到两个相连分流节点碰撞合并后，原中间边被保存为同一节点指向自身的自环；Schema 升至 v20，旧项目加载时只移除悬空边、自环及其孤立节点，网络规范化同步阻止新自环进入文档。真实 IndexedDB 历史已通过无截图 DOM 流程重新打开且错误状态消失；分流碰撞、规范化和迁移回归均通过。213 项测试通过、1 项跳过，主题、类型和生产构建通过 |
| 2026-08-24 | 实现兼容子线接入与真实分流节点 | Schema 升至 v19；接线可从既有锚点/母线落到兼容子线并拆分共享原子段，保存真实 `junction` 节点。节点只在编辑 hover 时显示圆点，可选择、框选、方向键步进及与图元/母线/途径点混合移动和旋转；同点途径点/节点自动合并，不兼容类型与手动路由冲突整次回滚。删除节点移除全部邻接边且不恢复直连，复制/Duplicate/跨图纸粘贴保留选区内部分流子树，监控流向穿过拆分后的物理边。210 项单元/组件测试通过、1 项跳过，类型、主题和生产构建通过；按项目约定未主动做截图视觉验收，等待用户实际复杂冷却管路操作反馈 |
| 2026-08-24 | 增加图元指标名称与单位总开关 | Schema 升至 v18；单选图元右侧属性面板新增默认开启的“显示指标名称与单位”实例开关，一个开关统一作用于全部数值和文本指标。关闭后画布不挂载指标名称/单位文字并按读数或状态收紧标签宽度，所有值、三级告警颜色和包含完整语义的辅助技术名称保留；设置进入历史、复制与项目持久化，v17 及更早项目默认显示。201 项单元/组件测试通过、1 项跳过，13 条无截图端到端流程、类型、主题和生产构建通过，等待用户视觉与操作验收 |
| 2026-08-24 | 修复网络拆分后的重复孤立路由 | 增量路由与等待态快照改为按 `networkId + edgeId` 复合身份复用，逻辑边保留 ID 但迁移到新网络时会丢弃旧网络几何；画布渲染前过滤错误归属并去重，点击按实际渲染的复合身份解析，避免重复 React key、灰白孤立路径和无法映射到属性面板的选择。选中子线的拖动命中层在无修饰键按下时同步收敛混合选区，避免删除子线时误删此前追加选中的母线。新增网络迁移、重复快照及删除前选区回归；201 项单元/组件测试通过、1 项跳过，13 条无截图端到端流程、类型、主题和生产构建通过；运行中页面 DOM 核对 75 条路径的复合身份与 edge ID 均唯一 |
| 2026-08-24 | 实现可选择途径点与子线手动改线 | Schema 升至 v17，图纸级保存规范化 8px 途径点、逻辑边保存有序引用；选中子线后可拖动任意原子线段创建共享或独立约束，途径点支持单选、追加、框选、删除，以及与图元/母线混合移动和 90° 旋转，含点选区不缩放。Worker/主线程与增量路由统一经过硬约束，冲突整次回滚并汇总 Toast；右侧面板可恢复自动布线并回收孤立点，复制/Duplicate/跨图纸粘贴、撤销重做、JSON/旧版迁移均保留引用语义。198 项单元/组件测试通过、1 项跳过，类型、主题和生产构建通过，等待用户操作与视觉验收 |
| 2026-08-20 | 更新四类制冷线路预设颜色 | 一次回路冷/热和二次回路冷/热的业务默认值与设计 Token 已统一调整为 `#FFC800`、`#FF6011`、`#0084FF`、`#00F074`，覆盖未自定义线路、接线锚点和锚点编辑器；已有显式实例颜色保持不变，无需升级 Schema。188 项单元/组件测试通过、1 项跳过，主题一致性、类型检查和生产构建通过，等待用户视觉验收配色 |
| 2026-08-20 | 将中键平移改为命令式共享视口 | 中键移动通过逐帧 latest-wins 直接更新 SVG 世界根节点合成变换、WebGL 点阵及流动层 Uniform，不再逐帧协调 React 画布树；每 120px 屏幕位移低频同步 overscan 裁剪，结束时提交最终会话视口。新增真实 POD A 25% 缩放性能回归，短距离平移期间 React 画布渲染次数不变且保存状态不变。基准帧时间 P95 由约 50ms 降至 16.8ms，任务总耗时约 1150ms→291ms，脚本耗时约 949ms→57ms。187 项单元/组件测试通过、1 项跳过，类型/主题检查、生产构建及 13 条无截图浏览器流程通过，等待用户操作验收 |
| 2026-08-20 | 取消子线路由共享复用评分并优先居中拐点 | 删除 A* 状态与路由接口中的复用步数、可复用边和共享偏好；正式路由与交互预览在硬约束、实际长度和完整转折之后直接按中点折弯评分。同网络仍允许线路自然重合，但不再为延长共同段主动偏移拐点。新增目标侧已有同网络主干的回归测试；187 项单元/组件测试通过、1 项跳过，类型/主题检查和生产构建通过。125 图元、171 边真实基准完整路由约 2128ms，三网络增量约 31ms，等待用户视觉验收中点效果 |
| 2026-08-20 | 将左侧菜单折叠图标改为折线箭头 | 原面板形状图标已改为 Lucide `ChevronLeft` / `ChevronRight`：左栏展开时显示向左折线箭头，收起后显示向右折线箭头。位置、无背景/描边样式、折叠逻辑和辅助技术语义不变。186 项单元/组件测试通过、1 项跳过，类型/主题检查、生产构建和最终 12 条无截图浏览器流程通过 |
| 2026-08-20 | 调整左侧菜单折叠按钮外观与位置 | 收起/展开按钮已移除常态和 hover 背景及描边，保留图标、hover 颜色和键盘聚焦环。左栏展开时，按钮左边缘从菜单右分隔线开始，整个点击区位于菜单外侧；收起后仍位于画布左边缘。186 项单元/组件测试通过、1 项跳过，类型/主题检查、生产构建和 12 条无截图浏览器流程通过，等待用户视觉验收 |
| 2026-08-20 | 增加左侧菜单收起/展开入口 | 左栏右边缘中部新增纯图形按钮，编辑与监控模式共享当前会话折叠状态；收起后左栏宽度让给画布，画布左边缘保留展开入口，菜单组件不卸载。按钮具有可访问名称与展开语义，状态不修改项目、dirty、历史或持久化数据。186 项单元/组件测试通过、1 项跳过，类型/主题检查、生产构建和 12 条无截图浏览器流程通过，等待用户视觉与操作验收 |
| 2026-08-20 | 移除画布标尺与鼠标坐标并优化中键平移 | 删除顶部/左侧标尺组件、刻度计算和相关 CSS，画布恢复全幅；状态栏不再显示鼠标 X/Y，继续保留自适应点阵与当前网格间隔。中键平移在世界坐标、接线/母线判断之前走独立快速路径，纯平移不再上报应用层命令状态。真实归档 90 次移动中指针到提交延迟 P95 由约 28.9ms 降至 16.6ms，最大 17.5ms。185 项单元/组件测试通过、1 项跳过，类型/主题检查、生产构建和 11 条无截图浏览器流程通过，等待用户操作验收 |
| 2026-08-20 | 支持批量设置子线通行方向 | 纯子线多选和线路网络选择现在统一显示通行方向，一致值直接回显，混合值显示“多种方向”；选择双向、起点→终点或终点→起点后，通过画布批量接口一次更新全部所选逻辑边并只形成一个撤销步骤。母线混合选择隐藏方向字段，单选标签和既有批量颜色逻辑不变。187 项单元/组件测试、类型/主题检查和生产构建通过，等待用户操作验收 |
| 2026-08-20 | 实现子线单向通行约束 | Schema 升级至 v16，独立逻辑子线可在右侧属性面板配置双向、起点→终点或终点→起点；单向子线在配置末端显示固定屏幕尺寸箭头。电力拓扑改用出向/入向邻接，逆箭头路径不可达，明确单向可消除部分多 Source 方向冲突；旧项目默认双向，方向随历史、内部拓扑复制、JSON 与本地保存持久化。186 项单元/组件测试、类型/主题检查和生产构建通过，等待用户视觉与业务验收 |
| 2026-08-19 | 按 Source 独立计算电力最短路径并取并集 | 每个 Grid、Generator、Battery 实例独立计算到各可达 Target 的全部等长最短路径，再合并动画结果；POD A 中邻近 Battery 不再抢占 Grid→UPS→FM 路径。同一区间被推导为相反方向时不动画。Generator 运行与 Battery 放电门控已记入后续任务。183 项单元/组件测试、类型/主题检查、生产构建及 1 条无截图浏览器流程通过，等待用户在实际 POD A 图纸验收流向 |
| 2026-08-19 | 扩展电力流向 Source 与 Target | Source 由 Grid 扩展为 Grid、Generator、Battery，Target 由算力/动力 POD 扩展为算力 POD、动力 POD、FM；继续使用多源最短有效路径并集，FM 作为终点停止内部传播。状态栏改用通用“电力起点 → 终点”文案。181 项单元/组件测试、类型/主题检查、生产构建及 1 条无截图浏览器流程通过，等待用户检查实际图纸中的业务流向 |
| 2026-08-19 | 接入 Cabinet A 与 Cabinet B 两个独立图元 | 原 Cabinet 改名为 Cabinet A 并继续使用稳定键 `cabinet`，新增独立 `cabinet-b`；旧项目加载时刷新安装素材元数据、保留已有锚点并自动追加缺失素材，旧默认实例名同步为 Cabinet A且自定义名称不变。180 项单元/组件测试、类型/主题检查、生产构建和 11 条无截图浏览器流程通过，等待用户视觉验收两份素材 |
| 2026-08-19 | 调整正式路由评分顺序（已取代） | 曾将同网络复用从 `0.9` 长度折扣拆为独立复用步数，正式 A* 按实际长度、完整转折数、共享复用数量和中点偏好依次评分；该过渡顺序已由 2026-08-20 的中点优先规则取代。178 项单元/组件测试、类型/主题检查、生产构建通过；当时 125 图元、171 边真实基准完整路由约 1742ms，三网络增量约 29ms |
| 2026-08-19 | 调整数值指标单位排版 | 数值单位改为紧跟指标名称的无空格半角括号形式，例如 `有功功率(kW)`；右侧读数列只显示当前数值。同步更新标签宽度测量、辅助技术文案、布局单测和无截图浏览器流程，不修改 Schema 或已保存配置 |
| 2026-08-19 | 扩展文本型运行指标与正常优先模拟 | Schema 升级至 v15；运行指标增加数值/文本类型，旧 v14 指标自动迁移为数值。文本指标可维护带等级的候选状态，默认运行/停机/离线；编辑模式稳定展示首个正常候选，监控模式按正常 90、次要 6、重要 3、紧急 1 的等级权重每 30 秒抽样，状态等级仍只作用于指标值颜色。177 项单元/组件测试、类型/主题检查、生产构建及新增数值/文本无截图浏览器流程通过 |
| 2026-08-19 | 将画布缩放和平移改为纯会话状态 | 删除项目 Store 的视口写入接口及画布持久化回调；缩放按钮、滚轮和中键平移现在只更新当前画布内存，不修改业务文档、`updatedAt`、dirty、IndexedDB、JSON 或编辑历史，顶部保持“已保存”。Schema 中既有 viewport 暂作兼容初始值，不再由交互更新。175 项单元/组件测试、类型/主题检查、生产构建和 10 条无截图浏览器流程通过 |
| 2026-08-19 | 实现图元实例运行指标与三级告警 | Schema 升级至 v14；单图元可配置、排序和删除最多 5 项指标，名称与运行数据分别显隐并组合布局。编辑模式使用范围中间值稳定预览且不刷新，监控模式进入即随机生成数值并由全图 30 秒节拍刷新；上限/下限/区间外规则只将数值按次要黄、重要橙、紧急红着色。复制链路保留配置并生成新指标 ID，不复制当前值。同步修正真实归档更新后性能流程的目标图纸和可视图元定位；175 项单元/组件测试、类型/主题检查、生产构建和 10 条无截图 DOM/性能流程通过 |
| 2026-08-19 | 实现跨图纸对象复制与剪切移动 | 编辑模式对象剪贴板在当前项目内跨图纸保留；`Command/Ctrl+C` 与 `Command/Ctrl+X` 后可通过图纸树切换到同专业目标图纸并直接 `Command/Ctrl+V`，副本保持相对布局、贴合 8px 网格并落在当前视口中心。图元、母线和选区内部接线获得新 ID，外部支路不复制；跨专业粘贴使用既有 Toast 拒绝，新建/打开/导入项目会清空剪贴板。顺带使全选包含母线。165 项单元/组件测试、类型/主题检查、生产构建和 8 条无截图浏览器流程通过 |
| 2026-08-18 | 全面优化接线图绘制与路由性能 | Worker 支持过期任务取消、显示属性快照直用和按连接/空间影响范围增量重路由；单任务复用障碍网格，交叉与标签避让使用空间索引，拱桥路径缓存。大图启用 overscan 静态层裁剪、按需锚点、同色共享滤镜及逐帧视口更新。125 图元、20 母线、67 网络、171 边真实归档中，完整路由由约 2572ms 降至约 1809ms，移动已接线图元的正式增量路由约 33ms、只重算 3 个网络并复用 158 条边；当前视口 SVG 后代 562 个、线路 67/171、空闲锚点 0、颜色滤镜 3。163 项单元/组件测试、类型/主题检查、生产构建和 7 条无截图浏览器流程通过 |
| 2026-08-18 | 收敛四区工作区并将命令移入画布 | 删除 42px 次级上下文工具栏及画布外标题/状态布局；画布取消外层 padding、边框和圆角，直接铺满中间区域。返回、路径与统计、编辑/播放命令、缩放和状态改为贴近标尺后可绘图区边缘的透明 HUD，只有按钮保留控件背景。157 项单元/组件测试、类型/主题检查、生产构建和 6 条无截图浏览器流程通过，等待用户视觉验收 |
| 2026-08-18 | 仅动画有效电力 Source→Target 路径 | 电力拓扑从每个可达算力/动力 POD 沿 Grid 多源最短距离反向裁剪，移动虚线只覆盖全部等长最短有效路径的并集；无 Target 整链、主干旁死端支路和不通往 Target 的母线区间不动画，Switch 闭合只恢复路径资格。冷却逻辑明确后另行实现。157 项单元/组件测试、类型/主题检查和生产构建通过，等待用户视觉与业务验收 |
| 2026-08-18 | 分离子线与母线动画宽度并放宽虚线颜色 | 普通子线保持 2px 屏幕三角带，4px 母线改用 4.5px 覆盖宽度，解决母线底边露色且不改变子线观感；虚线改为可集中手调的 `#303238`。2026-08-25 核对提交历史后校正：实际透明度 Token 自首次实现起为 `0.5`，此前文档中的 `0.92` 未进入代码。154 项单元/组件测试、类型/主题检查和生产构建通过，等待用户视觉验收 |
| 2026-08-18 | 编辑属性面板控制 Switch 并分离关开颜色 | 编辑模式单选 Switch 后可通过右侧开关控制与监控画布共享的独立运行态，切换立即保存且不污染项目 dirty、JSON 或历史。Schema 升级至 v13，Switch 关/开颜色独立配置、预览、回显、复制和持久化；旧单色自动复制迁移到两个状态槽，空选全局颜色总览也按关/开分槽替换。153 项单元/组件测试、类型/主题检查、生产构建和 5 项无截图浏览器流程通过，等待用户视觉与操作验收 |
| 2026-08-18 | 修正 Off Switch 停流与动画覆盖 | Off Switch 现在过滤所有直接相邻普通子线的流动动画，同时保留更上游已导通馈线；WebGL 覆盖由原生单像素线改为屏幕空间 2px 三角带，相邻采样段搭接，并使用近黑全不透明 6px/6px 虚线。151 项单元/组件测试、类型/主题检查、生产构建和 5 项无截图浏览器流程通过，等待用户视觉验收覆盖、颜色和停流效果 |
| 2026-08-18 | 收紧子线标签端点边界 | 标签不再围绕端点居中，而是沿端点相邻线段向子线内侧展开：横向使用左/右边界对齐与 8px 水平 padding，纵向使用顶/底边界对齐与 8px 垂直 padding，保证文字不越过端点。150 项单元/组件测试、类型/主题检查、生产构建及子线无截图浏览器流程通过 |
| 2026-08-17 | 实现监控模式首轮 | 新增顶部编辑/监控 Tab；监控画布禁止编辑并保留导航、缩放和平移。Switch On/Off 使用独立 IndexedDB 运行态逐次自动保存，不污染项目 JSON、dirty 或历史；电力流向以 Grid 为 Source、算力/动力 POD 为 Target，经图元和母线 tap 拓扑定向，Off Switch 切断下游。播放时以单批次 WebGL 深色移动虚线覆盖当前线路并沿真圆弧拱桥采样路径移动，共享同向段去重。150 项单元/组件测试、类型/主题检查、生产构建和 5 项无截图浏览器流程通过，等待用户视觉与操作验收 |
| 2026-08-17 | 增加子线端点文字标签 | Schema 升级至 v12；独立逻辑子线新增可选标签、实例显隐、源/目标端点与正/负侧位。局部横线映射上/下，局部竖线映射左/右，并与端点保持 8px 间距；拖动仅预览四态布局，松手单次提交。文字、显隐和方位随历史、内部拓扑复制、JSON 与本地保存持久化；相关单元/组件测试、类型/主题/机械 UI 检查、生产构建及子线无截图浏览器流程通过 |
| 2026-08-17 | 增加母线标签实例开关 | Schema 升级至 v11，新增可选 `Busbar.labelVisible`，v1–v10 母线缺省开启；单选纯母线后在标签输入下显示独立开关，空文字始终不渲染，关闭非空标签只隐藏当前母线并保留内容与端点方位。显隐随复制、撤销/重做、JSON 和本地保存持久化。137 项单元/组件测试、类型与主题检查和生产构建通过 |
| 2026-08-17 | 将图元标签开关改为单图元属性 | 已删除顶部图纸级入口和 `Diagram.canvas.showElementLabels`；Schema v10 最终使用可选 `DiagramElement.labelVisible`，旧图元默认显示。单选图元后可在右侧属性面板独立开关，隐藏时只跳过该图元标签布局与命中区域，并随复制、撤销/重做、JSON 和本地保存持久化。134 项单元/组件测试、类型与主题检查和生产构建通过 |
| 2026-08-17 | 初版图纸级图元标签显隐开关（已取代） | 曾使用 `Diagram.canvas.showElementLabels` 和顶部眼睛图标控制当前图纸，非视觉检查通过后由用户明确否决其作用域与入口；KD-051 改为右侧属性面板的单图元实例开关，图纸级字段和顶部入口已移除 |
| 2026-08-17 | 增加母线双端点可选文字标签 | Schema 升级至 v9；母线新增可选 `label` 与 `labelEndpoint`，旧项目及新建母线默认不显示文字。单选属性面板可填写或清空；非空标签复用设备标识样式，只在起点/终点外侧显示并支持拖动切换，后续按视觉反馈将专用端点间距由 2px 调整为 8px。标签随母线复制、历史、JSON 和本地保存持久化，不参与拓扑和 8px 网格。129 项单元/组件测试、类型/主题检查和生产构建通过 |
| 2026-08-17 | 支持子线/母线混合多选批量改色 | `Shift` / `Ctrl` / `Command` 单击可跨网络追加或移除逻辑子线，共享主干按全部逻辑边参与选择，母线可与子线共同保持选中；统一“线路颜色”HEX 字段支持多色状态、原位预览、恢复默认及单事务提交/撤销，母线 tap 同步预览。多选删除改为统一过滤后单次规范化。123 项单元/组件测试、类型/主题检查、生产构建和 4 项无截图浏览器流程通过 |
| 2026-08-17 | 增加空选画布颜色总览与全局替换 | 空选属性面板按图元、母线、子线汇总当前实际颜色和使用数量；默认色与自定义色按显示值合并，点击后只替换当前画布同类别同色对象。连续输入原位预览，完成后单事务提交，取消与撤销均恢复。122 项单元/组件测试、类型/主题检查、生产构建和 4 项无截图浏览器流程通过 |
| 2026-08-17 | 实现图元设备标识标签 | Schema 升级至 v8；新图元与 v1–v7 项目自动补齐设备标识，标签以世界空间文字始终显示并自动四侧避让锚点出线区域；仅单选后可拖动改变持久化侧向，旋转保持水平文字和画布侧向，多选禁用标签交互；复制、撤销/重做、JSON/IndexedDB 与重复标识提示已贯通。118 项单元/组件测试、类型/主题检查、生产构建和 4 项无截图浏览器流程通过 |
| 2026-08-17 | 修正颜色回显、HEX 选择器与母线 tap 继承 | 移除系统原生 RGB 颜色面板，改为应用内饱和度/亮度、色相和 HEX 选择器；独立子线重新选择回显保存值，网络多色状态显示“多种颜色”；母线 tap 与候选点在预览、提交、撤销、重做和恢复默认时继承母线颜色。111 项单元/组件测试、类型/主题检查、生产构建和 4 项无截图浏览器流程通过 |
| 2026-08-17 | 支持母线与子线颜色编辑 | Schema 升级至 v7，母线与逻辑边新增可选 HEX 颜色；属性面板默认显示可编辑 HEX 值，支持单/多母线、独立子线和共享主干网络批量改色、轻量预览、恢复默认及单步撤销重做。106 项单元/组件测试、类型/主题检查、生产构建和 4 项无截图浏览器流程通过 |
| 2026-08-12 | 初始化项目记忆与文档系统 | 已创建项目目标、关键决策、TODO、历史归档和持续同步规则 |
| 2026-08-12 | 明确第一阶段核心需求 | 已确认运维用户、Web 交付、层级下钻、基础编辑操作及 JSON 导入导出 |
| 2026-08-13 | 确认第一阶段实施方案 | 已接受 React/X6/Ant Design 技术栈、本地优先存储和工程制图台界面方向 |
| 2026-08-13 | 实现第一阶段基础编辑闭环 | 已完成版本化领域模型、双线路四层导航、X6 编辑画布、双标尺、属性编辑、IndexedDB 保存/打开和 JSON 导入导出 |
| 2026-08-13 | 建立第一阶段非视觉验证基线 | 10 项单元/组件测试、类型检查、生产构建及 1 项真实浏览器 DOM 流程通过；视觉验收清单已交给用户 |
| 2026-08-13 | 根据首次验收反馈调整界面与画布手势 | 已改为中性深色极简风格、滚轮直接缩放、中键平移和左树单行；修复矮屏根容器高度，真实浏览器断言通过 |
| 2026-08-13 | 明确后续监控模式的动态能力 | 已确认监控模式需要状态动画、图表和遥测数据模拟，并要求编辑文档与高频运行态分层；模拟不包含物理计算 |
| 2026-08-13 | 调整编辑器和 UI 技术基线 | 初版未通过用户体验验收；已确认改用 AIDC 项目验证过的正交 WebGL + SVG 二维编辑方案及其 UI 设计系统，原 X6/Ant Design 方案停止继续扩展 |
| 2026-08-13 | 按 AIDC 基线完成第一阶段重构 | 已移除 X6/Ant Design，建立 R3F/Three GLSL 网格 + SVG 编辑覆盖层、自定义单事务历史和 AIDC Token/Recipe UI；打开项目 dirty 语义与 viewport 持久化同步修复 |
| 2026-08-13 | 重建第一阶段非视觉验收基线 | 17 项单元/组件测试、类型检查、主题一致性、生产构建和 1 项完整无截图浏览器编辑流程通过；等待用户视觉与操作验收 |
| 2026-08-13 | 建立正式图元资产目录 | 已创建 `src/assets/symbols/` 并记录素材格式、命名、目录接入和 `assetKey` 稳定性约定；现有示例素材暂不迁移 |
| 2026-08-13 | 接入正式图元并收紧几何约束 | 活动素材库已替换为目录内 19 个 SVG；默认使用原始尺寸且画布只显示 SVG；8px 网格、等比离散缩放、CHWP 最小 40×16 和 90° 旋转均通过非截图自动化验证 |
| 2026-08-13 | 将画布网格改为 8px 点阵 | GLSL 与 WebGL 回退均改为点阵；首版采用 8/40 主次点，后由 KD-017 的自适应单级点阵取代 |
| 2026-08-13 | 新增并迭代 POD 图元 | 已新增 64×64 的算力 POD 与动力 POD；前三种视觉方案未通过验收，当前按用户建议试行 `AI/PWR + POD` 英文缩写与立方体组合，等待视觉反馈 |
| 2026-08-13 | 修正缩放后点阵真实比例及标尺错位 | 曾统一为 8/40 世界单位序列；该呈现后由 KD-017 的 8px 倍数自适应单级点阵取代 |
| 2026-08-13 | 改用 8px 倍数自适应单级点阵 | 所有点统一大小和颜色；可见间隔随缩放在 8、16、24、32px 等 8px 倍数间切换，标尺与状态栏使用同一当前间隔，WebGL 与 CSS 回退实现一致 |
| 2026-08-13 | 修复 WebGL 网格缩放参数停留在初始值 | 外层 viewport 通过稳定 ref 与显式 invalidate 传入 R3F；Three.js `onBeforeRender` 直接更新 `ShaderMaterial.uniforms`。浏览器测试拦截 GPU uniform 上传，确认最大缩放收到 `uZoom=4`、缩小一级收到 `uGrid=16` |
| 2026-08-13 | 确认图元锚点编辑器设计简报 | 已确认素材hover编辑图标、双栏弹窗、左侧复用素材列表和右侧8px画布；锚点名称输入、类型下拉及图标式撤销/重做/删除统一放在顶部，方向由边缘自动确定且不可编辑；修改即时应用，弹窗仅保留右上角关闭按钮 |
| 2026-08-13 | 实现图元锚点编辑器 | 已完成素材hover编辑入口、复用素材列表、双栏原生模态框、8px边缘锚点画布、名称/六类型编辑、自动外向方向、拖动、删除、即时应用与独立撤销/重做；项目模式升级至v2并完成JSON、IndexedDB和v1迁移验证 |
| 2026-08-13 | 调整图元素材专业分类 | CPD 已归入冷却，FM、Cabinet、算力 POD、动力 POD 已归入电力；新旧项目加载时同步当前分类与“电力线路”名称，并保留已有锚点 |
| 2026-08-13 | 增加并调整图元拖动十字对齐区域 | 画布内移动与素材拖入均显示贯穿画布的水平/垂直半透明对齐带；纵向带宽和横向带高分别跟随图元当前显示宽高，交叉区与图元一致；结束/取消后隐藏，不进入项目数据和编辑历史 |
| 2026-08-13 | 合并 Switch 状态并增加指定图元实例颜色 | 素材库将 SwitchOn/Off 归并为默认 Off 的单一 Switch，并保留 On/Off 运行态素材映射；2WV、CV、Generator、Grid、MP、Switch、Transformer 可在右侧属性面板改色和恢复默认。Schema v3 自动迁移旧 Switch 键；47 项单元/组件测试、类型检查、主题检查、生产构建和完整无截图浏览器流程通过，等待用户视觉验收 |
| 2026-08-13 | 完成多选图元整体变换与对齐反馈 | 多选成员均显示独立轮廓和整体包围框；可整体移动、等比缩放和绕中心90°步进旋转，结果均贴合8px网格；十字带仅在实际位移后显示，每个成员各有一组且重叠区不叠加颜色 |
| 2026-08-14 | 第一阶段编辑与图元锚点编辑器验收通过 | 用户已确认现有基础编辑、多选变换、对齐反馈及锚点编辑能力通过验收；项目解除“验收前不得开始接线”的阶段约束，进入线路连接与管理规划 |
| 2026-08-14 | 实现图元接线与自动路由首轮 | Schema 升级至v4；完成连通网络、六类型演化、锚点接线、8px正交最短避障、显式分叉、非连接拱桥、选择/删除、历史和自动重路由。53项单元/组件测试、类型检查、主题检查、生产构建和1项完整无截图浏览器流程通过 |
| 2026-08-14 | 实现电力母线 | Schema 升级至v5；完成独立母线、隐式tap连通、线路工具拖入、选择/移动/长度调整/90°旋转、接线接入、共线避让、未连接拱桥、删除清理和历史。60项单元/组件测试、类型检查、主题检查、生产构建和2项无截图浏览器流程通过 |
| 2026-08-14 | 优化母线tap与锚点方向路由 | tap默认沿母线遍历合法8px位置并按总长度、完整转折、滑移距离和稳定offset自动求优；A*计入两端锚点出线方向。新增2项几何测试，全量62项单元/组件测试、构建和2项无截图浏览器流程通过 |
| 2026-08-14 | 收敛母线tap视觉 | 已将已建立tap的固定屏幕半径由3.5px改为2.5px并移除深色描边；候选点、分叉点和命中逻辑保持不变，类型检查与主题一致性检查通过 |
| 2026-08-14 | 实现母线普通子线互联 | 已完成选中母线hover空心起点、双母线普通子线、同母线自连/重复边约束、网络合并与删除后拆分、双端tap联合求优和多子线避让。68项单元/组件测试、构建和3项无截图浏览器流程通过 |
| 2026-08-14 | 实现母线缩短时 tap 受约束压缩 | 已取消固定40px及最外侧tap阻挡；按不同tap数计算动态下限，缩短时比例投影并保持顺序、8px网格和间距，母线/tap单事务撤销重做。73项单元/组件测试、类型/主题/机械UI检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-14 | 补齐编辑器取消、保存与 Duplicate 快捷键 | 已实现主画布 `Esc` 清空接线/图元/线路/母线状态、全局 `Command/Ctrl+S` 保存及多选图元 `Command/Ctrl+D` 单事务复制；75项单元/组件测试、类型/主题/机械UI检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-14 | 优化图元颜色拖拽性能 | 已把连续颜色输入从整项目提交改为 SVG `feFlood`/数据属性原位预览，最终 change/blur 单次提交；高频测试确认4次预览为0次项目写入、最终仅1次提交。75项单元/组件测试、类型/主题检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-14 | 重构同锚点多线与网络级共享路径 | Schema 升级至v6；同一锚点可复用稳定节点连接多条逻辑边，普通子线分叉入口和可见分叉点已移除；同网络网格段允许共享并自动派生分叉，独立支路/共同主干分别支持逻辑边/网络选择删除；v5固定分叉自动迁移。77项单元/组件测试、类型/主题检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-14 | 优化图元拖动与起线跟随性能 | 高频预览已按动画帧合并并跳过重复网格点；图元手势期间使用端点附着的轻量线路预览，提交后单次完整重路由；交互起线恢复有效 A* 启发式。8支路基准中6次图元预览由约889ms降至约0.9ms，6次起线预览由约254ms降至约50ms；等待用户操作验收 |
| 2026-08-14 | 实现母线混合选择并优化线路交互 | 框选已同时包含完整母线与图元，支持统一移动和90°旋转且混合选区禁用缩放；母线拉伸改为逐帧轻量线路/tap预览，4次预览约0.26ms、对照完整重路由约10.8ms；终点图元内部预览由约1086ms降至约15ms，悬停兼容锚点时直接按锚点方向接入；等长少折线路新增中点折弯偏好。88项单元/组件测试、生产构建及3项无截图浏览器流程通过，等待用户操作验收 |
| 2026-08-17 | 母线支持复制、粘贴和 Duplicate | 编辑器剪贴板已统一承载图元与母线；母线单选和混合选择均可通过 `Command/Ctrl+C`、`Command/Ctrl+V` 与 `Command/Ctrl+D` 复制为偏移 16px 的新选择。该项初始不复制 tap、接线或线路拓扑，内部拓扑边界随后由 KD-040 和下一条完成记录取代。88项单元/组件测试、主题/类型检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-17 | 对象复制保留选区内部线路拓扑 | 复制/粘贴与 Duplicate 会筛选两端对象均在选区内的逻辑边，重建图元锚点、母线 tap、节点、边和网络 ID，并按副本位置重新解析类型与自动路由；选区外支路不复制。含内部线路的副本会移出原选区包围框并保留 16px 间隔，避免重叠导致线路不可见。91项单元/组件测试、主题/类型检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-17 | 增强拱桥并记录监控流动架构 | 非连接交叉已改为默认16×8px真圆弧、密集交叉自适应收紧、每条跨线单一5px画布色底衬及可见/命中路径同源；路径数据按路由结果缓存。动画只新增WebGL批量覆盖、显示路径复用、遥测分层和性能降级设计文档，未写动画代码。92项单元/组件测试、主题/类型检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-17 | 收敛拱桥尺寸并加粗底衬 | 根据用户视觉反馈，将孤立拱桥由16×8px收敛为12×6px，画布色底衬由5px加粗为7px；密集交叉收紧、单path底衬、命中同源和拓扑规则不变。92项单元/组件测试、主题/类型检查和生产构建通过，等待用户视觉验收 |
| 2026-08-17 | 继续收敛拱桥并强化底衬 | 根据用户视觉对比，将孤立拱桥由12×6px继续收敛为8×4px，画布色底衬由7px加粗为12px；其余交叉几何、渲染与拓扑规则不变。92项单元/组件测试、主题/类型检查和生产构建通过，等待用户视觉验收 |
| 2026-08-17 | 放开母线异侧接入点共用 | 路由器已把母线接入占用拆为真实穿越禁区和法向侧向占用；相反两侧接线可自动滑到同一派生8px点，同侧线路及未连接穿越点继续避让。新增水平/垂直母线异侧共点几何测试；97项单元/组件测试、主题/类型检查、生产构建及3条无截图浏览器流程通过，等待用户操作验收 |
| 2026-08-17 | 优化方向键连续移动性能 | 图元与母线方向键移动已改为逐帧轻量预览，松键短暂空闲或退出交互时一次性提交并正式重路由；长按/快速连按合并为一个撤销步骤。8支路基准中8次预览约0.59ms，对照8次完整重路由约1081.67ms；95项单元/组件测试、主题/类型检查、生产构建及3项无截图浏览器流程通过 |
| 2026-08-17 | 将正式线路路由移出主线程并收敛大型画布渲染 | 完整网络 A* 已迁入 latest-wins Web Worker；交互继续使用稳定快照轻量预览，新接线即时显示临时路径，接线会话缓存障碍/占用索引并限制交互搜索规模；图元和线路边按对象记忆渲染，重复选择不再写状态，入口取消开发期双执行。67 图元、13 母线、73 边真实归档中，原约692ms松手长任务已消除，生产构建完整拖动无50ms长任务；102项单元/组件测试、类型/主题检查、生产构建和4项无截图浏览器流程通过 |
| 2026-08-17 | 修复远距离接线虚线消失 | 交互路由从无权 A* 改为仅预览使用1.2权重的加权 A*，保留24,000状态性能上限但不再大面积探索等价最短状态；3200×1600远距离终点由约98ms后返回空改为约9ms生成路径，6次接线预览约4.6ms，正式 Worker 路由评分不变。103项单元/组件测试、生产构建和4项无截图浏览器流程通过 |

| 2026-09-01 | 统一母线接入点为可编辑宿主节点 | 保存 offset 已成为路由、预览、监控与重载的唯一实际位置；母线节点支持 hover、选择/框选、轴向及混合移动、双击起线和删除，同点兼容节点自动吸收合并。母线缩短越过节点时停止并单次 Toast，起点调整保持节点世界坐标。Schema 保持 v29；353 项单元/组件测试通过、1 项跳过，生产构建和全量 29 条无截图端到端流程通过，等待用户视觉与复杂图纸验收 |

## 维护说明

- 开始任务时将选中的事项标记为`进行中`。
- 任务受外部条件影响无法继续时标记为`阻塞`并注明原因和解除条件。
- 完成任务时记录可验证结果，不只写“已完成”。
- 已完成明细过长或阶段结束时，将旧记录迁移到 `archive/`。
