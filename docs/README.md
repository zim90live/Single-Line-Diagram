# 项目文档中心

本目录是 AIDC 一次接线图绘制/监控系统的长期项目记录。目标是让需求、决策、执行状态和历史背景始终可追溯，降低跨任务沟通和返工成本。

## 文档地图

| 文档 | 用途 | 何时更新 |
| --- | --- | --- |
| [PROJECT_GOALS.md](PROJECT_GOALS.md) | 项目愿景、范围、成功标准与边界 | 项目方向或范围变化时 |
| [KEY_DECISIONS.md](KEY_DECISIONS.md) | 已确认且会约束后续工作的关键决策 | 形成、修改或废止决策时 |
| [TODO.md](TODO.md) | 当前工作、优先级、阻塞项和完成状态 | 每次推进工作时 |
| [FIRST_PHASE_DESIGN_BRIEF.md](FIRST_PHASE_DESIGN_BRIEF.md) | 第一阶段编辑系统的产品、交互、数据和技术方案 | 第一阶段方案或验收范围变化时 |
| [FIRST_PHASE_ACCEPTANCE.md](FIRST_PHASE_ACCEPTANCE.md) | 第一阶段基础编辑的功能与用户视觉验收清单 | 验收基线、结果或问题变化时 |
| [SYMBOL_ANCHOR_EDITOR_BRIEF.md](SYMBOL_ANCHOR_EDITOR_BRIEF.md) | 图元资产级接线锚点编辑器，以及专用水泵资产和入口/出口角色的数据、交互与验收方案 | 锚点编辑、接线端点或水泵端口规则变化时 |
| [CONNECTION_DESIGN_BRIEF.md](CONNECTION_DESIGN_BRIEF.md) | 图元接线、同锚点多线、中点优先路由、类型演化和拱桥方案 | 接线规则、路由或线路管理边界变化时 |
| [DIRECT_LINE_DRAWING_DESIGN_BRIEF.md](DIRECT_LINE_DRAWING_DESIGN_BRIEF.md) | 显式持续绘线工具、自由端点、闭环、并网与退出语义 | 自由绘线交互、开路端点或闭环语义变化时 |
| [MANUAL_ROUTE_WAYPOINT_DESIGN_BRIEF.md](MANUAL_ROUTE_WAYPOINT_DESIGN_BRIEF.md) | 子线段拖动、可选择/共享途径点、真实分流节点、冲突处理与恢复自动布线方案 | 手动改线、子线接入、点对象或变换约束变化时 |
| [UNIFIED_CONNECTION_NODE_DESIGN_BRIEF.md](UNIFIED_CONNECTION_NODE_DESIGN_BRIEF.md) | 途径点/分流节点统一、连接度派生角色、节点手势与节点起线方案 | 统一节点模型、节点起线、删除或合并语义变化时 |
| [DERIVED_CORNER_NODE_DESIGN_BRIEF.md](DERIVED_CORNER_NODE_DESIGN_BRIEF.md) | 自动路由拐角候选节点、渐进固化、直接起线与回滚边界 | 自动折点交互、候选节点固化或拐角起线规则变化时 |
| [BUSBAR_DESIGN_BRIEF.md](BUSBAR_DESIGN_BRIEF.md) | 电力母线的独立模型、tap拓扑、编辑交互、接线与交叉方案 | 母线模型、几何、接入或管理规则变化时 |
| [BUSBAR_INTERCONNECT_DESIGN_BRIEF.md](BUSBAR_INTERCONNECT_DESIGN_BRIEF.md) | 不同母线通过普通子线互联、双端tap联合求优和网络拆分方案 | 母线互联交互、拓扑或路由规则变化时 |
| [ROUTING_QUALITY_DESIGN_BRIEF.md](ROUTING_QUALITY_DESIGN_BRIEF.md) | 母线节点固定位置、图元锚点即时转弯和完整路径评分方案 | 默认路由质量、母线节点或稳定性规则变化时 |
| [MONITOR_FLOW_ANIMATION_DESIGN.md](MONITOR_FLOW_ANIMATION_DESIGN.md) | 后续电流/水流动画的分层、路径复用和性能约束 | 监控流动表达、动画规模或性能基线变化时 |
| [MONITOR_RUNTIME_ARCHITECTURE.md](MONITOR_RUNTIME_ARCHITECTURE.md) | 监控模式迁移到其他 React 项目的运行时分层、公共接口、数据 Provider 和后续导出包边界 | 监控代码拆分、跨项目复用或运行时导出策略变化时 |
| [AIDC_INTEGRATION_PREPARATION.md](AIDC_INTEGRATION_PREPARATION.md) | 合入 AIDC 的共享 UI 真源、运行时包边界、宿主接口、当前差异和实施顺序 | AIDC 合入、共享组件包或宿主 API 变化时 |
| [DEMO_RUNTIME_SIMULATION_DESIGN.md](DEMO_RUNTIME_SIMULATION_DESIGN.md) | 可移植拟真演示运行时、设备指标档案、异常预算、确定性场景和 RuntimeBundle 演进方案 | 模拟状态、设备范围、故障分配或跨项目演示策略变化时 |
| [../DESIGN_SYSTEM_PORT.md](../DESIGN_SYSTEM_PORT.md) | AIDC `97ecddf` Token、组件与业务 UI Pattern 的迁移映射和后续规则 | 设计真源、基础组件或迁移范围变化时 |
| [BATCH_PROPERTY_EDITING_DESIGN_BRIEF.md](BATCH_PROPERTY_EDITING_DESIGN_BRIEF.md) | 手动多选图元、母线和子线后的共同属性、混合值、指标模板与事务边界 | 批量选择范围、共同属性或覆盖语义变化时 |
| [COOLING_PIPELINE_DIRECTION_DESIGN_BRIEF.md](COOLING_PIPELINE_DIRECTION_DESIGN_BRIEF.md) | 制冷管路统一静态样式、三层方向语义、闭合回路模拟与无遥测验收方案 | 制冷管路样式、方向约束、水泵端口或水流模拟规则变化时 |
| [COOLING_VISUAL_FLOW_SIMULATION_DESIGN.md](COOLING_VISUAL_FLOW_SIMULATION_DESIGN.md) | 无遥测阶段的泵功率控制、等阻力视觉流量、监控属性面板与非物理边界 | 制冷模拟流量、泵阀控制、动画速度或压力边界变化时 |
| [MONITOR_ELEMENT_METRICS_DESIGN.md](MONITOR_ELEMENT_METRICS_DESIGN.md) | 图元实例数值/文本运行指标、30 秒模拟、三级告警和组合标签方案 | 指标配置、模拟数据、告警等级或画布显示规则变化时 |
| [GENERIC_SYMBOL_DESIGN_BRIEF.md](GENERIC_SYMBOL_DESIGN_BRIEF.md) | 通用图元的虚线框、自由网格尺寸、框内设备标识、颜色与运行指标规则 | 通用图元外观、几何、标签或指标职责变化时 |
| [archive/](archive/) | 阶段总结、已失效记录和历史快照 | 阶段结束或文档清理时 |

项目级持续协作规则记录在仓库根目录的 `AGENTS.md`，后续 Codex 任务应自动读取。

## 同步流程

1. 开工前：阅读项目目标、有效关键决策和当前 TODO。
2. 执行中：发现新约束、范围变化或冲突时，先提醒用户并澄清方向。
3. 完成后：更新 TODO；如目标或决策发生变化，同时更新对应文档。
4. 阶段结束：将不再适合留在活动文档中的详细记录移入 `archive/`，并保留日期和来源。

## 状态约定

- TODO 状态：`待办`、`进行中`、`阻塞`、`已完成`、`已取消`。
- 决策状态：`提议`、`已接受`、`已取代`、`已废止`。
- 优先级：`P0` 必须立即处理，`P1` 核心近期工作，`P2` 后续重要工作，`P3` 候选优化。

## 最近同步

- 2026-09-07：TMU、FM、UPS-group、Cabinet 已替换为用户最新 SVG。TMU 按 SVG 原始画布改为 48×48，旧 48×64、72×96、64×96 默认实例会迁移到新尺寸并保留锚点与接线；FM、UPS-group、Cabinet 保持原有 64×64、48×48、48×48 尺寸。
- 2026-09-07：冷却链路四种默认色更新为一次冷 `#FCBC00`、一次热 `#FB7800`、二次冷 `#1BA4FF`、二次热 `#00B387`，业务默认值与主题 Token 同步，显式自定义颜色保留。详见 KD-044 配色修订。
- 2026-09-07：CWP、CHWP、CT、PHE 用最新附件 SVG 替换同名 PNG，逻辑尺寸分别更新为 80×128、80×128、96×96、80×128；旧项目实例与边缘锚点在载入时迁移，稳定键、角色和接线引用保持。CPD 继续使用 PNG，Schema 保持 v37，详见 KD-150。
- 2026-09-04：所有图元锚点取消固定法向引出段，网格锚点可在端点直接向外或沿边缘两个切向接线，非网格锚点只保留最短正交过渡；方向降为等价路径软偏好，其他图元仍是硬障碍，母线规则不变。预览、正式路由和端部子线拖动已统一，Schema v37 与持久化拓扑不变，详见 KD-148。
- 2026-09-04：MP 改为单锚点非侵入式管路测量点：替换为用户附件的单侧引线 SVG，固定底部通用冷却锚点，可直接接到任意兼容管路，且不形成进/出口、内部通路或流动门控。新 MP 预置温度、压力、流量；Schema v37 将旧双端 MP 管网安全合并到唯一测点，实际 `WuHu AIDC 0904` 存档 46 个 MP 迁移通过。详见 KD-147。
- 2026-09-04：Schema v36 增加电力模板锚点 A/B 输入角色和电力子图入口通道。双路受电图元与 Cabinet 统一执行 A 可达优先、否则自动切 B、两路均失电停电；下探图纸继承父级实际通道，UPS-group 两路均失电时对应 UPS 子图由 Battery-group 自动备电。编辑器支持模板角色、入口通道批量配置和实例下探图纸绑定，v35 及更早的常规“电路 1/2”双输入资产自动迁移。详见 KD-146。
- 2026-09-03：AIDC 被确认为接线图监控运行时的目标宿主和 UI 唯一真源；业务 UI 导入已收敛到未来包名 `@aidc/ui`，新增 AIDC 提交/文件锁与漂移审计。RuntimeBundleViewer 同时向宿主开放指标、设备态、图元和水泵派生流量 presentation，供 AIDC 使用同一套右侧组件承载。详见 KD-144 与 `AIDC_INTEGRATION_PREPARATION.md`。
- 2026-09-03：Schema v35 将 CWP、CHWP 及其他显式 `pump` 实例的启停状态和输出功率纳入项目存档；编辑与监控面板共用同一控制组件和实例快照，任一模式调整都会标记 dirty。保存、JSON、复制与 RuntimeBundle v2 均保留，旧存档默认运行/100%，高频读数仍不持久化。详见 KD-142 与 `COOLING_VISUAL_FLOW_SIMULATION_DESIGN.md`。
- 2026-09-03：按用户澄清，将编辑与监控业务的工作区外壳统一为 AIDC `97ecddf` 监控模式：280px 监控树、360px 检查器、90% 面板/30px 模糊/无阴影、参考树与返回 SVG、透明路径区和低描边命令控件；编辑管理与素材能力保留但改用监控视觉组合。详见 KD-139 与 `DESIGN_SYSTEM_PORT.md`。
- 2026-09-03：电力素材库新增 48×48 Supply、Load、AC-AC Converter、Rectifier、Inverter；Supply 与 Load 增加默认运行的“运行 / 待机”双态及两套独立颜色，支持编辑/监控切换、保存和 RuntimeBundle 导出，待机演示功率与负载率归零。其他三项保持附件原始配色且无状态入口；五类设备均不自动取得 Source/Target、拓扑截断、预置锚点或父子绑定语义。详见 KD-140 与 `DEMO_RUNTIME_SIMULATION_DESIGN.md`。
- 2026-09-03：校准 UPS L 演示：修复 `ups-group` 父图元未被供电下探识别导致的整页无电流动画；锂电组 SOC 稳定保持 `82–98%`，备电时间改为由同一 SOC 快照派生并保持强正相关，三级异常底色调整为更明亮的黄/橙/红。详见 KD-137 与 `DEMO_RUNTIME_SIMULATION_DESIGN.md`。
- 2026-09-03：根据第二轮视觉反馈校正沉浸式 HUD：移除统一渐变与常驻内描边，编辑树、路径、检查器和监控树改用各自单色半透明材质；编辑树恢复整行深灰选择态和紧凑图标层级，监控树改为无阴影、低对比行状态并增加无边框搜索。右侧信息块和只读元数据去除非必要外框，表单焦点与错误边界保留。详见 KD-136 与 `DESIGN_SYSTEM_PORT.md`。
- 2026-09-03：根据视觉反馈补齐 AIDC `97ecddf` 的沉浸式工作区 Pattern：画布铺满应用，64px 半透明顶栏以及 4px 贴边的编辑/监控树、路径、命令、缩放与右侧检查器改为悬浮 HUD；树行、层级缩进、图标、选中态和面板材质同步校准。详见 KD-135 与 `DESIGN_SYSTEM_PORT.md`。
- 2026-09-03：完成 AIDC `97ecddf` 设计系统迁移：`theme.json` 成为作用域化 Token/Recipe 唯一真源，基础组件补齐为九个视觉原语及 Pressable/TabList 语义辅助，Topbar、树、画布 HUD 和右侧面板改用共享组件；冷却、电力与告警值独立置于 Domain Token 层。详见 KD-134 与 `DESIGN_SYSTEM_PORT.md`。
- 2026-09-03：完成确定性 Demo Runtime Engine 核心实现：所有现有设备使用版本化演示范围，每张非空图纸稳定分配极少数异常；On/Off、Supply/Load 运行/待机、泵阀、离线、指标与流动拓扑保持一致，播放暂停冻结数据。RuntimeBundle 升级到 v2 并固化种子、档案版本、指标语义和故障分配，v1 继续兼容；旧手工范围保留但不再主导标准演示。全量 456 项测试通过、1 项跳过，生产构建通过；详见 KD-133 与 `DEMO_RUNTIME_SIMULATION_DESIGN.md`。
- 2026-09-03：图元与节点间子线的运行指标改为名称列、数据列独立测量；所有行共享最长数值/文本状态宽度、统一右对齐和异常背景宽度，设备标识、子线标题或指标名称不再拉宽数据列。母线既有 14px 四态标题布局不变，详见 KD-132 与 `MONITOR_ELEMENT_METRICS_DESIGN.md`。
- 2026-09-03：完成共享场景与路由目录迁移：几何/视口、点阵、手势、线路表现、连接几何、三类标签布局、素材目录归入 `scene/`，正式路由调度、Worker 和 Hook 归入 `runtime/`；`runtime/scene/monitoring` 产品代码不再导入 `editor/`，旧路径保留兼容重导出。详见 KD-131。
- 2026-09-03：编辑画布与监控画布共用 `DiagramScenePrimitives`、`connectionScene` 与纯数据 `flowPresentation`，统一图元主体、三类标签、母线基线/节点、颜色与冷却滤镜、线路渲染分组、最终显示路径、静态流动分组、桥面/管壳、方向箭头及相关拓扑派生。编辑事件继续通过稳定 ref/children 或画布包裹层注入，纯场景派生不加载 Three，运行时入口不携带编辑命令。详见 KD-130。
- 2026-09-03：跨项目 React 入口和当前产品监控模式统一使用 `DiagramMonitorCanvas`，直接渲染 RuntimeView 的线路、图元、标签、点阵和流动动画，并保留下探选择及中键/空格/触控板视口手势；只有编辑模式实例化 `DiagramCanvas`。详见 KD-129 和 `MONITOR_RUNTIME_ARCHITECTURE.md`。
- 2026-09-03：完成 RuntimeBundle v1 与独立 React 宿主入口：可从一张或多张入口图纸裁剪可校验运行时 JSON，携带下级子树、必要祖先、业务画布对象、已使用资产、On/Off 快照和冻结下探关系；`RuntimeBundleViewer` 支持宿主管理图纸、播放、运行态覆盖和数据 Provider。编辑 Store、历史、dirty、当前相机和随机读数不进入包，项目 Schema 保持 v33。详见 KD-128 和 `MONITOR_RUNTIME_ARCHITECTURE.md`。
- 2026-09-03：建立 `src/runtime/` 监控运行时边界：完整项目可派生单图运行时视图，运行状态与下探关系收敛为统一上下文，指标和冷却数据支持 Provider 注入，监控拓扑从画布抽为 Hook，并提供只读 React 查看器入口。现有应用已反向使用核心边界，视觉、交互、项目 Schema 与模拟结果不变。详见 KD-127 和 `MONITOR_RUNTIME_ARCHITECTURE.md`。
- 2026-09-03：编辑与监控主画布背景改为纯黑 `#000000`；WebGL 与 CSS 回退路径一致，现有自适应点状网格完整保留，全局主题和通用图元背景不变。详见 KD-126。
- 2026-09-02：监控模式下，冷却 POD 内的 `CDU-N` 可按编号进入同级直接子图“TMU到FM N”；当前 POD A 的 `CDU-01` 点击后进入“TMU到FM 01”。显式目标仍优先，编辑模式与项目数据不变，详见 KD-125。
- 2026-09-02：电力素材库新增用户提供的 48×48 UPS-group，使用独立稳定键 `ups-group`；首版作为普通电力图元，不继承现有 UPS 的锚点、监控下探或其他特殊运行语义。旧项目自动补入，Schema 保持 v33。详见 KD-123。
- 2026-09-02：冷却子线可把起点端或终点端配置为“水流源头”；每个有效源头按 `100 m³/h` 视觉基准向同一管网中其他可达开放自由端注入，并继续服从管路方向、泵阀、CV 和设备内部回路约束。源头与水泵贡献可叠加，Schema 保持 v33，详见 KD-124。
- 2026-09-02：母线标签可按实例配置文字颜色；右侧属性面板提供 HEX 选择器、即时预览和恢复默认，默认 `#B6B8B4`，固定 14px 字号及四态端点布局不变。Schema 升至 v33，详见 KD-122。
- 2026-09-02：母线标签字号由 10px 提升至 14px，并像子线标签一样按“几何端点 + 垂直侧”形成四种位置；水平母线映射上/下，垂直母线映射左/右，拖动同时选择最近端点和指针侧。Schema 升至 v32，详见 KD-121。
- 2026-09-02：Cabinet 明确为支持 A/B 双路独立进线的计算机柜终点；不同锚点不再内部互联，两路都可达时同时显示电流，单路失电时只保留有效一路，避免分接单元 L 继续表现为主用/备用切换。详见 KD-120。
- 2026-09-02：真实电力子线节点端在拖动、手动改线、混合移动或粘贴落点时精确落到唯一母线，会保留 ID 与全部邻接边并自动转换为母线宿主节点，同点复用、同母线网络立即合并；无节点被动穿越继续不连通。Schema 保持 v31，详见 KD-119。
- 2026-09-02：配置显式外部供电入口的电力子图会补齐有效母线的全部 tap 区间和两端物理尾段；同一已带电设备的其他明确单向母线入线也保留动画，避免并行输入被最短路径距离差裁掉。无显式入口和普通冗余回路保持原规则，详见 KD-118。
- 2026-09-02：图元模板锚点拖动不再锁定初始边；一次手势可在四条边之间切换，预览吸附最近的非角点 8px 合法位置，并按新边自动更新外向方向。数据模型与 Schema 不变，详见 KD-117。
- 2026-09-02：电力子图的子线可把起点端或终点端标记为“外部供电入口”；存在显式入口时只从这些真实进线端注入父级供电，停用母线与自由边界的自动 Source 推断，未配置的旧图继续兼容原规则。入口与通行方向独立，冲突时明确提示并停流。Schema 升至 v31，详见 KD-116。
- 2026-09-02：电力母线可在右侧属性面板单选或批量设置监控电流为自动、起点→终点或终点→起点；人工方向只在真实带电时按完整母线长度动画，父图断电不制造电流。画布不显示静态方向箭头或入口圆点，编辑 hover/选中显示入口/出口文字，监控方向由电流动画表达。Schema 升至 v30，旧项目默认自动，详见 KD-115。
- 2026-09-02：普通图元的设备标识与指标组合标签只避让实际已连接锚点的出线走廊；未连线锚点不再迫使标签改变默认方位，接线增删后按当前拓扑重新布局。Schema 与持久化不变，详见 KD-113。
- 2026-09-02：电力素材库新增用户提供的 48×48 Battery-group，使用独立稳定键 `battery-group`；首版作为普通电力图元，不继承 Battery 的 Source 角色，也不预置锚点、改色或状态。旧项目自动补入，Schema 保持 v29。详见 KD-110。
- 2026-09-01：进入或跳转图纸时，画布按旋转图元、完整母线和自由线路节点的稳定业务边界自动居中看全一次；保留 48px 屏幕留白，小图不放大，超大图可低于常规 25%，异步路由不触发二次跳动。视窗仍为纯会话状态，详见 KD-109。
- 2026-09-01：电力素材库新增 32×32 Tap-off Unit，作为 Tap-off Unit A/B 的独立子图元；可单独拖入、配置锚点并在右侧修改单一实例颜色，首版不自动嵌入或跟随父图元。旧项目自动补入，Schema 保持 v29。详见 KD-108。
- 2026-09-01：电力素材库新增用户提供的 48×48 Cabinet；原 Cabinet A/B 的显示名改为 Tap-off Unit A/B，历史稳定键、锚点和接线保持，新 Cabinet 使用独立键。旧默认实例名定向更新，自定义名称不变，Schema 保持 v29。详见 KD-107。
- 2026-09-01：母线接入点在用户口径中统一为“节点”，保存位置直接作为实际位置；支持 hover、选择/框选、沿母线及混合移动、双击起线和删除，同点接线复用一个节点。母线缩短不得越过节点并单次 Toast，Schema 保持 v29。详见 KD-106。
- 2026-09-01：编辑模式的非根图纸行在 hover 或键盘聚焦时于右侧显示删除图标；点击立即删除目标及完整子树，同步清理图元、母线与连接网络，当前图纸被删时回到原上级。根图与监控模式无入口，Schema 保持 v29。详见 KD-105。
- 2026-09-01：监控模式的水泵停止后改用用户提供的统一深色 `PumpOff.png`，重新运行或返回编辑模式时恢复原设备素材；替换复用原尺寸、旋转、锚点、接线和标签。当时泵状态为会话态、Schema v29；其存储边界已于 2026-09-03 由 KD-142 / Schema v35 取代。详见 KD-104。
- 2026-09-01：CDU 已换用用户提供的 192×96 横版 SVG，画布逻辑尺寸同步由 96×96 调整为 192×96；旧项目的实例、缩放、旋转位置和锚点会一次性迁移并保留接线引用，Schema 保持 v29。详见 KD-103。
- 2026-09-01：节点间子线新增整线“下层 / 自动 / 上层”跨线层级，纯子线多选可批量设置；编辑、监控静态与动画按同一最终顺序处理拱桥和遮罩。Schema 升至 v29，旧项目默认自动；子线跨母线、真正节点/分叉、拓扑与流量不变。详见 KD-102。
- 2026-09-01：冷却素材库稳定键 `tmu` 当时换用 640×960 透明 PNG，画布逻辑尺寸曾修订为 64×96；该素材已于 2026-09-07 替换为 SVG，并按附件原始画布改为 48×48，兼容迁移旧 48×64、72×96、64×96 默认实例。首版仍不预置锚点或冷却设备角色，Schema 保持 v28。详见 KD-101。
- 2026-09-01：修复节点分段后的冷却拐角退化为平头直角。自由二连节点的两条相邻子线现在各绘制半个 `8px` 圆弧并在中点连续拼合；短段自动收紧，三向/四向分叉、图元锚点和紧邻拱桥处仍保持原规则。编辑管体/内芯/选择/命中及监控静态与动画复用同一显示几何，不修改 Schema，详见 KD-079。
- 2026-09-01：恰好单选一条子线时，右侧属性面板可像图元一样配置最多 5 项数值或文本运行指标，并独立控制“显示子线标签”“显示运行数据”“显示指标名称与单位”。编辑预览、监控 30 秒模拟、告警、复制与保存复用图元机制；Schema v28 把配置保存到节点间边，节点拆分只在标签端点段保留一份，详见 KD-100。
- 2026-09-01：画布 HUD 新增显式“绘制线路”按钮，开启后可从空白网格点或已有拓扑起笔，连续单击绘制真实正交线路；落到已有对象或闭环后工具仍开启，`Esc` 才退出。Schema v27 允许自由开路端点和显式同网络闭环，详见 KD-099。
- 2026-09-01：通用图元右侧属性面板新增独立“背景颜色”HEX 控件，默认 `#121316`，支持即时预览、确认、恢复默认、历史、复制和项目持久化；虚线框颜色与显隐不受影响。复用实例属性容器，Schema 保持 v25；详见 KD-097。
- 2026-08-31：继续修复监控模式同级拱桥遮罩。静态连接由按角色整批绘制改为按编辑态稳定线路组顺序逐组重放“遮罩 → 管体 → 内芯”，主要—主要、辅助—辅助、主要—辅助和电力交叉统一保持正确上下层级；监控片段仅增加运行时边归属，不改变 Schema。详见 KD-095。
- 2026-08-31：冷却辅助线路外管由主线共用的 `#262626 / 50%` 内阴影拆为独立 `#1D1F20 / 20%`，继续降低材质对比；同时修正监控主要—辅助拱桥的补绘顺序为“辅助内芯 → 遮罩 → 主线外管 → 主线内芯”，避免遮罩擦除主线外管。详见 KD-094、KD-095。
- 2026-08-31：冷却辅助线路管体由 `6px` 进一步收至 `4px`，保留 `1px` 内芯；辅助拱桥遮罩由 `14px` 收至 `12px`。选择高亮、命中、拓扑与动画语义不变；详见 KD-094。
- 2026-08-31：修复混合选区旋转把自由节点写到半网格、导致本地历史无法通过 8px 校验的问题。旋转后节点统一吸附；载入时只定向修复编辑器生成的 `route-waypoint-*`、`connection-node-*` 越网格坐标，外部非法节点仍严格拒绝。Schema 保持 v25；详见 KD-096。
- 2026-08-31：修复拱桥归属正确但实际上下绘制顺序错误。冷却线路按辅助→主要拆分稳定批次，监控静态层在辅助内芯后补绘主线桥面遮罩；电力继续母线先绘、普通子线后绘。Schema 与路由不变；详见 KD-095。
- 2026-08-31：拱桥归属改为确定性业务层级：普通子线始终跨越未连接母线，冷却主要线路始终跨越辅助线路；同级子线仍按稳定创建顺序。修改主辅级别只重算交叉归属，不触发寻路，Schema 保持 v25；详见 KD-095。
- 2026-08-31：图元多指标继续共用统一名称列和右对齐数值列，最宽行的名称/单位到数值列间距从 8px 收紧为 0px；短名称行保持列对齐。仅调整派生标签几何，不改变 Schema、告警或持久化；详见 KD-060。
- 2026-08-31：项目升级至 Schema v25；冷却逻辑子线可在右侧面板单选或批量设为主要/辅助。辅助线路先调整为 `20%` 原色与 `80% #70736F` 的低对比显示色和 `6px / 1px` 管体/内芯，随后管体进一步收至 `4px`；主要线路保持 `10px / 2px`。粗细只表达视觉层级，不改变拓扑和流量。编辑模式选中的冷却子线继续使用不透明 `6px #B7B8B5` 中性高亮轨道；详见 KD-094。
- 2026-08-31：编辑模式图纸树增加新增图纸、行内重命名、拖拽改父级/同级顺序和 `Alt+方向键` 键盘管理；子树移动自动归一化四级层级，拒绝跨线路、循环、根图移动与超深操作。冷却/电力线路分组和含子图节点支持独立收起/展开及普通 `←/→`，当前路径按需自动展开；折叠仅属会话状态，不触发保存。复用既有父子字段和数组顺序，Schema 不变，管理结果进入 dirty、本地保存与 JSON；监控模式只读，详见 KD-093。
- 2026-08-29：图元运行指标的次要、重要、紧急异常数据改为黄、橙、红紧凑背景和黑色文字；指标名称与单位保持原样且不带底色，正常数据继续透明中性。名称与单位隐藏时异常数据徽标尺寸不变，只随仅值布局重新对齐。Schema 与告警计算不变；详见 KD-092。
- 2026-08-29：修复 macOS 普通鼠标像素级小步进被误判为触控板平移；分类器优先识别 Chromium/WebKit 离散滚轮 tick 并兼容 Retina，页面滚动继续由非被动监听取消，详见 KD-091。
- 2026-08-28：macOS 触控板增加双指平移与指针中心捏合缩放；连续像素滚动与离散鼠标滚轮在会话内区分，平移复用命令式 SVG/WebGL 热路径，全部视口操作继续不触发保存状态，详见 KD-091。
- 2026-08-28：编辑与监控主画布增加 Figma 风格的空格+鼠标左键临时抓手，复用中键平移的命令式 SVG/WebGL 会话视口；表单输入与业务编辑不被抢占，纯平移继续不触发保存状态，详见 KD-090。
- 2026-08-27：CHWP、CWP、CPD、CT、PHE 由同名 SVG 切换为 5 倍像素密度透明 PNG。前四者保持逻辑尺寸；PHE 改为 200×80 横版，旧 80×160 实例和标准锚点在载入时受控迁移并保留锚点 ID 与接线引用；详见 KD-089。
- 2026-08-26：完成制冷视觉流量模拟增量：监控模式可选择泵阀并在右侧面板控制，水泵支持 `0–100%` 输出功率；固定 `100 m³/h` 基准，经等阻力网络分配有符号视觉流量并驱动动画方向/速度。不模拟压力或其他水力参数，详见 KD-087。
- 2026-08-26：项目升级至 Schema v24；CV 使用独立 `check-valve` 角色，开启时只允许入口→出口、关闭时完全阻断。旧项目标准 CV 自动补齐顶部入口和底部出口且保留锚点 ID 与接线，2WV/普通阀门仍为开启后双向；详见 KD-086。
- 2026-08-26：项目升级至 Schema v23；Switch、2WV、CV 的 On/Off 状态进入图元快照、本地存档与 JSON 导入导出。兼容 `monitorSwitchStates` 表继续即时镜像，旧文档缺失字段时可恢复历史状态；显式项目快照优先。状态切换仍不更新 `updatedAt`、不标记 dirty、不进入撤销历史，详见 KD-085。
- 2026-08-26：修复真实 POD A 六台水泵无动画。直接接线继续严格区分冷热类型，设备内部改按一次/二次回路族连通，使 CT/PHE 同一回路冷热端贯通且 PHE 两侧隔离；全量简单路径枚举替换为逐物理区间的简单见证路径，避免并联图组合爆炸。真实图非截图验证 6/6 台泵有效并生成制冷 WebGL 动画层；详见 KD-083。
- 2026-08-25：完成制冷管路阶段 B 首版。项目升级至 Schema v22；冷却资产可标记为水泵或阀门，水泵锚点配置唯一入口/出口。确定性 Provider 默认泵运行、`50%` 流量、阀门打开，监控画布可点击泵阀切换会话状态。闭合回路求解执行线路类型、单向约束、阀门门控、多泵/并行路径并集与方向冲突静止；监控双层管体沿圆角及制冷拱桥叠加独立样式的移动虚线。旧 v21 项目不猜测角色，待用户视觉与真实图纸操作验收。
- 2026-08-25：编辑模式制冷管路的普通 90° 拐角改用 `8px` 真圆弧，短线段按相邻段一半自动收紧；同网络实际物理方向达到 3 个及以上的分叉保持直角，二连节点与共享两方向弯头仍可圆角。上层、下层、选择、命中和接线预览共用圆角显示几何，紧邻拱桥处不圆角；电力线路、监控模式、拓扑和持久化数据不变。
- 2026-08-25：继续微调制冷双层管路：编辑模式下层管体由 `12px` 收细为 `10px`，端帽由圆头改为平头并取消图元锚点端内缩，使平头直接止于锚点中心；制冷拱桥世界空间遮罩由 `15px` 加大为 `18px`。上层 `2px` 内芯、`16px` 世界空间命中、电力线路和监控模式不变。
- 2026-08-25：微调制冷双层管路：编辑模式回路色内芯由 `4px` 收细为 `2px`，制冷非连接交叉拱桥由 `8 × 4px` 放大为 `16 × 8px`；外管、命中宽度、电力线路和监控模式保持不变。
- 2026-08-25：修复制冷双层管体在图元锚点和公共段的视觉重叠。外管锚点端内缩 `6px`，内芯与交互路径保持原位；同网络逻辑边合并为复合管体并只计算一次内阴影，网络内按“拱桥底衬、管体、内芯”分层渲染。未修改路由、Schema 或监控模式，等待用户复验原截图位置及复杂交汇。
- 2026-08-25：制冷管路阶段 A 从单层 `4px` 修订为双层静态样式：下层 `12px #262626` 管体使用 `x 0 / y 1 / 模糊 3 / #FFFFFF 50%` 内阴影；该版上层 `4px` 回路色已被后续 `2px` 微调取代。同步扩展预览、选择态、粗管命中和拱桥底衬。电力线路、监控模式、水泵角色和水流动画不变。
- 2026-08-25：完成制冷管路阶段 A 初版单层静态编辑样式（已被上一条双层样式修订取代）；同步适配选择态、预览、透明命中、拖段命中、拱桥底衬和固定屏幕单向箭头。
- 2026-08-25：确认专用水泵素材的出入口在既有锚点编辑器中按资产级配置：素材标记为水泵，冷却锚点分别指定唯一入口和出口，全部实例继承且旋转不互换。配置不完整不阻止静态编辑，但禁用模拟驱动并返回诊断；通用图元首版不提供实例级水泵角色或端口覆盖。
- 2026-08-25：确认制冷管路静态样式、三层方向和无遥测模拟方案；其中初始单层 `4px` 视觉已被后续双层修订取代。编辑模式只表达回路、拓扑和允许方向；动态阶段以运行水泵出口到同一水泵入口的有效闭合路径判定动画资格，使用确定性模拟 Provider 验证，不进行物理水力计算。
- 2026-08-26：2WV、CV 使用用户新增的 Off/On 素材归并为默认 Off 的稳定状态图元；与 Switch 共享跨模式独立运行态、右侧状态开关和分离的关/开颜色槽，旧单色自动复制迁移。监控点击直接切换并自动保存；作为制冷阀门时同一状态门控内部水路。Schema 与 IndexedDB 版本不变，详细约束见 KD-084。
- 2026-08-25：通用图元新增实例级“显示虚线框”开关，默认开启；关闭只隐藏描边，背景、框内标识、锚点、运行指标和已配置颜色保留。复用既有实例属性容器，不升级 Schema。
- 2026-08-25：修复通用图元新建锚点在冷却图中无法连接：默认锚点类型改为按当前图纸系统选择，且已验证自由尺寸、旋转后的通用图元可以正常起线和落线；已有锚点的明确类型不自动改写。
- 2026-08-25：新增稳定键 `generic` 的通用图元：默认灰色可改色虚线框、近黑背景与框内白色设备标识；宽高分别吸附 8px 网格，旋转时标识保持水平并经省略和裁剪限制在框内，运行指标继续复用四侧布局。本次不升级 Schema。
- 2026-08-25：修复既有无解手动线路阻止无关图元移动。对象变换只阻止本次新增的节点约束冲突；无解边虽然没有派生路径，也会在后续几何变化时重新路由，因此移开遮挡图元可立即恢复线路，不再永久复用旧无解状态。
- 2026-08-24：完成自动路由拐角派生候选节点：编辑 hover 按需显示，纯点击只选择子线，可直接拖动物化节点或双击以延迟源节点派生子线；只有成功操作才在同一事务中固化统一节点与新边，`Esc`、取消或失败不留下数据。共享转向拐角复用一个节点，不新增 Schema 或常驻拐角 DOM，等待用户视觉与复杂管路操作验收。
- 2026-08-24：完成统一节点与节点起线，项目升级至 Schema v21。旧途径点和 `junction` 迁移为网络内统一 `node`，逻辑边以有序节点引用保持属性完整；节点可单击选择、拖动/混合变换、双击起线并连接兼容锚点、母线、另一网络节点或子线。同点合并、按物理连接度删除、v20 历史兼容和监控物理区间动画已贯通，等待用户视觉与复杂管路操作验收。
- 2026-09-01：节点、图元和母线支持混合框选/追加多选后保持相对位置一起移动；项目升级至 Schema v26，持久化节点成为相邻子线段的真实边界。v1–v25 的 `routeNodeIds` 自动展开为节点间边并保留颜色、方向、主辅角色和单份标签；选择、删除及批量属性不再跨越节点或误扩为整个网络。
- 2026-08-24：项目升级至 Schema v20；连接网络规范化会清理分流节点合并产生的退化自环，v1–v19 历史载入时同时修复悬空边及其孤立节点。真实 v19 IndexedDB 缓存已恢复打开，其余有效拓扑保留。
- 2026-08-24：项目升级至 Schema v19；从图元锚点或母线发起的接线可落到兼容子线，在 8px 落点拆分实际共享段并创建真实分流节点。节点仅在编辑 hover 时提示，可选择、混合移动/旋转和删除；同点途径点/分流节点自动合并，不兼容类型整次回滚。删除节点移除全部邻接边，复制与监控流向保留内部拓扑。
- 2026-08-24：项目升级至 Schema v18；单选图元的右侧属性面板新增“显示指标名称与单位”实例级总开关，可一次隐藏或恢复全部指标名称与数值单位。关闭后读数、文本状态、告警颜色和无障碍语义保留，组合标签按可见值收紧；旧项目默认显示。
- 2026-08-24：完成子线手动改线首版：所有普通子线段可拖动并创建 8px 途径点；途径点支持单选、追加、框选、删除及与图元/母线混合移动和 90° 旋转，共享主干复用共享点。Schema 升至 v17，Worker/主线程路由、复制、历史、冲突 Toast、孤立点回收和“恢复自动布线”已贯通，等待用户操作与视觉验收。
- 2026-08-20：中键平移改为 SVG/WebGL 共享的命令式会话视口，逐帧只更新 SVG 合成变换和 WebGL Uniform，每 120px 屏幕位移或手势结束时才同步 React 裁剪状态；POD A 在 25% 缩放下的帧时间 P95 由约 50ms 降至 16.8ms，纯平移继续不修改保存状态。
- 2026-08-20：左侧导航/素材栏右边缘外侧新增无背景、无描边的左/右折线箭头收起/展开入口；编辑与监控模式共享当前会话状态，收起后画布获得左栏宽度，不修改项目或保存状态。
- 2026-08-20：移除顶部/左侧标尺与状态栏鼠标 X/Y 坐标，点阵和交互区域扩展到完整画布；中键平移热路径跳过世界坐标、接线判断和无关父层状态更新，真实归档指针到提交延迟 P95 由约 28.9ms 降至 16.6ms。
- 2026-08-19：电力动画改为按每个 Grid、Generator、Battery 实例独立计算到各 Target 的最短路径后取并集，邻近 Battery 不再抢占 Grid→UPS→FM 等较长有效路径；相反方向冲突区间不动画。Generator 运行态与 Battery 充放电门控已记录为后续增量。
- 2026-08-19：电力动画 Source 扩展为 Grid、Generator、Battery，Target 扩展为算力 POD、动力 POD、FM；继续按多源最短有效路径并集裁剪，且 FM 作为终点停止内部传播。
- 2026-08-19：数值指标单位改为以无空格半角括号紧跟标签名称，例如 `有功功率(kW)`；右侧读数列只显示当前数值，不修改 Schema 或已保存单位字段。
- 2026-08-19：项目升级至 Schema v15；运行指标支持数值与文本两类，文本候选状态直接配置正常/次要/重要/紧急等级。编辑模式稳定预览首个正常状态，监控模式按 `90 / 6 / 3 / 1` 等级权重每 30 秒抽样，使模拟数据绝大多数正常；旧 v14 指标自动迁移为数值型。
- 2026-08-19：画布缩放和平移改为纯会话视图状态，不再写项目 Store、本地存储、JSON 或编辑历史，也不标记 dirty；只浏览画布时顶部继续显示“已保存”。
- 2026-08-19：项目升级至 Schema v14并实现图元实例运行指标：每个图元最多 5 项，设备名称与指标组合布局且独立显隐；编辑模式使用模拟范围中间值稳定预览且不刷新，监控模式立即生成随机数值并每 30 秒统一更新。支持上限、下限和区间外三级阈值，告警只给数值使用次要黄、重要橙、紧急红；配置随复制和保存，副本重建指标 ID，派生数值不持久化。
- 2026-08-19：对象剪贴板支持在同一项目的同专业图纸之间保留；`Command/Ctrl+C` 或 `Command/Ctrl+X` 后可切换图纸并用 `Command/Ctrl+V` 在当前视口中心复制或移动图元、母线及选区内部接线，外部支路不复制，跨专业粘贴被拒绝。
- 2026-08-18：工作区收敛为顶部栏、左侧导航/素材栏、中间画布、右侧属性栏四区；删除次级上下文工具栏，画布取消 padding/边框/圆角，返回、编辑/播放、缩放、标题/路径和状态以除按钮外无背景的贴边 HUD 移入画布。
- 2026-08-18：电力流动动画改为只覆盖有效 Grid→算力/动力 POD 最短业务路径并集；无 Target 整链、死端支路及不通往 Target 的母线区间不动画，冷却链路后续单独定义。
- 2026-08-18：监控动画按静态对象线宽分别使用 2px 子线覆盖和 4.5px 母线覆盖；虚线使用可集中手调的中性深灰 `#303238`。2026-08-25 核对历史实现后确认透明度 Token 自首次提交起实际为 `0.5`，此前文档记录的 `0.92` 未实际进入代码。
- 2026-08-18：接线图运行时升级为可取消的增量 Worker 路由、空间索引、显示路径缓存、大图视口裁剪、按需锚点与共享 SVG 颜色滤镜；新增最大真实归档的显式性能基准，不修改项目 Schema。
- 2026-08-18：项目升级至 Schema v13；编辑模式右侧属性面板可控制与监控画布共享的 Switch 独立运行态，Switch 关/开颜色分别配置，旧单色自动迁移到两个状态槽，空选颜色总览按状态槽独立替换。
- 2026-08-18：监控动画按验收改为 Off Switch 两侧相邻线路均停流；WebGL 覆盖由原生细线改为屏幕空间三角带，修复静态线路和拱桥处覆盖不完整。
- 2026-08-18：子线标签改为沿端点相邻线段向内侧展开；横向使用左/右对齐，纵向使用顶/底对齐，并保留 8px 轴向 padding，不越过端点。
- 2026-08-17：项目升级至 Schema v12；独立逻辑子线新增可选端点标签和实例显隐，标签可在源/目标端点切换，并依据局部横线/竖线方向选择上/下或左/右侧。
- 2026-08-17：项目升级至 Schema v11；单选纯母线后可在右侧属性面板独立控制其非空标签显隐，关闭只隐藏文字并保留内容与端点方位，设置随复制、历史、项目与 JSON 保存。
- 2026-08-17：项目升级至 Schema v10；单选普通图元后可在右侧属性面板独立控制其标签显隐，默认显示，设置随图元复制、历史、项目与 JSON 保存；早期图纸级顶部开关方案已撤回。
- 2026-08-17：项目升级至 Schema v9；母线新增可选文字标签，默认隐藏，填写后只显示在起点或终点外侧并保持 8px 视觉间距，支持单选后拖动切换端点并随复制、历史和存储持久化。
- 2026-08-17：子线支持跨网络追加多选，并可与一条或多条母线组成混合选择；右侧属性面板通过同一 HEX 字段批量预览、提交、恢复默认和撤销。
- 2026-08-17：空选属性面板新增画布颜色总览，按图元、母线、子线汇总实际颜色和数量；点击颜色可单事务全局替换当前画布内同类别同色对象。
- 2026-08-17：颜色入口改为应用内 HEX 选择器，修复子线重新选择回显和网络多色状态；母线 tap 与候选接入点改为继承所属母线颜色。
- 2026-08-17：项目升级至 Schema v8，完成图元设备标识始终显示、自动四侧避让和单选后二阶段标签拖动；标签内容继续由右侧属性面板编辑，旋转不改变文字方向或标签侧向，具体设计见 `ELEMENT_LABEL_DESIGN_BRIEF.md`。
- 2026-08-17：项目升级至 Schema v7；母线与逻辑子线新增可选颜色持久化，属性面板默认显示可编辑 HEX 值，并支持网络选择批量改色、轻量预览和单步撤销重做。
- 2026-08-17：非连接交叉改为真圆弧与合并局部底衬，可见/命中路径同源；监控电流/水流动画只完成 WebGL 批量覆盖、显示路径复用和性能降级文档约束，本轮未写动画代码。
- 2026-08-17：监控模式首轮已实现顶部模式 Tab、只读画布、Switch 独立自动保存、Grid→算力/动力 POD 业务拓扑定向、Switch 下游门控和单批次 WebGL 深色移动虚线；冷却业务方向、遥测模拟及图表保留为后续增量。
- 2026-08-14：接线升级为 Schema v6 同锚点多线与网络级共享路径；移除普通子线分叉入口和可见分叉点，旧固定分叉自动迁移为端点逻辑连接；77 项单元/组件测试与 3 条无截图浏览器流程通过。
- 2026-08-14：完成不同母线显式普通子线互联、选中母线 hover 空心起点、双端 tap 联合求优、多子线避让和网络删除拆分；68 项单元/组件测试与 3 条无截图浏览器流程通过。
- 2026-08-14：将已建立母线 tap 改为更小的无描边实心点。
- 2026-08-14：当时完成母线 tap 默认自动滑移、锚点方向感知与完整路径评分优化；其中自动滑移已于 2026-09-01 被 KD-106 取代，锚点方向评分继续有效。
- 2026-08-14：完成 Schema v5 电力母线、隐式 tap 连通、母线编辑、普通接线接入、交叉拱桥和无截图自动化验证，进入用户视觉与操作验收。
- 2026-08-14：完成线路连接与自动路由首轮、Schema v4、分叉和拱桥实现及无截图自动化验证，进入用户视觉与操作验收。
- 2026-08-14：第一阶段通过用户验收，确认线路连通网络、六种类型、正交避障、显式分叉和未连接交叉拱桥方案，进入接线实施。
- 2026-08-13：完成图元锚点编辑器、项目模式 v2、v1 迁移及无截图自动化验证，进入用户视觉与操作验收。
- 2026-08-13：根据首次验收反馈将界面改为中性深色极简，画布改用滚轮缩放和中键平移，左树改为单行，并归档被取代的浅色方向。
- 2026-08-13：完成第一阶段基础编辑实现与非视觉自动化验证，新增验收清单和设计系统记录，等待用户视觉与操作验收。
- 2026-08-13：确认第一阶段技术栈、本地优先持久化和工程制图台界面方向，进入实施阶段。
- 2026-08-12：补充目标用户、Web 交付、层级下钻、完整基础编辑能力、JSON 导入导出及待确认技术方案。
- 2026-08-12：记录双模式、冷却/电力双线路、多层级图纸及第一阶段编辑范围；专业名称于 2026-08-13 按 KD-022 统一。
- 2026-08-12：初始化项目文档系统和项目级持久协作规则。
