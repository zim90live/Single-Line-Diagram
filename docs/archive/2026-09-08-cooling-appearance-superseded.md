# 制冷旧材质验收条目归档

日期：2026-09-08。以下未完成条目的材质、宽度与动画表现由 KD-164 取代；拓扑、门控、流量、编辑与存档约束继续有效，汇总到当前 TODO 验收。

| 优先级 | 原状态 | 原任务 | 原完成定义 |
| --- | --- | --- | --- |
| P1 | 待用户验收 | 验收冷却管路选择态与主要/辅助视觉层级 | 在编辑模式分别单选冷却独立支路、追加多选及点击共享主干选择网络，确认主要和辅助线路均出现连续清晰的 `6px #B7B8B5` 中性高亮轨道，圆角、拱桥和共享段不因逻辑边重叠而变亮；电力线路选择态不变。用右侧“管路级别”把单条和批量冷却子线设为辅助，确认混合选择显示“多种级别”；主要线路为 `10px / 2px` 管体/内芯、`#262626 / 50%` 内阴影，辅助线路为 `4px / 1px`、`#1D1F20 / 20%` 内阴影，辅助内芯色为 `20%` 原回路/实例色与 `80% #70736F`。恢复主要后精确恢复原色和材质，同一共享区间含主要与辅助引用时主要样式应覆盖辅助样式。再检查路由、命中、方向箭头、节点与电力/母线面板边界不变，编辑撤销/重做、复制、节点拆分/收缩、保存/刷新和 JSON 往返保留角色；监控模式使用对应内芯宽度和低对比色，但动画资格、方向、速度与流量不变，无流量区间继续基于角色显示色压黑 |
| P1 | 待用户验收 | 验收电路与水路无流动线路样式 | 在电力与制冷图纸的监控模式分别检查无流动普通子线、电力母线和制冷内芯，确认均直接呈现为自身原色与黑色各 50% 混合后的暗色，色相保留、明度降低；移动虚线仍使用既有中性深灰。放大检查压暗线的抗锯齿边缘不再透出原业务色，并检查制冷圆角、分叉和共享段没有鳞片、棋盘格、接缝或局部过黑。部分流动逻辑边只替换静止区间。播放时有流动段保留业务色并显示移动虚线，暂停后有流动段仍保留业务色、无流动段继续变暗；编辑模式视觉保持不变 |
| P1 | 待用户验收 | 验收制冷闭合回路动画与视觉流量控制 | 在锚点编辑器把专用素材分别标记为水泵、阀门或止回阀，并为水泵/止回阀配置唯一入口、出口。进入监控模式选择泵阀，确认右侧运行面板可启停、开关并设置水泵 `0–100%` 输出功率，画布对象仍不可移动。检查 `100% / 50% / 0%` 分别对应 `100 / 50 / 0 m³/h`，流量指标与动画速度同步；等价并联支路速度一致，汇流与多泵对冲按净流量连续表达。再确认 2WV/CV 状态保存、CV 只允许入口→出口、反向单向子线阻断、关阀/开路/死端静止；水泵启停/输出功率应产生 dirty 并在保存往返后保留，但不产生撤销记录，派生流量和动画状态不产生 dirty。动画沿主要 `10px / 2px`、辅助 `4px / 1px` 的管体/世界空间内芯、普通圆角与 `16 × 8px` 拱桥连续；在最低、100% 与最高缩放下，移动虚线和无流动深色内芯的粗细都与对应原内芯一致，电力线路宽度仍保持固定屏幕效果 |
| P1 | 待用户验收 | 验证制冷管体重叠修复与视觉微调 | 用户复验截图中的阀门/锚点上下管路，确认主要线路 `10px / 2px`、辅助线路 `4px / 1px` 的平头管体/内芯均直接截止并完整连接于锚点中心；检查普通 90° 拐角为 `8px` 圆角，短段能自动收紧，尤其确认持续绘线和节点分段产生的两条相邻子线在自由二连节点处仍连续圆角，而 T 形、十字及三方向转角分叉保持直角。再检查同网络共线公共段和交汇节点只有一层管体与一次内阴影，没有灰色鼓包、接缝或重复亮边；制冷交叉拱桥为 `16 × 8px`，主要 `18px`、辅助 `12px` 遮罩均能完全隔开下方管路，紧邻拱桥弯点不互相挤压，同时确认电力拱桥仍为 `8 × 4px`、监控制冷拱桥与编辑态同为 `16 × 8px`。补充检查接线预览圆角与平头、最低/最高缩放、选择与 `16px` 点击/拖段命中；保存数据应保持不变 |
