## Design Context

### Users

AIDC 运维人员在桌面 Web 工作区中创建、维护并最终监控冷却与电力一次接线图。核心任务是跨园区、楼宇、POD、设备层级快速定位图纸，准确排布图元，并在长时间操作中保持清晰和可控。

### Brand Personality

中性、克制、精密。界面应像安静可靠的运行台，而不是通用后台、营销页面或高饱和“科技大屏”。

### Aesthetic Direction

沿用 AIDC 项目的中性深色极简方向：连续炭黑工作面、低对比分隔、Google Sans Code 界面字体、克制的冷却/电力语义色。画布是第一视觉焦点，图元只显示 SVG 本身；避免装饰卡片、霓虹、渐变文字和无意义动效。布局、质感和视觉验收由用户完成。

### Design Principles

1. 画布优先：页面只保留顶部栏、左侧导航/素材栏、中间画布和右侧属性栏四个一级区域。画布标题、层级返回、编辑/播放命令、缩放和状态信息使用紧贴标尺后可绘图区边缘的极简 HUD；只有按钮保留控件背景，其余信息透明呈现，不再增加次级工具栏或卡片外壳。
2. 精确优先：网格、标尺、选择和变换反馈必须服务于准确编辑。
   图元产生实际网格位移后使用贯穿画布的中性十字对齐带辅助定位；多选成员各自显示选中轮廓并共享整体变换框。所有结果不改变 8px 吸附规则。
3. 语义用色：彩色只表达冷却、电力、用户明确设置的线路对象颜色或运行状态，普通交互保持中性；颜色编辑使用应用内 HEX 控件，母线 tap 跟随母线颜色；Switch 关/开颜色是两个独立的实例配置槽，并随当前运行态呈现；多条子线与母线/子线混合选择使用统一的批量线路颜色入口，空选属性面板按对象类型与 Switch 状态槽汇总实际颜色并提供同类同槽同色的全局替换。
4. 组件边界清晰：Token/Recipe 与 UI 原语拥有视觉规则，业务组件拥有布局与状态。
5. 渐进增强：先呈现 SVG 原始内容，名称、背景、监控状态和动画按明确需求增量加入。
6. 拓扑可读：接线锚点只在图元 hover 或接线状态出现；普通交叉使用克制的拱桥表示不连通，显式分叉使用实心节点。线路保持 8px 正交路径，类型色只表达电力与冷却回路语义；子线标签只绑定逻辑端点与局部线段侧向，沿线段向子线内侧展开且不越过端点，不保存自由坐标或遮盖共享主干语义。监控移动虚线只表达有效 Source→Target 流动，不把单纯可达、带电或 Switch 闭合表现为实际流量。
7. 熟悉操作优先：跨图纸搬移复用 `Command/Ctrl+C`、`Command/Ctrl+X`、`Command/Ctrl+V`，不新增弹窗、浮层或工具栏层级；兼容性错误使用既有 Toast，目标落点以当前视口为上下文。
