# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

已确认：React + TypeScript + Vite；Three.js / React Three Fiber 正交 WebGL 场景绘制网格，SVG 覆盖层承担图元与编辑交互；Zustand 管理应用状态；Zod 校验版本化 JSON；Dexie/IndexedDB 负责本地项目存储；AIDC typed Token / Recipe 与自有 React UI 原语构成界面基线；Vitest、Testing Library 和 Playwright 非截图断言负责自动化验证。

## Users

主要用户为 AIDC 运维人员。他们在桌面环境中创建、整理和维护冷却、电力两套线路的多层级图纸，后续还会在同一产品中查看运行监控信息。

## Product Purpose

建设编辑、监控双模式的 AIDC 一次接线图系统。产品先交付可用的图元编辑能力，再扩展线路连接与管理，最终接入监控数据。

成功意味着运维人员能够稳定创建、保存、打开和交换多层级图纸，并高效完成图元布局操作；后续连接与监控能力不需要推翻第一阶段的数据模型。

## Positioning

产品以冷却、电力双线路和园区、楼宇、POD、设备多层级下钻为核心组织方式，将一次接线图编辑与未来运行监控统一在同一图纸模型上。

## Operating Context

- 交付形态为 Web 系统，第一阶段按桌面端操作设计。
- 冷却、电力线路分别包含园区、楼宇、POD、设备等层级图纸。
- 层级图纸通过下钻和返回关联；冷却/电力分组及含子图节点可收起/展开，编辑模式还可新增、重命名，并在同一线路内拖动调整父级或同级顺序。
- 通过层级树、面包屑、返回或管理操作进入图纸时，画布自动居中看全该图纸的图元、母线和独立线路节点；之后的缩放和平移仍只属于当前会话。
- 第一阶段界面使用中性深色、极简的桌面运维工作台；左侧图纸树节点保持单行。
- 用户从素材区拖入图元，在画布内完成选择、变换和排布。
- 项目需要新建、保存、打开，并支持 JSON 文件导入和导出。

## Capabilities and Constraints

- 第一阶段包含：拖入图元、单选、多选、框选、旋转、移动、缩放、贴齐网格、顶部/左侧标尺、撤销/重做、复制/粘贴、删除和键盘快捷键。
- 画布支持触控板双指平移与指针中心捏合缩放；离散鼠标滚轮继续缩放，按住鼠标中键拖动或按住空格+左键也可平移；其他时候左键保留给选择、框选和图元编辑。
- 图纸切换自动适配保留屏幕安全留白，小图不强制放大，空图回到标准视窗；适配与其他视口操作不触发项目保存、dirty 或撤销历史。
- 第一阶段验收通过后，才实施线路连接和线路管理。
- 监控模式属于后续阶段。
- 当前活动图元来自 `src/assets/symbols/`，用户后续仍可继续导入素材；新增素材必须显式登记，不能假定最终清单已经封闭。
- JSON 必须带模式版本并在导入时校验，防止无效文件破坏项目状态。
- 第一阶段采用本地优先存储：浏览器 IndexedDB 保存项目，JSON 用于备份和跨机器交换；存储能力通过接口隔离，后续可替换为服务端实现。
- 冷却与电力使用独立图纸树和独立画布；线路根图固定，图纸管理不得跨线路、形成循环或超过园区/楼宇/POD/设备四层。编辑模式可直接删除非根图纸及其完整子树并清理对应画布内容，监控模式继续只读。典型/最大图元数量和正式浏览器兼容矩阵尚未最终确认。
- 图元锚点属于资产模板，只能位于非角点的边缘8px网格点；当前只定义锚点，不实现线路连接。

## Evidence on Hand

- 当前活动素材来自 `src/assets/symbols/` 中登记的21个SVG；新增素材仍需按目录约定登记稳定键、分类和原始尺寸。
- 第一阶段实现包含双线路四层默认图纸树、R3F/Three GLSL点阵、SVG编辑覆盖层、资产级锚点编辑器、IndexedDB Repository 和项目模式v2；它们构成当前可运行验收基线。
- 当前自动化证据包括38项Vitest/Testing Library测试、TypeScript检查、生产构建及1项无截图真实浏览器完整流程。
- 当前没有真实监控数据、协议、告警规则或性能规模数据；后续工作不得编造。

## Product Principles

- 分阶段交付：先把编辑体验做完整并验收，再进入连接和监控。
- 层级清晰：任何图纸都能明确知道所属线路、当前层级和上下级关系。
- 数据可迁移：项目 JSON 可校验、可升级、可跨环境交换。
- 模型可扩展：第一阶段不实现连线和监控，但数据模型为端口、边和运行状态保留演进空间。
- 操作高效：常用动作同时提供直接操作、工具栏和键盘快捷键。
