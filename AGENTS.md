# 项目记忆与协作约定

## 项目背景

- 本项目用于创建 AIDC 一次接线图绘制/监控系统。
- 项目目标、关键决策、TODO 与历史记录统一维护在 `docs/`，入口见 `docs/README.md`。

## 不主动进行视觉检查

- 除非用户明确要求，否则不要主动使用 Computer Use、浏览器截图、Playwright 截图、`view_image` 或其他截图/图像查看方式做视觉检查或视觉验收。
- 所有视觉检查与视觉验收默认交给用户；需要视觉结论时，应请用户检查并提供反馈。
- 可以继续执行与视觉无关的自动化检查，例如类型检查、单元测试、构建、静态分析、DOM/数据断言和文件结构检查。

## 文档与实现同步

- 开始会影响产品范围、架构、数据模型或用户流程的工作前，先阅读 `docs/PROJECT_GOALS.md`、`docs/KEY_DECISIONS.md` 和 `docs/TODO.md`。
- 每次实现或需求变更后，同步更新相关文档：目标变化写入项目目标；形成或改变约束时写入关键决策；工作状态写入 TODO；已失效或阶段性记录移入历史归档。
- 不要把仍然有效的目标或决策直接删除；被替代的决策应标记为“已取代”，注明替代它的决策编号，并在归档中保留上下文。
- 如果发现请求、方案或实现与现有项目目标或关键决策冲突，应在执行冲突性改动前明确提醒用户，指出冲突内容及影响；用户确认新方向后，再更新文档并实施。
- 完成任务时，在最终说明中简要列出同步过的文档；若无需更新文档，也要确认本次没有产生目标、决策或 TODO 变化。

## 文档维护原则

- 文档使用简体中文，文件名和稳定标识可使用英文。
- 记录结论、原因、影响和日期，避免只记录讨论过程。
- `docs/TODO.md` 只保留当前有效的工作视图；已完成或取消的阶段性明细定期归档到 `docs/archive/`。
