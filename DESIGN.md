---
name: AIDC 一次接线图
description: 沿用 AIDC 项目的中性深色、高密度运维工作台设计系统
colors:
  canvas: "#141518"
  canvas-deep: "#101114"
  panel: "rgba(27, 29, 32, 0.96)"
  panel-raised: "rgba(35, 37, 41, 0.98)"
  ink: "#f2f3ef"
  muted: "#b6b8b4"
  dim: "#858784"
  accent: "#f0f1ed"
  cooling: "#77b4bf"
  electrical: "#d5b96f"
  success: "#8eac94"
  warning: "#d9a464"
  danger: "#df816f"
typography:
  interface:
    fontFamily: "Google Sans Code Variable, Google Sans Code, PingFang SC, Microsoft YaHei, ui-monospace, monospace"
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1.35
  label:
    fontFamily: "Google Sans Code Variable, Google Sans Code, PingFang SC, Microsoft YaHei, ui-monospace, monospace"
    fontSize: "10px"
    fontWeight: 500
    lineHeight: 1.4
rounded:
  small: "6px"
  control: "8px"
  panel: "12px"
  pill: "999px"
spacing:
  xs: "2px"
  sm: "8px"
  md: "12px"
  lg: "16px"
components:
  button:
    height: "28px"
    rounded: "{rounded.control}"
  field:
    height: "32px"
    rounded: "{rounded.control}"
  status-tag:
    height: "22px"
    rounded: "{rounded.pill}"
---

# Design System: AIDC 一次接线图

## 设计真源

- `design-system/theme.json` 是活动 Token 与 Recipe 的唯一真源；`npm run generate:theme` 确定性生成 `src/styles/theme.generated.css`。
- `src/components/ui/` 只承载 Button、IconButton、TextField、SelectField、NumericField、StatusTag 与无样式 Pressable 等视觉原语。
- 图纸树、素材库、属性面板、对话框和画布工具属于业务组件，只能组合原语和 Token，不得复制原语内部视觉规则。
- 图标统一使用 Lucide。项目不再使用 Ant Design 组件或主题。

## 视觉方向

Creative North Star 是“安静的运行台”。工作区由连续的中性深色表面、低对比分隔和紧凑的等宽界面字体构成。画布和图纸内容是第一视觉焦点，外壳不使用营销式大标题、装饰插画、霓虹光效或无意义卡片。

- 常规交互保持近无彩色；冷却、电力、成功、警告和危险色只表达语义。
- 持久文本不小于 10px；项目名、面板标题和关键状态建立清晰但克制的层级。
- 固定工作区依靠一像素边界和表面明度分层；阴影只用于需要脱离工作面的对话框、侧滑面板和 Toast。
- 6px、8px、12px 分别用于小表面、控件和弹层；药丸形只用于短状态标签。
- 动画只用于控件状态和未来监控数据，不为静态装饰制造动效；需遵循 `prefers-reduced-motion`。
- 编辑/监控模式使用顶部紧凑双 Tab，监控播放控件位于上下文工具栏；监控画布隐藏素材和属性编辑区，保留层级导航及最大画布面积。编辑模式单选 Switch 时，右侧属性面板复用紧凑 switch 控制其独立运行态。

## 工作区布局

- 顶部 52px 项目栏：品牌、项目名、保存状态和文件操作。
- 次级 42px 工具栏：返回、线路状态、面包屑和编辑命令。
- 主区三列：260px 层级/素材栏、弹性画布、288px 属性栏；桌面最小宽度 1180px。
- 左侧树每一行固定 28px 且永不换行；长名称省略，层级类型保留。
- 新功能优先进入既有工具栏或侧栏，不在画布上覆盖与图纸无关的卡片。
- 图元锚点编辑器使用大型双栏原生模态框：260px 素材浏览栏与弹性8px锚点画布；顶部集中名称、类型和图标命令，弹窗无底部操作区。

## 画布表达

- WebGL 层绘制自适应单级点阵：8px 是最小世界单位，缩小时按 16、24、32px 等 8px 倍数降低显示密度；所有点使用相同大小、颜色和透明度，不使用连续网格线，并为后续运行态动画预留批处理能力。
- SVG 层绘制图元、标签、选择框、框选区域和变换控制柄；顶部/左侧标尺随视口同步。
- 滚轮以指针为中心缩放；鼠标中键平移；左键只负责选择、框选和编辑。
- 画布内图元拖动只在产生实际 8px 网格位移后，在 SVG 交互层、图元下方显示中性半透明十字对齐带；多选拖动时每个图元都有一组，合并裁剪后只统一着色一次。
- 多选状态下，每个图元使用中性细实线表达成员选中态，外层整体包围框使用既有虚线和缩放/旋转手柄；各图元按自身显隐属性继续显示或隐藏既有标签，但不增加额外的选择文字或浮层。
- 图元 SVG 本身不添加名称、背景、滤镜或卡片外壳；旁侧设备标识使用无背景的 10px 世界空间水平文字和克制的画布色描边，随画布缩放。标签不进入图元尺寸、框选、吸附、变换包围框或线路障碍。用户提供的 SVG 可在素材缩略图内使用浅色衬底，画布不形成浅色卡片。
- 设备标识以 2px 默认视觉间距贴近图元，并在画布坐标的四侧使用独立于 8px 主网格的 2px 精细步进自动避让锚点出线区域；只有恰好单选一个普通图元时才可拖动标签改变侧向。图元旋转不旋转文字，也不改变标签侧向；多选时标签不响应指针。
- 单选普通图元时，右侧属性面板在设备标识下使用紧凑 switch 控制该图元标签；默认开启，关闭后不生成该图元标签布局或交互命中区域。显隐状态随图元历史、复制和保存，其他图元与母线标签不受影响；顶部工具栏不放置标签显隐入口。
- 母线默认没有标签文字，填写非空内容且当前母线的右侧属性开关开启后，才复用设备标识的 10px 世界空间文字样式显示；关闭开关保留文字与端点方位。位置只允许在母线起点或终点的线段外侧，与端点保持 8px 专用视觉间距，默认终点。只有恰好单选一条纯母线时才可拖动切换端点；文字不旋转，也不参与母线几何和线路拓扑。
- 独立逻辑子线默认没有标签文字；单选一条纯子线后可在右侧填写并用实例开关显隐，非空标签复用同一 10px 水平世界空间文字。标签只绑定源或目标端点与局部线段侧向：横向端点段可放上/下，纵向端点段可放左/右，文字边界与线路保持 8px 视觉间距，默认目标端点负侧。文字沿端点相邻线段向子线内侧展开，横向使用左/右边界对齐与 8px 水平 padding，纵向使用顶/底边界对齐与 8px 垂直 padding，不越过端点。拖动只在四个端点侧候选间切换，不保存绝对坐标、不触发逐帧路由，也不参与线路障碍或拓扑。
- 同一锚点的多条线路共享系统派生的共同路径；自动分叉不绘制连接点、候选锚点或手柄，普通线路也不作为接线目标。
- 独立支路选中时只强调对应逻辑连接；共同主干选中时统一强调整个线路网络。`Shift` / `Ctrl` / `Command` 单击允许追加或移除跨网络子线，也允许母线与子线共同保持选中；普通图元仍与线路选择互斥。不同网络的非连接交叉使用带局部画布色底衬的真圆弧拱桥；底衬按线路合并，不使用滤镜、阴影或逐交叉 DOM。
- 监控流动覆盖使用独立 WebGL 屏幕空间三角带：普通子线为 2px，母线为 4.5px；沿静态线路与拱桥连续显示 `#303238`、0.92 透明度的中性深灰 6px/6px 移动虚线。相邻采样段轻微搭接，避免露出未覆盖的彩色边缘；Off Switch 的两侧直接相邻子线均不显示动画。

## 组件与状态

- 主按钮使用浅色实底，仅用于当前上下文的主要动作；普通工具使用中性软底或透明底。
- 输入框高 32px，标签置于字段上方；属性提交以 blur/Enter 为事务边界，Escape 取消草稿。
- 对象标签显隐使用既有紧凑 switch，且只在可明确归属的单对象选择中出现；共享主干、多子线或母线/子线混合选择不显示含义含混的批量标签入口。
- 图元、母线和子线颜色统一使用属性面板内的 HEX 选择器，不调用系统原生 RGB/HEX 面板；展开态只提供饱和度/亮度、色相与 HEX 值。连续调整只原位预览 SVG 颜色，不更新项目、历史或线路路由；“完成”或 HEX 输入失焦后单次提交，“取消”恢复已保存值。Switch 单选时分别显示“关状态颜色”和“开状态颜色”，只预览当前运行态对应的颜色，另一状态的草稿不改变当前画面。多条子线或母线/子线混合选择共用一个“线路颜色”字段和一次历史事务；选择内存在不同颜色时明确显示“多种颜色”。
- 画布未选择对象时，属性面板按图元、母线、子线分组列出当前实际使用的颜色与对象数量；Switch 关/开颜色作为两个明确的图元颜色槽独立汇总。点击颜色后复用同一 HEX 选择器，只替换当前画布内同一类别、同一颜色槽且当前实际颜色相同的对象。默认色与自定义色均参与汇总，不支持实例改色的原始 SVG 图元不列入颜色总览。
- Switch 运行态由编辑属性面板与监控画布共享，立即写入独立运行态存储；切换状态不修改项目 dirty、JSON 或撤销历史。关/开颜色属于图纸实例配置，进入项目历史、复制和持久化。
- 母线 tap 与候选接入点继承所属母线颜色；子线颜色独立保存，不反向改变 tap 或母线。
- 锚点编辑属于即时应用例外：名称与类型直接更新项目并进入弹窗内撤销历史；关闭只退出编辑，不回滚修改。
- 冷却与电力只在短状态标签中使用对应语义色；普通选中和焦点保持中性白。
- 禁用、悬停、选中和错误必须同时通过边界/明度或文字表达，不得只依赖颜色。

## 约束

- 不新增第二套组件库、第二套图标库或散落的颜色/圆角常量。
- 不让树节点、面包屑、状态栏和素材名称换行。
- 不把 Three.js/R3F 临时对象或高频监控数据写入项目 JSON。
- 不主动以截图作视觉结论；布局、质感和图元显示效果由用户验收。
