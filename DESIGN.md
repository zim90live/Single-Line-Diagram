---
name: AIDC 一次接线图
description: 沿用 AIDC 项目的中性深色、高密度运维工作台设计系统
tokenSource: design-system/theme.json
componentManifest: design-system/component-manifest.json
---

# Design System: AIDC 一次接线图

## 设计真源

- `design-system/theme.json` 是活动 Token 与 Recipe 的唯一真源；`npm run generate:theme` 确定性生成 `src/styles/theme.generated.css`。
- `src/components/ui/` 承载 Button、IconButton、TextField、NumericField、SelectField、Switch、SegmentedControl、Tab、StatusTag 九个视觉原语，以及 Pressable、TabList 两个语义辅助组件；统一从 `index.ts` 导出。
- `src/styles/components.css` 是基础组件结构、变体和交互状态的唯一视觉实现；`src/styles.css` 只承载 `legacy` 业务 Pattern。
- 图纸树、素材库、属性面板、对话框和画布工具属于业务组件，只能组合原语和 Token，不得复制原语内部视觉规则。
- 图标统一使用 Lucide。项目不再使用 Ant Design 组件或主题。

## 视觉方向

Creative North Star 是“安静的运行台”。工作区由连续的中性深色表面、低对比分隔和紧凑的等宽界面字体构成。画布和图纸内容是第一视觉焦点，外壳不使用营销式大标题、装饰插画、霓虹光效或无意义卡片。

- 常规交互保持近无彩色；冷却、电力、成功、警告和危险色只表达语义。
- 持久文本不小于 10px；项目名、面板标题和关键状态建立清晰但克制的层级。
- 工作区以全屏画布为底，左右悬浮面板统一采用 AIDC 监控模式的单色半透明表面和 30px 背景模糊，不使用面板阴影或常驻描边。
- 6px、8px、12px 分别用于小表面、控件和弹层；药丸形只用于短状态标签。
- 动画只用于控件状态和未来监控数据，不为静态装饰制造动效；需遵循 `prefers-reduced-motion`。
- 编辑/监控模式使用顶部紧凑双 Tab；监控播放控件与编辑命令均位于画布内贴边 HUD。监控画布隐藏素材和属性编辑区，保留层级导航及最大画布面积。编辑模式单选 Switch 时，右侧属性面板复用紧凑 switch 控制其独立运行态。

## 工作区布局

- 主画布绝对铺满整个应用，顶部栏和左右面板只作为 HUD 覆盖在画布上，不再以网格列切走绘图面积。
- 顶部栏高 64px，使用 30% 深色背景、18px 模糊与轻阴影；其余悬浮面板统一距离视口边缘 4px。
- 编辑与监控业务共用从顶栏下方开始的 280px 左侧面板，以及 360px / 24px 内边距的常规右侧检查器；两者延伸至底部 4px。监控设备详情面板是受控例外：宽 500px、内边距 24px，其他材质、圆角与边距不变。
- 返回使用独立 44px 无背景按钮，路径与对象统计直接跟随，不形成路径面板；编辑/播放和缩放命令使用 6px 无阴影监控浮层。左侧折叠只隐藏覆盖层并移动入口，不改变画布尺寸或会话视口。
- 两种业务模式的树行均为 28px，使用同一组监控 SVG、无边框搜索和 40%/76%/100% 文字图标层级；长名称省略。冷却/电力标题整行可折叠，含子图节点用左侧参考箭头收起/展开；叶节点保留等宽空槽。编辑业务的新增、重命名、拖放和删除继续可用，但操作入口使用监控工具组样式；监控业务保持只读。
- 新功能优先进入现有悬浮面板或画布 HUD，不新增会压缩画布的横向布局层。
- 图元锚点编辑器使用大型双栏原生模态框：260px 素材浏览栏与弹性8px锚点画布；顶部集中名称、类型和图标命令，弹窗无底部操作区。

## 画布表达

- 主画布使用纯黑 `#000000` 背景；WebGL 与 CSS 回退层保持一致。WebGL 层绘制自适应单级点阵：8px 是最小世界单位，缩小时按 16、24、32px 等 8px 倍数降低显示密度；所有点使用相同大小、颜色和透明度，不使用连续网格线，并为后续运行态动画预留批处理能力。
- SVG 层绘制图元、标签、选择框、框选区域和变换控制柄；不挂载顶部/左侧标尺或鼠标坐标显示。
- 画布容器无外层 padding、边框和圆角，点阵直接铺满整个应用；HUD 只覆盖画布，不改变画布尺寸、视口坐标或鼠标手势。
- 离散鼠标滚轮与触控板捏合均以指针为中心缩放；触控板双指移动、鼠标中键、空格+左键均可平移，其他时候左键只负责选择、框选和编辑。三种平移手势共用共享命令式视口，逐帧更新 SVG 合成层与 WebGL Uniform，不触发逐输入事件的 React 全树重渲染；位移达到 120px 屏幕距离或手势结束时才同步 React 视口，用于更新 overscan 裁剪集合和最终会话状态。捏合与其他缩放继续按帧更新 React 视口，以同步屏幕空间表达。
- 进入或跳转到一张图纸时，视窗按旋转后图元、母线和自由线路节点的完整业务边界居中适配一次，并保留 48px 屏幕留白；小图不放大超过 100%，空图恢复标准视窗，超大图以看全优先。适配不以异步路由结果、同图内容编辑、侧栏尺寸变化或模式切换为触发源，避免镜头二次跳动或打断用户后续观察。
- 画布内图元拖动只在产生实际 8px 网格位移后，在 SVG 交互层、图元下方显示中性半透明十字对齐带；多选拖动时每个图元都有一组，合并裁剪后只统一着色一次。
- 多选状态下，每个图元使用中性细实线表达成员选中态，外层整体包围框使用既有虚线和缩放/旋转手柄；各图元按自身显隐属性继续显示或隐藏既有标签，但不增加额外的选择文字或浮层。
- 图元 SVG 本身不添加名称、背景、滤镜或卡片外壳；旁侧设备标识使用无背景的 10px 世界空间水平文字和克制的画布色描边，随画布缩放。标签不进入图元尺寸、框选、吸附、变换包围框或线路障碍。用户提供的 SVG 可在素材缩略图内使用浅色衬底，画布不形成浅色卡片。
- 通用图元是受控例外：使用近黑背景、白色框内设备标识和默认灰色可改色虚线描边。单选时右侧复用紧凑 switch 控制当前实例的虚线显隐；关闭只把描边透明度降为零，背景、标识、锚点、运行指标、图元命中区域和已配置颜色保持。
- 设备标识以 2px 默认视觉间距贴近图元，并在画布坐标的四侧使用独立于 8px 主网格的 2px 精细步进自动避让锚点出线区域；只有恰好单选一个普通图元时才可拖动标签改变侧向。图元旋转不旋转文字，也不改变标签侧向；多选时标签不响应指针。
- 单选普通图元时，右侧属性面板在设备标识下使用紧凑 switch 控制该图元标签；默认开启，关闭后不生成该图元标签布局或交互命中区域。显隐状态随图元历史、复制和保存，其他图元与母线标签不受影响；顶部工具栏不放置标签显隐入口。
- 普通图元与节点间子线可在右侧属性面板独立配置最多 5 项数值或文本运行指标及“显示运行数据”实例开关；手动纯图元或纯子线多选时使用整套模板批量覆盖。编辑模式使用模拟范围中间值或首个正常文本状态稳定预览且不刷新，监控模式使用实时派生值；两种模式中对象名称和指标均共用同一个无背景世界空间标签组，任一内容关闭时只移除对应行。数值单位以无空格半角括号紧跟指标名称，例如 `有功功率(kW)`，右侧只保留数值读数；多条指标共用统一的右对齐数值列，最宽名称行与数值列使用 0px 布局间距，短名称行不打破列对齐。名称和单位保持中性，仅数值读数或文本状态本身按正常中性、次要黄、重要橙、紧急红着色，不显示告警等级文字或图标。
- 母线默认没有标签文字，填写非空内容且当前母线的右侧属性开关开启后，才复用设备标识的 10px 世界空间文字样式显示；关闭开关保留文字与端点方位。位置只允许在母线起点或终点的线段外侧，与端点保持 8px 专用视觉间距，默认终点。只有恰好单选一条纯母线时才可拖动切换端点；文字不旋转，也不参与母线几何和线路拓扑。
- 独立逻辑子线默认没有标签文字；单选一条纯子线后可在右侧填写，并用“显示子线标签”“显示运行数据”“显示指标名称与单位”三个开关分别控制名称、指标行和指标标题。非空名称及最多 5 项指标复用同一水平世界空间组合标签。标签组只绑定源或目标端点与局部线段侧向：横向端点段可放上/下，纵向端点段可放左/右，边界与线路保持 8px 视觉间距，默认目标端点负侧。内容沿端点相邻线段向子线内侧展开，横向使用左/右边界对齐与 8px 水平 padding，纵向使用顶/底边界对齐与 8px 垂直 padding，不越过端点。拖动只在四个端点侧候选间切换，不保存绝对坐标、不触发逐帧路由，也不参与线路障碍或拓扑。
- 同一锚点的多条线路共享系统派生的共同路径；自动分叉不绘制连接点、候选锚点或手柄，普通线路也不作为接线目标。
- 独立支路选中时只强调对应逻辑连接；共同主干选中时统一强调整个线路网络。`Shift` / `Ctrl` / `Command` 单击允许追加或移除跨网络子线，也允许母线与子线共同保持选中；普通图元仍与线路选择互斥。不同网络的非连接交叉使用带局部画布色底衬的真圆弧拱桥；底衬按线路合并，不使用滤镜、阴影或逐交叉 DOM。纯子线单选或多选时，右侧属性面板提供整条节点间子线的“下层 / 自动 / 上层”跨线层级；混合值明确显示“多种层级”，真正节点/分叉和子线跨母线固定规则不受影响。
- 监控流动覆盖使用独立 WebGL 屏幕空间三角带：普通子线为 2px，母线为 4.5px；沿静态线路与拱桥连续显示 `#303238`、0.92 透明度的中性深灰 6px/6px 移动虚线。相邻采样段轻微搭接，避免露出未覆盖的彩色边缘。移动虚线只表达有效电力 Source→Target 业务流动，不表达单纯带电；无 Target 整链、死端支路和 Off Switch 两侧均不动画。

## 组件与状态

- 监控渲染输入统一通过 `DiagramRuntimeView` 与 `DiagramRuntimeContext` 进入：前者只含当前图纸、资产、线路和下探映射，后者承载 Switch/泵阀状态、外部供电与可替换数据 Provider。跨项目交付使用独立版本的 RuntimeBundle v1，按入口图纸裁剪下级子树和必要祖先，冻结下探并固化 On/Off 初始状态；React 宿主通过 `RuntimeBundleViewer` 接管图纸、播放与数据源。跨项目查看器和当前产品监控模式统一使用监控专用 `DiagramMonitorCanvas`，只暴露缩放控制，不能导入编辑画布、编辑命令、Store、IndexedDB 或 dirty；中键、空格+左键和触控板视口手势继续可用。两条主画布以及面板内嵌的只读小场景共用 `DiagramScenePrimitives` 中的图元主体、普通线路基线、三类标签、母线基线/节点、颜色/冷却滤镜、监控静态线路、桥面遮罩、管壳和方向箭头，并通过纯数据 `connectionScene` 统一线路显示路径、渲染分组、管壳、静态流动和相关拓扑派生；几何/视口、点阵、手势、线路外观、连接几何、标签布局和素材目录以 `scene/` 为实现源，正式路由调度、Worker 与 Hook 以 `runtime/` 为实现源。`runtime/scene/monitoring` 产品代码不得反向依赖 `editor/`；编辑事件继续通过稳定 ref/children 或画布包裹层注入。RuntimeBundle 的资产解析/内嵌和发布方式属于独立交付阶段。
- 主按钮使用浅色实底，仅用于当前上下文的主要动作；普通工具使用中性软底或透明底。
- 输入框高 32px，标签置于字段上方；属性提交以 blur/Enter 为事务边界，Escape 取消草稿。
- 图纸树重命名在原行内完成并保持 28px 行高；拖拽时只显示克制的插入线或目标行轮廓，中部表示成为子图，上/下边缘表示同级前后排序。非根图纸以拖动手柄作为入口，并提供 `Alt+方向键` 的键盘替代；根图与监控模式不呈现可拖动态。非根图纸行在 hover 或键盘焦点进入时于最右侧显示 24px 删除图标，触控环境常驻；图标使用错误色并阻止行选择与拖动事件，根图和监控模式不渲染。普通 `←/→` 只收起或展开当前含子图节点，线路标题可用 Enter/空格整组切换；折叠状态不持久化，当前图纸变化时自动展开所属线路及祖先路径。
- 对象标签显隐使用既有紧凑 switch。单对象继续独立控制；手动纯图元、纯母线或纯节点间子线多选时显示批量共同开关，混合值使用三态，点击后统一开启。共享主干展开后的多条子线可按实际所选边批量生效；母线/子线或含节点的异类混合选择不显示含义含混的标签入口。标签内容和方位仍只允许单对象编辑。
- 图元、母线和子线颜色统一使用属性面板内的 HEX 选择器，不调用系统原生 RGB/HEX 面板；展开态只提供饱和度/亮度、色相与 HEX 值。连续调整只原位预览 SVG 颜色，不更新项目、历史或线路路由；“完成”或 HEX 输入失焦后单次提交，“取消”恢复已保存值。Switch、2WV、CV 单选时分别显示“关状态颜色”和“开状态颜色”，只预览当前运行态对应的颜色，另一状态的草稿不改变当前画面。多条子线或母线/子线混合选择共用一个“线路颜色”字段和一次历史事务；选择内存在不同颜色时明确显示“多种颜色”。
- 画布未选择对象时，属性面板按图元、母线、子线分组列出当前实际使用的颜色与对象数量；Switch、2WV、CV 的关/开颜色按设备拆成明确图元颜色槽独立汇总。点击颜色后复用同一 HEX 选择器，只替换当前画布内同一设备、同一颜色槽且当前实际颜色相同的对象。默认色与自定义色均参与汇总，不支持实例改色的原始 SVG 图元不列入颜色总览。
- Switch、2WV、CV 的 On/Off 状态由编辑属性面板与监控画布共享，并作为图元快照字段进入本地项目和 JSON 导入导出；兼容运行态存储继续即时镜像，确保无需手动保存也能跨刷新恢复。切换状态不修改项目 dirty、更新时间或撤销历史。关/开颜色属于图纸实例配置，进入项目历史、复制和持久化；2WV/CV 作为制冷阀门时以同一状态覆盖 Provider 的阀门门控。
- 显式配置为 `pump` 的 CHWP、CWP 及其他水泵图元按实例保存 `coolingPumpRunning` 与 `coolingPumpOutputPower`。编辑和监控右侧面板共用同一启停/输出功率控制组件；编辑态只编辑快照且不显示派生流量，监控态在同一快照上附加模拟流量。任一面板修改都会标记 dirty 并更新项目时间，但不进入编辑历史；保存、JSON、复制与 RuntimeBundle v2 保留该快照。旧存档缺少字段时默认运行/100%，显式宿主覆盖优先；普通非双状态阀门与高频派生数据仍不持久化。
- 监控右侧面板严格由当前单选图元驱动，空选时完全隐藏，不显示占位说明。Switch 还有独立的实例级“监控点击行为”：缺省控制 On/Off，也可改为打开 MLVR 设备详情，不从设备标识推断。Generator 和单体 UPS 直接打开详情，`ups-group` 优先保留子图下探。500px 详情复用 AIDC 的连续信息面：20px 设备标题、状态/图纸/时间元数据、无卡片底色的三列摘要、452×160 SVG 趋势、紫/蓝/青三相图例与 112×28 的日/周/月等分切换，以及四列 31px 告警表；日/周/月分别覆盖最近 24 小时、7 天、30 天，并同步切换横轴为时刻或日期。趋势标题工具栏在窄窗口可换行，不得制造横向页面溢出。UPS 使用 452×360 的数据驱动只读小画布：真实素材通过 `DiagramElementVisual` 渲染，7 条拓扑边通过正式 8px 正交路由器和共享线路基线生成，主路与电池的 5 条活动支路复用监控静态流动层；全局播放开启后叠加复用监控流动颜色、6/6 虚线和速度配置的共享 SVG 移动虚线原语，暂停时只保留静态运行色，不为面板再创建第二个 WebGL 画布。旁路、主路、负载和电池数据使用共享图元标签字体、描边、行高、名称列、读数列与告警样式，正常读数在小画布内统一使用白色 `--panel-ink`；展示层允许一行包含彼此独立的电压、电流双值，不扩展或拼接全局指标 Schema。全部图元和标签的合成边界整体对齐画布中心，拓扑坐标仍保持 8px 网格，只在渲染层施加居中偏移。不得重新写死 SVG 路径或旧式 `tspan` 数据块。小场景使用局部合成锚点，不修改五类全局素材“无预置锚点”的既有边界。图元/子线标签数据列可打开紧邻读数上方的 360×233 趋势浮层，标题栏为 40px，图表带坐标、虚线网格和蓝色面积层；浮层数值来自运行快照，稳定趋势不进入项目 JSON。范围下拉固定为 76×24px，并为最长的“24小时”保留完整文字宽度，不得越过箭头或浮层边界。
- 运行指标配置属于图元实例或相邻节点间子线实例并进入项目历史与持久化；编辑模式的中间数值或正常文本状态预览不刷新，监控模式进入时立即生成模拟值并由全图单一 30 秒节拍更新。文本状态按正常 90、次要 6、重要 3、紧急 1 的等级权重抽样；两类派生值均不写入项目 JSON 或 dirty，复制对象时保留配置但重建指标 ID，不复制当前值。节点拆分时整组标签数据只保留在配置端点段，避免同一指标重复显示。
- 手动多选纯图元或纯子线时，右侧属性面板批量显示运行数据、指标名称与单位和整套运行指标配置；图元同一颜色模型还显示对应颜色，纯母线多选显示标签和颜色。共同布尔值不一致时使用三态；指标结构不一致时必须明确选择某个对象或空白模板，应用后整套覆盖并为每个目标重建指标与文本候选 ID。普通批量属性每次操作只形成一个历史事务；全部为 Switch/2WV/CV 时还可批量切换 On/Off，该运行态写入一个存储事务但不进入 dirty 或编辑历史。
- 母线 tap 与候选接入点继承所属母线颜色；子线颜色独立保存，不反向改变 tap 或母线。
- 母线接入点在界面统一称为“节点”，常态继续只显示既有 2.5px 实心点；编辑 hover/选中复用自由节点圆环，不增加第二套手柄样式。保存位置就是实际位置，单独或混合拖动受宿主母线轴向与端点范围约束；双击起线、Delete、框选和追加选择复用自由节点手势。母线缩短越过节点时手柄停止并在单次手势中只显示一条汇总 Toast。
- 对象剪贴板使用系统通用的 `Command/Ctrl+C`、`Command/Ctrl+X`、`Command/Ctrl+V`，不增加跨图纸搬移弹窗或画布浮层。切换图纸后仍可粘贴到同一线路系统，跨图纸内容以当前视口中心为落点；跨专业拒绝通过既有 Toast 反馈。两端对象均被选中的内部线路随对象迁移；框选或追加多选显式包含节点时，再复制该节点直接接触的全部子线段，未选宿主端转为同位置自由端点，且不递归带入外部对象或下一跳支路。节点副本继续保持选中；节点剪切不在当前能力范围内。
- 锚点编辑属于即时应用例外：名称与类型直接更新项目并进入弹窗内撤销历史；关闭只退出编辑，不回滚修改。
- 冷却与电力只在短状态标签中使用对应语义色；普通选中和焦点保持中性白。
- 禁用、悬停、选中和错误必须同时通过边界/明度或文字表达，不得只依赖颜色。

## 约束

- 不新增第二套组件库、第二套图标库或散落的颜色/圆角常量。
- 不让树节点、面包屑、状态栏和素材名称换行。
- 不把 Three.js/R3F 临时对象或高频监控数据写入项目 JSON。
- 不主动以截图作视觉结论；布局、质感和图元显示效果由用户验收。
